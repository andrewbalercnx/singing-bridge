# Findings Tracker: Sprint 14 (plan)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | websocket-contract-internally-in | The WebSocket contract is internally inconsistent and not implementable as written. It requires `ErrorCode::Forbidden... | ADDRESSED | Added `Forbidden` variant to `ErrorCode`. Made `AccompanimentState` a full snapshot on every broadcast — no partial updates, no client-side caching. | R1,R2,R3 |
| 2 | R1 | High | domain | score-coordinate-contract-diverg | The score-coordinate contract diverges from the authoritative accompaniment model. The plan switches `BarCoord` to ab... | ADDRESSED | Plan now uses canonical `{bar, page, x_frac, y_frac, w_frac, h_frac}` from `sidecar.rs` throughout; pixel conversion deferred to frontend render time. | R1,R2,R3 |
| 3 | R1 | High | domain | variant-playback-not-domain | Variant playback is not domain-correct. The plan drives score sync from asset-level timing data and ignores `respect_... | ADDRESSED | No variant restriction needed. The production sidecar ignores `respect_repeats` (parses but never passes to midi_to_wav), so all variants produce identical WAVs. bar_timings is derived from the same MIDI, so sync is consistent for all variants. | R1,R2,R3 |
| 4 | R1 | High | security | session-media-remains-authorized | Session media remains authorized by bearer token alone. The plan keeps `/api/media/<token>` URLs without participant ... | ADDRESSED | Tokens issued with 2-hour session TTL; blob keys stored in `AccompanimentSnapshot`; revoked immediately via `invalidate_by_blob_keys` on Stop/disconnect/teardown. | R1,R2,R3 |
| 5 | R1 | High | security | plan-leaves-live-accompaniment | The plan leaves live accompaniment tokens valid after stop and disconnect even though immediate revocation support al... | ADDRESSED | See F4. `invalidate_by_blob_keys` called before cleared-state broadcast on Stop and disconnect. Test case 5 verifies token returns 404 post-stop. | R1,R2,R3 |
| 6 | R1 | Medium | code_quality | plan-introduces-unbounded-page | The plan introduces unbounded `page_urls`, `bar_coords`, `bar_timings`, and per-page token fan-out. This breaks the r... | ADDRESSED | Explicit caps: ≤20 pages, ≤2000 bar_coords, ≤2000 bar_timings. Validation table added to §1. | R1,R2,R3 |
| 7 | R1 | Medium | code_quality | validation-rules-new-message | Validation rules for new message fields are underspecified. The plan does not define server-side checks for `position... | ADDRESSED | Validation rules specified: position_ms ≤ 14,400,000; tempo_pct 1..=400; bar_timings monotone; bar_coords fractional fields in [0,1]; out-of-range page indices skipped. | R1,R2,R3 |
| 8 | R1 | Medium | test_quality | test-plan-misses-key | The test plan misses the key pause/resume cache-retention path that the design depends on. It does not prove that sco... | ADDRESSED | Full snapshot design eliminates client-side cache. Drawer test 11 proves Pause message includes page_urls/bar_coords and seekToBar works without re-fetch. E2E test 4 confirms no new /api/media/ requests on Resume. | R1,R2,R3 |
| 9 | R1 | Medium | test_quality | boundary-failure-path-test | Boundary and failure-path test coverage is incomplete. Exact bar-boundary lookup, after-last-bar, empty timing arrays... | ADDRESSED | Added: before-first-bar (test 8), after-last-bar (test 9), empty bar_timings (test 10), audio onerror (test 15), invalid token returns 404 (WS test 5), rapid Play/Pause/Play/Stop (WS test 15, drawer test 17). | R1,R2,R3 |
| 10 | R1 | Low | security | live-session-media-should | Live session media should not reuse the current five-minute cacheable token response. Session-scoped playback URLs sh... | ADDRESSED | Added `no_cache` flag to `MediaTokenStore::Entry`; accompaniment tokens set `no_cache=true`; media endpoint returns `Cache-Control: no-store` for flagged tokens. Existing library tokens unaffected. | R1,R2,R3 |
| 11 | R1 | Low | code_quality | plan-does-not-require | The plan does not require the repository’s standard file headers and public-interface documentation for new modules a... | ADDRESSED | Standard file headers explicitly required in §2, §3, §4. | R1,R2,R3 |
| 12 | R2 | High | security | replacement-play-does-not | Replacement play does not revoke previously issued accompaniment media tokens before overwriting session state. Old b... | ADDRESSED | Added explicit step: `invalidate_by_blob_keys(&old_snapshot.all_blob_keys())` before writing new snapshot on replacement Play. WS test 16 verifies old token returns 404 and new token returns 200. | R2,R3 |
| 13 | R2 | High | domain | bar-lookup-tempo-scaling | The bar-lookup tempo scaling formula is inverted in the plan text and reinforced by the test parenthetical. The state... | ADDRESSED | Formula corrected to `(tempo_pct / 100)`: at tempo_pct=50, 10s real × 0.5 = 5.0 score seconds. Explained inline. Test 6 updated — no longer cites `(100/tempo_pct)` as "old formula". | R2,R3 |
| 14 | R2 | Medium | code_quality | handle-accompaniment-play-specif | `handle_accompaniment_play` is specified as one oversized handler with too many responsibilities. It violates the pro... | ADDRESSED | Decomposed into 7 named steps: check_role, validate_play_fields, fetch_and_validate_variant, revoke_old_tokens, issue_tokens, build_snapshot, write+broadcast. | R2,R3 |
| 15 | R2 | Medium | code_quality | raf-bar-advancement-path | The rAF bar-advancement path has no guard for `tempo_pct = null`, `0`, or negative. That produces invalid arithmetic ... | ADDRESSED | Added guard: if tempo_pct is null, 0, or ≤ 0, skip bar lookup; do not call seekToBar. Drawer test 13 covers this case. | R2,R3 |
| 16 | R2 | Medium | code_quality | cleared-state-semantics-incomple | Cleared-state semantics are incomplete because `position_ms` and `tempo_pct` are not specified for the stopped snapsh... | ADDRESSED | Cleared-state contract defined: position_ms=0, tempo_pct=None. WS test 21 asserts position_ms==0. | R2,R3 |
| 17 | R2 | Medium | test_quality | regression-guard-library-token | The regression guard for library-token caching has no enforcing test. A `no_cache` regression on non-accompaniment me... | ADDRESSED | Added enforcing test `media_token_library_cache_control` to regression guards section. | R2,R3 |
| 18 | R2 | Medium | test_quality | errorcode | The `ErrorCode::Internal` path for non-monotone `bar_timings` from DB is specified but untested. (File: `server/tests... | ADDRESSED | WS test 17 covers DB-sourced non-monotone bar_timings → ErrorCode::Internal. | R2,R3 |
| 19 | R2 | Low | code_quality | accompanimentsnapshot | `AccompanimentSnapshot::all_blob_keys()` is referenced but not defined precisely. The return shape and ordering shoul... | ADDRESSED | Defined: returns `[wav_blob_key] + page_blob_keys` (WAV first, then pages in order). | R2,R3 |
| 20 | R2 | Low | code_quality | clock-skew-correction-formula | The clock-skew correction formula is underspecified. The plan must state that skew is sampled once on receipt and the... | ADDRESSED | Skew computed once on receipt: `skewMs = clamp(server_time_ms - Date.now(), -500, 500)`; stored and added per-frame. Not resampled per rAF tick. | R2,R3 |
| 21 | R2 | Low | test_quality | drawer-coverage-conflates-bar | Drawer coverage conflates `bar_timings = null` and `bar_timings = []`. They are distinct JS cases and both must be te... | ADDRESSED | Tests 11 and 12 are now separate: null (WAV-only) vs [] (empty, score panel visible but no advance). | R2,R3 |
| 22 | R2 | Low | test_quality | boundary-acceptance-values-posit | Boundary acceptance values for `position_ms` and `tempo_pct` are not tested. Only rejection cases are covered. (File:... | ADDRESSED | Drawer tests 21 and 22 added: position_ms=14,400,000 accepted; tempo_pct=1 and 400 accepted. | R2,R3 |
| 23 | R2 | Low | test_quality | clock-skew-correction-tested | Clock-skew correction is tested only for positive clamp. The negative clamp path is untested. (File: `web/assets/test... | ADDRESSED | Drawer test 8 (positive clamp) and test 7 (negative clamp, server clock 600ms ahead → clamped to -500ms) both present. | R2,R3 |
| 24 | R2 | Low | test_quality | server-side-skipping-bar | Server-side skipping of `bar_coords` entries with out-of-range page indices is specified but untested. (File: `server... | ADDRESSED | WS test 18 covers: DB bar_coords with page=5 but only 2 pages → entry skipped silently; broadcast does not include it. | R2,R3 |
| 25 | R3 | Medium | domain | resume-from-pause-token | Resume-from-pause token handling is internally inconsistent. The lifecycle section says resume reuses stored URLs, bu... | OPEN |  | R3 |
| 26 | R3 | Medium | code_quality | seektobar-assumes-bar-coords | `seekToBar` assumes `bar_coords` is sorted by bar number, but no server invariant or frontend sort guarantees that pr... | OPEN |  | R3 |
| 27 | R3 | Low | code_quality | plan-does-not-specify | The plan does not specify how the rAF loop is cancelled on rapid state changes. A boolean stop flag is not sufficient... | OPEN |  | R3 |
| 28 | R3 | Low | code_quality | accompanimentpause-validation-no | `AccompanimentPause` validation is not explicitly decomposed. The bounds table covers `position_ms`, but the handler ... | OPEN |  | R3 |
| 29 | R3 | Low | test_quality | no-websocket-test-pins | No websocket test pins idle `AccompanimentStop` or idle `AccompanimentPause` behavior. These retry-driven states are ... | OPEN |  | R3 |
| 30 | R3 | Low | test_quality | no-rust-websocket-test | No Rust websocket test covers acceptance of `position_ms = 14_400_000`. Rejection at `14_400_001` is covered, but the... | OPEN |  | R3 |
| 31 | R3 | Low | domain | seektobar-behavior-unspecified-w | `seekToBar` behavior is unspecified when the target score image has not loaded and `naturalWidth` is zero. The plan s... | OPEN |  | R3 |
