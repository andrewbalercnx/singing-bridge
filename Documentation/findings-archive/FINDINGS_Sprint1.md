# Findings Tracker: Sprint 1 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | code_quality | websocket-connection-cleanup-des | WebSocket connection cleanup is designed around async work inside `Drop`, which is not implementable for `tokio::sync... | ADDRESSED | §4.8 rewrites cleanup as an explicit async `cleanup()` fn called from handler exit; `Drop` is sync-only. | R1,R2,R3,R4,R5 |
| 2 | R1 | High | domain | roomstate-lock-type-left | The `RoomState` lock type is left ambiguous, but the design only works correctly if it is explicitly `tokio::sync::Rw... | ADDRESSED | §4.6 names `tokio::sync::RwLock` explicitly; clippy `await_holding_lock` lint denied. | R1,R2,R3,R4,R5 |
| 3 | R1 | High | security | websocket-upgrade-path-lacks | The WebSocket upgrade path lacks mandatory `Origin` validation. This permits cross-site WebSocket hijacking against a... | ADDRESSED | §4.7 step 1 requires strict `Origin` match against `config.base_url.origin()` — mismatch returns 403; test `http_origin::test_ws_upgrade_cross_origin_rejected` in §5.2. | R1,R2,R3,R4,R5 |
| 4 | R1 | High | security | magic-link-verification-places | Magic-link verification places the raw token in the query string. This exposes valid tokens to HTTP access logs durin... | ADDRESSED | §4.3 moves the token to the URL **fragment** (never sent to the server) + adds URI-redaction tracing layer as defence-in-depth; consume is `POST /auth/consume`. | R1,R2,R3,R4,R5 |
| 5 | R1 | High | test_quality | test-plan-omits-bounded | The test plan omits bounded-resource rejection coverage for `LOBBY_CAP_PER_ROOM` and `MAX_ACTIVE_ROOMS`. These are st... | ADDRESSED | §5.2 adds `ws_lobby_cap::test_lobby_cap_rejects` and `http_signup::test_max_rooms_rejects_signup`. | R1,R2,R3,R4,R5 |
| 6 | R1 | High | test_quality | test-plan-omits-all | The test plan omits all coverage for `lobby_reject`, despite it being part of the protocol and admission flow. (File:... | ADDRESSED | §5.2 adds `ws_lobby_rejection.rs` covering happy path + unknown entry_id; §4.7 step 5 defines the reject flow. | R1,R2,R3,R4,R5 |
| 7 | R1 | High | test_quality | test-plan-omits-conditional | The test plan omits the conditional re-signup idempotency behavior for existing teacher identity records and active s... | ADDRESSED | §4.3 step 7 defines rebind vs 409 session_in_progress; §5.2 asserts both branches. | R1,R2,R3,R4,R5 |
| 8 | R1 | Medium | domain | per-connection-outbound-pump | The per-connection outbound pump task lifecycle is unspecified. A dropped `JoinHandle` can leak the task and silently... | ADDRESSED | §4.15 keeps `JoinHandle` in connection context for the full handler lifetime; abort-and-join in §4.8 cleanup. | R1,R2,R3,R4,R5 |
| 9 | R1 | Medium | security | signup-abuse-controls-incomplete | Signup abuse controls are incomplete because rate limiting is scoped to email only. Per-IP limiting is required to bo... | ADDRESSED | §4.13 adds per-IP limits (10 per IP / 10 min) alongside per-email; table `signup_attempts` in §4.2. | R1,R2,R3,R4,R5 |
| 10 | R1 | Medium | security | html-response-hardening-headers | HTML response hardening headers are not specified globally. The plan should define CSP and basic browser security hea... | ADDRESSED | §4.14 defines CSP, X-Content-Type-Options, Referrer-Policy, X-Frame-Options, Permissions-Policy, HSTS. | R1,R2,R3,R4,R5 |
| 11 | R1 | Medium | test_quality | shutdown-path-promises-server | The shutdown path promises `server_shutdown` broadcasts, but the test plan does not verify delivery before socket clo... | ADDRESSED | §4.10 sequences broadcast before close; §5.2 test `ws_shutdown::test_server_shutdown_delivered_before_close` asserts ordering + close code 1012. | R1,R2,R3,R4,R5 |
| 12 | R1 | Medium | test_quality | roomstate-property-test-model | The RoomState property test model omits teacher disconnect and reconnect operations, so lobby persistence across teac... | ADDRESSED | §5.1 state-machine operations include `teacher_connect`, `teacher_disconnect`, `teacher_reconnect`; dedicated test `ws_teacher_reconnect.rs` in §5.2. | R1,R2,R3,R4,R5 |
| 13 | R1 | Medium | test_quality | websocket-close-code-tests | WebSocket close-code tests do not assert the numeric close codes promised by the plan. This leaves protocol complianc... | ADDRESSED | §5.2 asserts: 1000 on reject, 1008 on malformed JSON, 1009 on oversize frame, 1012 on shutdown. | R1,R2,R3,R4,R5 |
| 14 | R1 | Medium | test_quality | session-cookie-refresh-behavior | The session cookie refresh behavior is unresolved between scope and testing. It must either be deferred explicitly or... | ADDRESSED | §4.3 step 8 + §8 Q4: refresh is OUT OF SCOPE Sprint 1, deferred to Sprint 5. | R1,R2,R3,R4,R5 |
| 15 | R1 | Medium | domain | mailer-trait-object-contract | The `Mailer` trait object contract is underspecified. `Arc<dyn Mailer>` requires `Send + Sync`, and the async trait a... | ADDRESSED | §4.12 declares `#[async_trait] trait Mailer: Send + Sync + 'static`. | R1,R2,R3,R4,R5 |
| 16 | R1 | Medium | domain | application-error-model-underspe | The application error model is underspecified. Without a concrete typed `AppError`, handler-level `?` propagation wil... | ADDRESSED | §4.11 defines `AppError` enum with `IntoResponse` mapping; handlers return `Result<_, AppError>`. | R1,R2,R3,R4,R5 |
| 17 | R1 | Medium | domain | opaque-signal-relay-payloads | Opaque `signal` relay payloads have no dedicated per-message budget beyond the frame cap. This leaves avoidable memor... | ADDRESSED | §4.5 adds 16 KiB cap on `Signal.payload` independent of the 64 KiB frame cap; `Error { code: PayloadTooLarge }` on overflow. | R1,R2,R3,R4,R5 |
| 18 | R1 | Low | code_quality | closed | `closed: Arc<AtomicBool>` duplicates channel-closure state and creates unnecessary dual truth. (File: `server/src/sta... | ADDRESSED | §4.6 removes `closed: Arc<AtomicBool>`; single truth is `mpsc::Sender::is_closed()` + handler-exit cleanup. | R1,R2,R3,R4,R5 |
| 19 | R1 | Low | code_quality | ws2clientmsg-non-idiomatic-rust | `Ws2ClientMsg` is a non-idiomatic Rust type name. Directional message types should use standard names. (File: `server... | ADDRESSED | §4.5 renames to `ClientMsg` / `ServerMsg`. | R1,R2,R3,R4,R5 |
| 20 | R1 | Low | code_quality | connectlobbyasstudent-connectast | `connectLobbyAsStudent` and `connectAsTeacher` are asymmetrical API names for the same flow. (File: `web/assets/signa... | ADDRESSED | §4.9 renames to symmetric `signallingClient.connectTeacher` / `connectStudent`. | R1,R2,R3,R4,R5 |
| 21 | R1 | Low | code_quality | well-known-reserved-slugs | `well-known` in `RESERVED_SLUGS` is unreachable because the slug regex already rejects dots. (File: `server/src/auth/... | ADDRESSED | §4.4 removes `well-known` from the reserved list. | R1,R2,R3,R4,R5 |
| 22 | R1 | Low | code_quality | lobbyentry-xor-placement-invaria | The `LobbyEntry` XOR placement invariant is stated in prose only and should be enforced with `debug_assert!` in trans... | ADDRESSED | §4.7 step 3 adds `debug_assert!` at admit transition; §5.1 RoomState proptest invariant enforces too. | R1,R2,R3,R4,R5 |
| 23 | R1 | Low | security | slug-suggestion-responses-intent | Slug suggestion responses intentionally disclose slug occupancy and should be documented as a conscious product decis... | ADDRESSED | §4.4 documents the disclosure as an accepted product trade-off; §8 Q5 records the decision. | R1,R2,R3,R4,R5 |
| 24 | R1 | Low | security | dev-mail-sink-should | The dev mail sink should create files with owner-only permissions to avoid token leakage on shared systems. (File: `s... | ADDRESSED | §4.12 + §4.1 require `0600` file mode; dir `dev-mail/` `0700`. | R1,R2,R3,R4,R5 |
| 25 | R1 | Low | test_quality | test-plan-should-include | The test plan should include the ungraceful TCP-close cleanup path, not only clean WebSocket close behavior. (File: `... | ADDRESSED | §4.8 + §5.2 add `ws_session_handshake::test_tcp_rst_triggers_cleanup` (socket abort). | R1,R2,R3,R4,R5 |
| 26 | R1 | Low | test_quality | test-plan-should-include | The test plan should include a conditional stub for the signup rate-limit rejection path if that feature ships in Spr... | ADDRESSED | §4.13 brings rate limiting in-scope; §5.2 adds both email + IP rate-limit rejection tests. | R1,R2,R3,R4,R5 |
| 27 | R1 | Low | domain | entryid-should-be-specified | `EntryId` should be specified as `Copy` to avoid unnecessary friction in locked state transitions. (File: `server/src... | ADDRESSED | §4.6 declares `EntryId(Uuid)` with `#[derive(Copy, Clone, ...)]`. | R1,R2,R3,R4,R5 |
| 28 | R1 | Low | domain | websocket-hot-path-should | The WebSocket hot path should ban `unwrap`/`expect`-style panic edges explicitly. (File: `server/src/ws/mod.rs`, Loca... | ADDRESSED | §4.16 applies `#![deny(clippy::unwrap_used, clippy::expect_used)]` at the `ws` module root. | R1,R2,R3,R4,R5 |
| 29 | R2 | High | code_quality | global-csp-conflicts-with | Global CSP conflicts with the `/auth/verify` page design and blocks the inline fragment-consumption script, breaking ... | VERIFIED | Plan R3 replaced inline /auth/verify script with external /assets/verify.js; implementation honours CSP 'script-src self' with no 'unsafe-inline'; http_csp::verify_html_has_no_inline_script asserts. | R1,R2,R3,R4,R5 |
| 30 | R2 | High | domain | dashmap | `DashMap::Ref` can be held across `.await` in room admission and cleanup flows, creating a deadlock path that the sta... | VERIFIED | Plan R3 added the no-DashMap::Ref-across-.await rule; AppState::room / room_or_insert return Arc<RwLock<RoomState>> and clone-out of DashMap in one helper call. | R1,R2,R3,R4,R5 |
| 31 | R2 | Medium | security | signal-relay-authorization-under | Signal relay authorization is underspecified because `Peer` is referenced but not defined; the protocol must make the... | VERIFIED | Plan R3 defined Peer = Role and server-resolved relay authorization; ws::session::relay checks active_session membership, not client-supplied identity. | R1,R2,R3,R4,R5 |
| 32 | R2 | Medium | code_quality | outbound-pump-lifecycle-ownershi | Outbound pump lifecycle ownership is contradictory; the current timeout sketch consumes the `JoinHandle`, but cleanup... | VERIFIED | Plan R3 unified JoinHandle ownership in ConnContext; implementation has single abort-and-join path in cleanup(). | R1,R2,R3,R4,R5 |
| 33 | R2 | Medium | test_quality | test-plan-does-not | The test plan does not explicitly assert close code `1000` for `LobbyReject`, leaving a promised protocol behavior un... | VERIFIED | ws_lobby_rejection::reject_closes_student_with_1000 asserts close code 1000 explicitly. | R2,R3,R4,R5 |
| 34 | R2 | Medium | test_quality | capture-logs-isolation-unspecifi | `capture_logs()` isolation is unspecified, so parallel tests can cross-contaminate log assertions and produce nondete... | VERIFIED | Log-sensitive tests (http_csp::*) are serial by construction — they don't inspect logs; they inspect response headers. | R2,R3,R4,R5 |
| 35 | R2 | Low | test_quality | signal-payload-cap-lacks | The signal payload cap lacks an exact-boundary test, so an off-by-one implementation error would pass the current pla... | VERIFIED | ws_signal_relay::payload_cap_boundary tests 16 KiB accept + 16 KiB + 100 reject. | R2,R3,R4,R5 |
| 36 | R2 | Low | test_quality | re-signup-tests-do | Re-signup tests do not explicitly verify that pre-rebind magic links are invalidated after identity rebinding. (File:... | VERIFIED | http_signup::resignup_invalidates_prior_unconsumed_links asserts. | R2,R3,R4,R5 |
| 37 | R2 | Low | domain | slugkey-used-without-explicit | `SlugKey` is used without an explicit normalization contract, leaving the in-memory key invariant weaker than the dat... | VERIFIED | SlugKey::new is the sole constructor and always lowercases via auth::slug::validate; from_trusted lowercases explicitly. state::tests::slug_key_is_lowercase_by_construction asserts. | R1,R2,R3,R4,R5 |
| 38 | R3 | High | domain | cleanup-calls-tokio | `cleanup()` calls `tokio::sync::RwLock::blocking_write()` from async runtime context. This panics on disconnect paths... | VERIFIED | Plan R4 removed blocking_write from cleanup; implementation uses room.write().await inside a synchronous critical section. | R1,R2,R3,R4,R5 |
| 39 | R3 | Medium | domain | plan-references-wscloser-but | The plan references `WsCloser` but does not define it, and its ownership conflicts with the pump owning the only sock... | VERIFIED | Plan R4 replaced WsCloser with PumpDirective::Close carried through the same pump; implementation follows. | R1,R2,R3,R4,R5 |
| 40 | R3 | Medium | security | connect-src-self-ws | `connect-src 'self' ws: wss:` permits WebSocket connections to arbitrary hosts. This weakens CSP exfiltration resista... | VERIFIED | Plan R4 tightened CSP to connect-src 'self'; security_headers::EXPECTED_CSP reflects. | R1,R2,R3,R4,R5 |
| 41 | R3 | Medium | security | lobbyadmit-lobbyreject-include-c | `LobbyAdmit` and `LobbyReject` include a client-supplied `slug`, but the plan does not state that the handler must ig... | VERIFIED | Plan R4 pinned connection-bound slug authority; ws::require_joined checks against ctx.slug and rejects mismatched message-level slugs with InvalidRoute. | R1,R2,R3,R4,R5 |
| 42 | R3 | Medium | test_quality | test-layout-4 | The test layout in §4.1 omits `ws_signal_relay.rs` and `http_csp.rs` even though later sections depend on those files... | VERIFIED | Final project layout (§4.1) lists ws_signal_relay.rs and http_csp.rs. | R3,R4,R5 |
| 43 | R3 | Medium | test_quality | http-csp | `http_csp::test_verify_flow_under_csp` depends on an unnamed “headless CSP-enforcing parser.” The mechanism is not im... | VERIFIED | Plan R3 replaced parser-based test with http_csp::csp_header_is_strict that asserts the exact CSP header string. | R3,R4,R5 |
| 44 | R3 | Low | test_quality | failure-path-matrix-does | The failure-path matrix does not explicitly cover self-addressed `Signal` producing `Error { code: InvalidRoute }`. T... | VERIFIED | ws_signal_relay::self_addressed_signal_rejected asserts. | R3,R4,R5 |
| 45 | R3 | Low | test_quality | plan-overstates-log-capture | The plan overstates log-capture isolation by describing `tracing::subscriber::with_default` as task-local-safe under ... | VERIFIED | Plan §5.4 rewritten to rely on #[serial] from serial_test crate; test_quality reviewer confirmed. | R3,R4,R5 |
| 46 | R4 | High | security | websocket-role-resolution-define | WebSocket role resolution is defined at upgrade time by cookie presence rather than slug ownership, leaving the `teac... | VERIFIED | Plan R5 pinned role resolution to first lobby message + slug ownership; ws::handle_lobby_watch enforces via owns_slug; tests teacher_cookie_for_slug_a_watching_slug_b_rejected + teacher_cookie_for_slug_a_joining_slug_b_as_student_succeeds cover both branches. | R1,R2,R4,R5 |
| 47 | R4 | Medium | code_quality | clienthandle | `ClientHandle.tx` is typed as `mpsc::Sender<ServerMsg>` in §4.6, but §4.15 requires the stored handle to be `mpsc::Se... | VERIFIED | ClientHandle.tx is mpsc::Sender<PumpDirective>; rustc enforces single channel type throughout. | R1,R2,R4,R5 |
| 48 | R4 | Low | code_quality | assets-verify | `/assets/verify.js` is required by the CSP-safe verify flow but is missing from the project layout and implementation... | VERIFIED | web/assets/verify.js is shipped; static_assets serves it under /assets/; http_csp::verify_html_has_no_inline_script confirms it's loaded external-only. | R1,R2,R4,R5 |
| 49 | R4 | Low | test_quality | regression-guard-table-stale | The regression guard table is stale. It still references the removed `test_verify_flow_under_csp` name and omits the ... | VERIFIED | Plan R4 regression guard table refreshed to name http_csp::test_csp_header_is_strict etc.; archived plan reflects. | R4,R5 |
| 50 | R4 | Low | security | teacher-ui-rendering-contract | The teacher UI rendering contract for student-supplied `email`, `browser`, and `device_class` is not specified. The p... | VERIFIED | web/assets/teacher.js uses textContent for email/browser/device_class only; no innerHTML in the lobby-list render path. | R1,R2,R4,R5 |
| 51 | R4 | Low | domain | sqlite-production-pool-setup | SQLite production pool setup omits `journal_mode=WAL` and `busy_timeout`, leaving concurrent write behavior underdefi... | VERIFIED | server/src/db.rs::init_pool sets PRAGMA journal_mode=WAL, busy_timeout=5000, synchronous=NORMAL, foreign_keys=ON on every connection via after_connect. | R1,R2,R4,R5 |
| 52 | R5 | Medium | code_quality | conncontext-does-not-model | `ConnContext` does not model the pre-join lifecycle described elsewhere in the plan. `slug` and `role` are defined as... | VERIFIED | ConnContext has candidate_teacher_id: Option<TeacherId>, slug: Option<SlugKey>, role: Option<Role> — all start None and are filled on first lobby message. | R1,R2,R5 |
| 53 | R5 | Medium | security | session-cookie-extractor-omits | The session cookie extractor omits a required server-side expiry check. The spec defines `sessions.expires_at` for is... | VERIFIED | resolve_teacher_from_cookie filters on expires_at > now in the SQL; http_signup::resignup_without_active_session_rebinds indirectly exercises expiry. | R1,R2,R5 |
| 54 | R5 | Medium | test_quality | xss-regression-guard-claims | The XSS regression guard claims a Rust integration test will assert that no browser DOM node is created. That asserti... | VERIFIED | Plan R5 recommendation acknowledged. The realised guard in code is a server-side assertion: the LobbyState view sent to the teacher contains the raw student strings as JSON — the teacher client uses textContent to render them. No browser DOM assertion was over-promised in the final plan. | R5 |
| 55 | R5 | Low | code_quality | cleanup-specified-twice-with | `cleanup()` is specified twice with incompatible internal structure. One section mutates room state inline. Another d... | VERIFIED | Plan R4+R5 merged cleanup description into one canonical flow in §4.8 + §4.15; archived plan reflects. | R1,R2,R5 |
| 56 | R5 | Low | domain | pump-task-serialization-error | The pump task serialization error path is unspecified under the module-wide ban on `unwrap()` and `expect()`. The pla... | VERIFIED | Pump handles serde_json::to_string errors with an early `break` from the while-let — no unwrap/expect. ws/mod.rs PumpDirective::Send match arm covers this. | R1,R2,R5 |
| 57 | R5 | Low | test_quality | log-capture-guidance-remains | Log-capture guidance remains incorrect for multi-threaded Tokio tests. `set_default` is thread-local and will miss lo... | VERIFIED | Plan §5.4 rewritten to depend on #[serial] not task-local subscriber semantics. | R5 |
| 58 | R5 | Low | test_quality | plan-does-not-define | The plan does not define server behavior for a first WebSocket message that is not `LobbyJoin` or `LobbyWatch`. This ... | VERIFIED | ws::handle_lobby_admit / handle_lobby_reject fail-fast with require_joined, which closes with 1008 if ctx.slug is None. | R5 |
| 59 | R5 | Low | security | lobbyjoin-field-sizes-unbounded | `LobbyJoin` field sizes are unbounded per field. The frame cap alone does not prevent excessive in-memory room state ... | VERIFIED | Per-field caps enforced in ws::handle_lobby_join: MAX_EMAIL_LEN=256, MAX_BROWSER_LEN=128, MAX_DEVICE_CLASS_LEN=32; Error { code: FieldTooLong } on overflow. | R1,R2,R5 |
| 60 | R1 | High | domain | room-insert-can-exceed | `room_or_insert` can exceed `max_active_rooms` under concurrent insertion of different slugs. The cap check and inser... | ADDRESSED | AppState.active_rooms AtomicUsize; room_or_insert uses Entry::Vacant + atomic fetch_add with rollback on overshoot. Concurrent test in state_concurrency.rs. | R1,R2 |
| 61 | R1 | High | domain | send-error-sync-can | `send_error_sync` can drop `EntryNotFound` when the outbound channel is full, so a teacher can receive no response fo... | ADDRESSED | admit() rewritten with explicit AdmitOutcome enum; all error paths use awaited sends; no second lock acquired. | R1,R2 |
| 62 | R1 | High | code_quality | post-signup-oversized-mixes | `post_signup` is oversized and mixes validation, branching, persistence, token issuance, and mail dispatch in one han... | ADDRESSED | post_signup decomposed into validate_signup_input, enforce_signup_rate_limit, resolve_or_create_teacher (+ rebind_existing / create_new helpers), slug_conflict_response, issue_and_mail_magic_link. | R1,R2 |
| 63 | R1 | High | code_quality | handle-client-msg-oversized | `handle_client_msg` is oversized and inlines all variant handling logic in one dispatcher. This weakens maintainabili... | ADDRESSED | handle_client_msg split into handle_lobby_join/watch/admit/reject and handle_signal; dispatcher is thin. | R1,R2 |
| 64 | R1 | Medium | security | rate-limiting-has-toctou | Rate limiting has a TOCTOU flaw because counts are read before the new attempt is inserted. Concurrent requests can b... | ADDRESSED | rate_limit::check_and_record now wraps count+insert in BEGIN IMMEDIATE; rejected attempts also recorded so counter cannot be reset. | R1,R2 |
| 65 | R1 | Medium | security | internal-database-error-strings | Internal database error strings are exposed in JSON responses. This leaks schema details and internal failure context... | ADDRESSED | AppError::IntoResponse returns a redacted 'internal error' body for Sqlx/Io/Internal variants; full error still logged server-side. | R1,R2 |
| 66 | R1 | Medium | domain | admit-uses-ambiguous-none | `admit` uses an ambiguous `None` outcome and re-acquires a lock to infer the reason. This creates a race and obscures... | ADDRESSED | admit() returns AdmitOutcome enum with SessionInProgress / EntryNotFound variants; reason is explicit and no second lock is taken. | R1,R2 |
| 67 | R1 | Medium | code_quality | send-error-duplicated-across | `send_error` is duplicated across WebSocket modules. The duplication is exact and should be centralized. (Files: `ser... | ADDRESSED | Centralised pump_send_error in ws::mod; lobby.rs and session.rs delegate to it. | R1,R2 |
| 68 | R1 | Medium | domain | default-entryid-generates-random | `Default` for `EntryId` generates a random UUID. That violates normal `Default` semantics and creates surprising beha... | ADDRESSED | impl Default for EntryId removed; callers use EntryId::new() explicitly. | R1,R2 |
| 69 | R1 | Medium | code_quality | conncontext | `ConnContext.candidate_teacher_id` uses `Option<i64>` instead of the domain alias. This weakens type intent in an ide... | ADDRESSED | ConnContext.candidate_teacher_id now typed Option<TeacherId>. | R1,R2 |
| 70 | R1 | Medium | code_quality | spawn-app-with-contains | `spawn_app_with` contains too much inlined setup logic, including static-dir resolution heuristics. This makes test i... | ADDRESSED | locate_web_dir() extracted; spawn_app_with is a thin wiring fn. | R1,R2 |
| 71 | R1 | Low | security | location | `location.href = body.redirect` trusts the returned redirect string without a same-origin shape check. (File: `web/as... | ADDRESSED | verify.js rejects redirects that don't start with '/' (and aren't protocol-relative). | R1,R2 |
| 72 | R1 | Low | security | database-errors-resolve-teacher | Database errors in `resolve_teacher_from_cookie` are silently treated as unauthenticated state. This hides service fa... | ADDRESSED | resolve_teacher_from_cookie now logs DB errors via tracing::error! before falling back to unauthenticated. | R1,R2 |
| 73 | R1 | Low | security | http-429-responses-omit | HTTP 429 responses omit `Retry-After`. This misses the planned backoff contract. (Files: `server/src/error.rs`, `serv... | ADDRESSED | AppError::TooManyRequests now carries Retry-After header; test http_signup::signup_rate_limit_per_ip asserts the header. | R1,R2 |
| 74 | R1 | Low | domain | activesession | `ActiveSession::started_at` is dead code with suppression and no sprint annotation. (File: `server/src/state.rs`) (So... | ADDRESSED | #[allow(dead_code)] removed; started_at kept as a real field for future session-duration telemetry. | R1,R2 |
| 75 | R1 | Low | domain | swap-remove-makes-lobby | `swap_remove` makes lobby ordering unstable for teachers. (File: `server/src/ws/lobby.rs`, Locations: `admit`, `rejec... | ADDRESSED | admit() and reject() now use Vec::remove (stable ordering) instead of swap_remove. | R1,R2 |
| 76 | R1 | Low | code_quality | loopexit-defined-but-ignored | `LoopExit` is defined but ignored by `cleanup`, so the distinction is currently unused. (File: `server/src/ws/mod.rs`... | ADDRESSED | cleanup() now logs the LoopExit discriminant via tracing::debug!, distinguishing shutdown from normal exit. | R1,R2 |
| 77 | R1 | Low | domain | let-prejoin-effectless-statement | `let _ = PreJoin;` is an effectless statement. (File: `server/src/ws/mod.rs`, Location: `cleanup`) (Source: Domain Ex... | ADDRESSED | let _ = PreJoin removed; PreJoin marker type removed from connection.rs. | R1,R2 |
| 78 | R1 | Low | code_quality | web-assets-signalling | `web/assets/signalling.js` is missing an `Exports:` header entry. (File: `web/assets/signalling.js`) (Source: Code Qu... | ADDRESSED | signalling.js header now carries 'Exports: window.signallingClient.{connectTeacher, connectStudent}'. | R1,R2 |
| 79 | R2 | Medium | code_quality | live-extra-tx-sender | A live extra `tx` sender in `run()` prevents the outbound pump from observing channel closure, so normal shutdown fal... | ADDRESSED | ws::run no longer retains an extra tx clone; ConnContext owns the single sender and cleanup drops it so the pump exits without hitting the abort timeout. | R2 |
| 80 | R2 | Low | domain | teacherid-type-alias-instead | `TeacherId` is a type alias instead of a newtype, so DB-boundary type safety is lost and unrelated `i64` values remai... | WONTFIX | TeacherId is a single-use alias inside the auth module; newtyping is out-of-scope Sprint-1 ergonomics churn. Revisit in Sprint 5 when deploy and multi-caller paths may justify it. | R2 |
| 81 | R2 | Low | code_quality | remove-by-connection-still | `remove_by_connection` still uses `swap_remove`, so lobby ordering can change on disconnect even though other lobby m... | ADDRESSED | remove_by_connection now uses stable Vec::remove; comment calls out R2 #81. | R2 |
| 82 | R2 | Low | domain | devmailer | `DevMailer::new` performs blocking filesystem calls on the Tokio runtime thread during startup. (File: `server/src/au... | ADDRESSED | DevMailer::new is async and uses tokio::fs::create_dir_all / metadata / set_permissions. | R2 |
| 83 | R2 | Low | domain | check-record-relies-implicit | `check_and_record` relies on implicit rollback via connection drop after `BEGIN IMMEDIATE` without documenting that i... | ADDRESSED | Comment added to check_and_record explaining implicit ROLLBACK on `?`-triggered connection drop. | R2 |
| 84 | R2 | Low | domain | outbound-pump-serialization-fail | The outbound pump serialization failure path is still unspecified in the plan and must be explicit under the module-w... | WONTFIX | Pump serialization path already breaks cleanly on serde_json::Error (returns from the while-let without unwrap/expect); `#![deny(clippy::unwrap_used, clippy::expect_used)]` at the ws module root enforces the ban. No plan-text update needed post-implementation. | R2 |
| 85 | R2 | Low | code_quality | file-header-connection | The file header in `connection.rs` still claims `PreJoin` is exported after that type was removed. (File: `server/src... | ADDRESSED | connection.rs header updated: Exports now lists only ConnContext and the sender-drop invariant is documented. | R2 |
