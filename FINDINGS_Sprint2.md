# Findings Tracker: Sprint 2 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | test_quality | mungesdpforopusmusic-has-no-ci | `mungeSdpForOpusMusic` has no CI-enforced test path, and the planned browser-only self-tests do not satisfy release g... | ADDRESSED | R2: SDP munger extracted to `web/assets/sdp.js` with a UMD shim so Node can import it. `web/assets/tests/sdp.test.js` uses Node's built-in `node:test` runner and is added as a CI step in `.github/workflows/ci.yml` (§3, §4.3, §5.1, §10 steps 1-4). Nine fixtures including three real-browser captures + six synthetic boundary cases. | R1,R2,R3,R4 |
| 2 | R1 | High | code_quality | attachremoteaudio-contract-inter | The `attachRemoteAudio` contract is internally inconsistent across the plan, including parameter shape and target ele... | ADDRESSED | R2: Single contract documented in §3 module surface, implemented in §4.5, referenced identically in §4.4 wiring and §4.6 HTML. Signature: `attachRemoteAudio(ev: RTCTrackEvent) -> void`; DOM target: `#remote-audio`. Regression guard added to `http_teach_debug_marker.rs` (asserts both HTMLs carry `id="remote-audio"` and `id="unmute-audio"`, §5.3). | R1,R2,R3,R4 |
| 3 | R1 | Medium | code_quality, security | debug-gate-inconsistently-specif | The debug gate is inconsistently specified. The plan mixes a server-controlled meta-tag model with a mutable `window.... | ADDRESSED | R2: `window.SB_DEBUG` removed entirely. Sole debug gate is `document.querySelector('meta[name="sb-debug"]')`. In-browser self-tests removed from `audio.js` — all JS tests now run under Node (see finding #1). Grep guard `rg 'SB_DEBUG' web/assets` returning zero matches is on the sprint-exit checklist (§5.3, §10 step 19). | R1,R2,R3,R4 |
| 4 | R1 | Medium | test_quality | sdp-munger-fixture-coverage | SDP munger fixture coverage is incomplete for key boundary cases, including trailing-line insertion, empty `fmtp` rep... | ADDRESSED | R2: Three boundary fixtures added to `SDP_FIXTURES` — `empty_fmtp`, `trailing_rtpmap`, `mixed_line_endings` — each with a matching property assertion in §5.1. | R1,R2,R3,R4 |
| 5 | R1 | Medium | test_quality | remote-audio-handling-lacks | Remote audio handling lacks coverage for duplicate-track idempotency and autoplay-blocked recovery, leaving two user-... | ADDRESSED | R2: Idempotency: `attachRemoteAudio` uses `ev.track.id` to detect duplicates; pure predicate `hasTrack(stream, id)` extracted and Node-tested in `web/assets/tests/audio.test.js` (§5.2, §10 steps 5-6). Autoplay recovery: `play()` promise rejection caught and `#unmute-audio` button revealed; documented as a required manual verification step in the PR description (§5.2). | R1,R2,R3,R4 |
| 6 | R1 | Medium | code_quality | debug-overlay-polling-loop | The debug overlay polling loop has no teardown path, so the plan leaves a live timer attached to a closed or failed `... | ADDRESSED | R2: `startDebugOverlay(pc)` returns `{ stop() }` in every code path, including the disabled-in-prod no-op. Caller (signalling.js) invokes `.stop()` from `onPeerDisconnected` and `hangup()` alongside `wireBidirectionalAudio`'s `teardown()` (§4.7, §10 step 8). | R1,R2,R3,R4 |
| 7 | R1 | Medium | domain | loopback-implementation-plan-use | The loopback implementation plan uses `AppError::Internal` string wrapping for file reads instead of the existing `Fr... | ADDRESSED | R2: `get_loopback` uses bare `?` on `tokio::fs::read_to_string` so `io::Error` flows into `AppError::Io` via the existing `From` impl (§4.9). New test `test_loopback_missing_file_returns_internal_error` covers the path (§5.2). | R1,R2,R3,R4 |
| 8 | R1 | Medium | code_quality, test_quality | plan-claims-all-html | The plan claims `all_html_responses_carry_csp` is already parameterised over `dev`, but the reviewed test is not. The... | ADDRESSED | R2: Plan now treats parameterisation as new work. Existing `test_all_html_responses_carry_csp` is split into `_dev` and `_prod` variants using the new `TestOpts.dev` field. Listed as deliverable in §4.1, §4.10, §5.2, §10 step 17. | R1,R2,R3,R4 |
| 9 | R1 | Medium | code_quality | loopback-measurement-design-does | The loopback measurement design does not commit to either `ScriptProcessorNode` or `AudioWorkletNode`, so the file la... | ADDRESSED | R2: Committed to `AudioWorkletNode` with explicit rationale (render-thread, not deprecated). New file `web/assets/loopback-worklet.js` added to §4.1 layout and §4.8 design. §3 alternatives records the rejection of `ScriptProcessorNode`. | R1,R2,R3,R4 |
| 10 | R1 | Low | test_quality, code_quality | plan-omits-explicit-tracking | The plan omits explicit tracking for `TestApp::get_html`, the loopback missing-file error test, the real-browser SDP ... | ADDRESSED | R2: Each item explicitly tracked — `TestApp::get_html` in §4.10 and §10 step 14; `test_loopback_missing_file_returns_internal_error` in §5.2 and §10 step 16; real-browser SDP fixture capture in §5.1 fixture list and §5.2 manual deliverables (committed to `sdp.js` before merge); `PULSE_COUNT`/`PULSE_HZ`/`PULSE_MS`/`PULSE_SPACING_MS` named constants in §4.8 and §10 step 10. | R1,R2,R3,R4 |
| 11 | R2 | High | security | sharedarraybuffer-loopback-desig | `SharedArrayBuffer` in the loopback design will fail at runtime because `/loopback` is not specified to send `Cross-O... | ADDRESSED | R3: Loopback redesigned to use `MessagePort.postMessage(ArrayBuffer)` with transfer instead of `SharedArrayBuffer`; no COOP/COEP headers required. `http_loopback.rs::test_dev_loopback_serves_html` asserts the absence of both headers as a regression guard; grep guard at sprint exit blocks reintroduction (§3 alternatives, §4.8, §9 decision #8, §10 step 21). | R2,R3,R4 |
| 12 | R2 | Medium | domain | testopts | `TestOpts.dev` is documented as defaulting to `true`, but Rust `derive(Default)` would set it to `false`. Existing te... | ADDRESSED | R3: Hand-written `impl Default for TestOpts` sets `dev: true` explicitly (§4.10); no `derive(Default)`. Existing Sprint 1 tests keep their dev-mode semantics. | R2,R3,R4 |
| 13 | R2 | Medium | code_quality | hastrack-contract-inconsistent | The `hasTrack` contract is inconsistent. §4.5 shows the duplicate-track check inlined, while §5.2 and §10 require an ... | ADDRESSED | R3: `hasTrack(stream, id)` extracted as a pure helper in `audio.js`; `attachRemoteAudio` delegates to it (§3 module surface, §4.5). Node-tested in `web/assets/tests/audio.test.js` (§4.1, §10 step 6). | R2,R3,R4 |
| 14 | R2 | Medium | code_quality | web-assets-tests-audio | `web/assets/tests/audio.test.js` is required by the test plan but missing from the file-layout delta. An editor follo... | ADDRESSED | R3: File added to §4.1 layout and §10 step 6 checklist entry. | R2,R3,R4 |
| 15 | R2 | Low | test_quality | test-dev-loopback-serves | `test_dev_loopback_serves_html` is specified to assert an unnamed “recognisable harness string.” That assertion is to... | ADDRESSED | R3: Assertion tightened to the deterministic DOM identifier `id="loopback-start"` (§5.2); also asserts absence of COOP/COEP per finding #11. | R2,R3,R4 |
| 16 | R2 | Low | test_quality | checklist-does-not-explicitly | The checklist does not explicitly carry forward the requirement to extend `verify_html_has_no_inline_script` to `teac... | ADDRESSED | R3: Explicit checklist step 19 extends `verify_html_has_no_inline_script` to `/teach/<slug>` (post-replacement) and `/loopback`. | R2,R3,R4 |
| 17 | R2 | Low | code_quality | startdebugoverlay-pc-documents-r | `startDebugOverlay(pc)` documents `renderSettings(panel)` reading `track.getSettings()` but does not specify how the ... | ADDRESSED | R3: Signature changed to `startDebugOverlay(pc, {localTrack}) -> {stop()}`. Caller passes `local.track` from `wireBidirectionalAudio` at startup (§4.7, §10 step 8). | R2,R3,R4 |
| 18 | R2 | Low | test_quality | test-budget-count-wrong | The test budget count is wrong. The text says five new tests while enumerating six. (File: `PLAN_Sprint2.md`, Locatio... | ADDRESSED | R3: §5.5 now says "six new tests" matching the enumeration. | R2,R3,R4 |
| 19 | R3 | Medium | code_quality | startdebugoverlay-remains-incons | `startDebugOverlay` remains inconsistently specified across the plan after the R3 signature change. The implementatio... | ADDRESSED | R4: §4.7 now shows the consistent implementation form `startDebugOverlay(pc, opts)` with `opts.localTrack`. Both `connectTeacher` and `connectStudent` call it identically as `startDebugOverlay(pc, { localTrack: audio.local.track })`. §5.3 guard updated. | R2,R3,R4 |
| 20 | R3 | Medium | test_quality | hastrack-test-plan-under | The `hasTrack` test plan is under-specified. The plan still allows a single small test even though the helper has fou... | ADDRESSED | R4: §5.2 now documents all six `hasTrack` tests: present-id, absent-id, empty-stream, null/invalid-stream, invalid-id (empty/null/non-string), null entries in tracks array. Matches `audio.test.js`. Regression guard updated in §5.3. | R2,R3,R4 |
| 21 | R3 | Low | test_quality | plan-does-not-name | The plan does not name which test owns the structural assertion for `#remote-audio` and `#unmute-audio` across both H... | ADDRESSED | R4: §5.3 R1 #2 guard now explicitly names `test_dev_teach_html_carries_debug_marker` in `http_teach_debug_marker.rs` as the owner of the structural assertion. | R2,R3,R4 |
| 22 | R3 | Low | security | debug-overlay-spec-does | The debug-overlay spec does not explicitly require `renderSdp`, `renderStats`, and `renderSettings` to use text-only ... | ADDRESSED | R4: §4.7 now explicitly states the textContent-only contract — `buildPanel`, `setRow`, `renderSdp`, `renderStats`, `renderSettings` may only write via `element.textContent`; `innerHTML`/`insertAdjacentHTML`/`on*` attributes are prohibited; `setRow` is the single write path. | R2,R3,R4 |
| 23 | R3 | Low | domain | get-teach-specified-call | `get_teach` is specified to call `String::replace` on every request even in production, which adds avoidable scan and... | ADDRESSED | R4: §4.9 documents `inject_debug_marker(html, dev)` which short-circuits `return html` when `dev == false` — zero scan, zero allocation in prod. The production path never reaches `String::replace`. | R2,R3,R4 |
| 24 | R4 | Medium | test_quality | regression-guard-r1-2 | The regression guard for R1 #2 names only `test_dev_teach_html_carries_debug_marker` as owner of the structural asser... | ADDRESSED | Post-R2: §5.3 R1 #2 guard updated to name both `test_dev_teach_html_carries_debug_marker_student_view` and `test_dev_teach_html_carries_debug_marker_teacher_view` as separate owners of the structural assertion for each HTML variant. | R2,R4 |
| 25 | R4 | Low | code_quality | plan-still-documents-deprecated | The plan still documents the deprecated single-argument `startDebugOverlay` signature in two places, while the implem... | ADDRESSED | Post-R2: §3 and §10 step 8 updated to `startDebugOverlay(pc, {localTrack})`. | R2,R4 |
| 26 | R4 | Low | test_quality | implementation-checklist-still-s | The implementation checklist still says the `hasTrack` predicate needs “one small test,” but §5.2 and the existing te... | ADDRESSED | Post-R2: §10 step 6 updated to “six tests” with a brief description of the equivalence classes covered. | R2,R4 |
| 27 | R4 | Low | test_quality | 4 | The §4.5 pseudocode for `hasTrack` omits the null guard that exists in the implementation and is required by the docu... | ADDRESSED | Post-R2: §4.5 pseudocode updated to include the null/type guard on `id` and the null-entry guard in `some()`. | R2,R4 |
| 28 | R2 | Low | security | production-html-still-serves | Production HTML still serves the literal `<!-- sb:debug -->` placeholder comment, which leaks implementation metadata... | ADDRESSED | Post-R2: `inject_debug_marker` now strips the comment in prod mode (replaces with “”); `test_prod_teach_html_has_no_debug_marker` updated to assert the comment is absent. | R2 |
| 29 | R2 | Low | test_quality | verify-html-has-no | `verify_html_has_no_inline_script` was not extended to `/teach/<slug>` and `/loopback`, so the promised regression gu... | ADDRESSED | Post-R2: `verify_html_has_no_inline_script` extended to check `/teach/myroom` (student + teacher views) and `/loopback` via shared `assert_no_inline_script` helper in `http_csp.rs`. | R2 |
| 30 | R2 | Low | code_quality | loopback-worklet | `loopback-worklet.js` does not include the required `Exports`, `Depends`, and `Invariants` header fields used by proj... | ADDRESSED | Post-R2: `Exports`, `Depends`, and `Invariants` header fields added to `loopback-worklet.js`. | R2 |
