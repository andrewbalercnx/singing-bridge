# Findings Tracker: Sprint 20 (plan)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | automatic-vad-triggered-chat | Automatic VAD-triggered chat mode violates the product audio-fidelity requirement because it enables AEC/NS/AGC durin... | ADDRESSED | Fidelity tradeoff explicitly documented; Demonstrating/force-off chip added as protection for vocal demos; 3s hangover window acknowledged as per-PRD tradeoff | R1,R2,R3 |
| 2 | R1 | High | domain | ios-reclassification-changes-pro | The iOS reclassification changes the product policy defined in ADR-0001, but the plan does not include an ADR amendme... | ADDRESSED | ADR-0001 amendment added to deliverables and file map | R1,R2,R3 |
| 3 | R1 | High | test_quality | vad-test-strategy-not | The VAD test strategy is not verifiable as written. The claimed “9 state×event combinations” do not match the describ... | ADDRESSED | Full 9-cell state×event matrix defined for SILENT/ACTIVE/HANGOVER; forceMode test cases added | R1,R2,R3 |
| 4 | R1 | High | test_quality | rust-integration-test-path | The Rust integration test path is wrong. `tests/test_acoustic_profile.rs` at repo root will not run under the server ... | ADDRESSED | Rust test path corrected to server/tests/test_acoustic_profile.rs | R1,R2,R3 |
| 5 | R1 | High | code_quality | plan-keeps-headphones-confirmed | The plan keeps `headphones_confirmed` alongside the new `acoustic_profile`, leaving two fields that encode the same s... | ADDRESSED | headphones_confirmed removed from LobbyEntryView and LobbyEntry; single authoritative acoustic_profile field; HeadphonesConfirmed ClientMsg kept for backwards compat only | R1,R2,R3 |
| 6 | R1 | Medium | security | new-setacousticprofile-chattingm | New `SetAcousticProfile` and `ChattingMode` client messages omit `slug`, breaking the established `require_joined` ro... | ADDRESSED | Auth pattern clarified: role check via ConnContext (same as AccompanimentPlay); no slug needed; explicitly documented | R1,R2,R3 |
| 7 | R1 | Medium | domain | backwards-compat-derivation-acou | Backwards-compat derivation of `acoustic_profile` from legacy fields is ambiguous. `tier` alone cannot distinguish ol... | ADDRESSED | Precise fallback table defined: ios_forced only from explicit client send; absent+HeadphonesConfirmed=Headphones; absent+no confirm=Speakers | R1,R2,R3 |
| 8 | R1 | Medium | domain | plans-lobbyjoin-flow-acoustic | The plan’s `LobbyJoin` flow for `acoustic_profile` conflicts with the current student flow because self-check runs co... | ADDRESSED | Join flow clarified: LobbyJoin sends UA-derived profile (ios_forced or null); HeadphonesConfirmed upgrades to Headphones post-self-check; no race | R1,R2,R3 |
| 9 | R1 | Medium | code_quality | new-vad-module-not | The new VAD module is not testable as planned because `tickVad` is described as the pure unit-test target but is not ... | ADDRESSED | tickVad exported as window.sbVad.tickVad in module API | R1,R2,R3 |
| 10 | R1 | Medium | code_quality | new-files-changed-modules | New files and changed modules do not have required file-header updates called out. This breaks an established project... | ADDRESSED | File header requirements added to every row in file map table | R1,R2,R3 |
| 11 | R1 | Medium | security | handle-chatting-mode-behavior | `handle_chatting_mode` behavior when no active session exists is not specified. The plan must define a no-session fai... | ADDRESSED | handle_chatting_mode with no active session returns ServerMsg::Error { code: NotInSession } | R1,R2,R3 |
| 12 | R1 | Medium | test_quality | several-important-regression-edg | Several important regression and edge-path tests are missing: `suppress(false)` re-enable, `deriveAcousticProfile`, `... | ADDRESSED | Added: suppress(false) re-enable, deriveAcousticProfile all 3 cases, HeadphonesConfirmed->Headphones, sendChatMode with accompanimentIsPlaying gate, ios_forced muting, applyConstraints rejection path, debounce test | R1,R2,R3 |
| 13 | R1 | Low | domain | chattingmode-direction-inconsist | `ChattingMode` direction is inconsistent between JS and Rust sections. The teacher-side handler appears dead unless t... | ADDRESSED | ChattingMode direction clarified: teacher sends ClientMsg only; student receives ServerMsg only; teacher never receives it; connectTeacher does not handle it | R1,R2,R3 |
| 14 | R1 | Low | domain | chattingmode-state-not-persisted | `ChattingMode` state is not persisted across reconnect, so student constraint state can reset mid-session. This needs... | ADDRESSED | Reconnect behavior documented: non-persistent; VAD recreated from SILENT; student resets to AEC-off on reconnect | R1,R2,R3 |
| 15 | R1 | Low | security | acoustic-profile-advisory-client | `acoustic_profile` is advisory and client-controlled, but the trust boundary is not documented. (File: `server/src/ws... | ADDRESSED | Trust boundary documented in lobby.rs comment: acoustic_profile is advisory; teacher override is authoritative | R1,R2,R3 |
| 16 | R1 | Low | code_quality | onthresholdrms-offthresholdrms-a | `onThresholdRms` and `offThresholdRms` are ambiguous names for transition thresholds. (File: `web/assets/vad.js`, Loc... | ADDRESSED | Renamed to rmsVoiceOn / rmsVoiceOff | R1,R2,R3 |
| 17 | R1 | Low | code_quality | f7-f8-tags-undefined | `F7` and `F8` tags are undefined in the plan text. (File: `web/assets/teacher.js`, Location: chat-mode wiring) (Sourc... | ADDRESSED | F7/F8 references replaced with inline descriptions in chat-mode wiring section | R1,R2,R3 |
| 18 | R1 | Low | test_quality | exact-vad-hangover-boundary | Exact VAD hangover boundary behavior at `hangoverMs` is not specified in tests. (File: `web/assets/vad.js`, Location:... | ADDRESSED | Hangover boundary specified: fires when nowMs >= hangsUntilMs (inclusive) on next tick with rms < rmsVoiceOff | R1,R2,R3 |
| 19 | R1 | Low | security | rapid-chattingmode-toggles-can | Rapid `ChattingMode` toggles can trigger repeated `applyConstraints` calls; the plan should debounce or throttle this... | ADDRESSED | lastChatModeApplied debounce added: applyConstraints skipped if enabled unchanged; resets null on disconnect | R1,R2,R3 |
| 20 | R2 | High | security | confirm-headphones-specified-way | `confirm_headphones` is specified in a way that can overwrite `IosForced`, which violates the fallback table and re-e... | ADDRESSED | confirm_headphones is now a no-op for IosForced; only upgrades Speakers→Headphones; invariant documented | R2,R3 |
| 21 | R2 | Medium | test_quality | suppress-forcemode-have-no | `suppress` and `forceMode` have no resolved precedence or resume rule, so `onVoiceStart` emission and chat-mode state... | ADDRESSED | suppress takes priority over forceMode; precedence rules and all interaction paths documented in invariants and test strategy | R2,R3 |
| 22 | R2 | Medium | code_quality | tickvad-state-shape-not | `tickVad` state shape is not fully specified because only `HANGOVER` has a defined object form; `SILENT` and `ACTIVE`... | ADDRESSED | tickVad state discriminated union fully defined: {name:SILENT}/{name:ACTIVE}/{name:HANGOVER,hangsUntilMs}; in file header and test section | R2,R3 |
| 23 | R2 | Medium | test_quality | plan-does-not-require | The plan does not require a test that resets `lastChatModeApplied` on disconnect and verifies the first post-reconnec... | ADDRESSED | lastChatModeApplied reset test added: post-reconnect same-value chat mode must not be suppressed by stale debounce | R2,R3 |
| 24 | R2 | Low | test_quality | exact-vad-threshold-boundary | Exact VAD threshold boundary cases are missing for `rms === rmsVoiceOn` and `rms === rmsVoiceOff` (File: `web/assets/... | ADDRESSED | rms===rmsVoiceOn (SILENT→ACTIVE) and rms===rmsVoiceOff (hysteresis, no transition) boundary tests added | R2,R3 |
| 25 | R2 | Low | test_quality | forcemode-forcemode-auto-path | The `forceMode('on') -> forceMode('auto')` path is missing from the VAD test matrix (File: `web/assets/tests/vad.test... | ADDRESSED | forceMode(on)→forceMode(auto) test case added: VAD resumes SILENT, no immediate event | R2,R3 |
| 26 | R2 | Low | test_quality | initial-mount-with-non | Initial mount with a non-headphones acoustic profile is not explicitly tested for immediate muting and banner state (... | ADDRESSED | Mount-time muting test added: mount({acousticProfile:speakers}) immediately mutes | R2,R3 |
| 27 | R2 | Low | test_quality | fallback-table-not-fully | The fallback table is not fully exercised by explicit Rust tests for `"headphones"` and `"speakers"` join values (Fil... | ADDRESSED | All 7 fallback table rows now have explicit Rust tests including headphones/speakers explicit values and IosForced+HeadphonesConfirmed no-op | R2,R3 |
| 28 | R2 | Low | security | unknown-acoustic-profile-enum | Unknown `acoustic_profile` enum values still appear to fail deserialization and close the socket instead of degrading... | ADDRESSED | AcousticProfile uses #[serde(other)] Unknown variant; unknown values degrade to Speakers; documented | R2,R3 |
| 29 | R2 | Low | domain | student-side-handling-onacoustic | Student-side handling of `onAcousticProfileChanged` is not specified; this needs an explicit no-op or a defined behav... | ADDRESSED | Student onAcousticProfileChanged specified as no-op by default; callback defined for observability | R2,R3 |
| 30 | R2 | Low | code_quality | headphonesconfirmed-remains-back | `HeadphonesConfirmed` remains for backwards compatibility without a deprecation comment explaining why it must persis... | ADDRESSED | HeadphonesConfirmed retained with deprecation comment explaining backwards-compat purpose | R2,R3 |
| 31 | R2 | Low | code_quality | vad | The `vad.js` header should enumerate handle methods inline so the consumer API is discoverable from the file header a... | ADDRESSED | vad.js Exports header now lists handle methods (suppress, forceMode, teardown) and state discriminated union inline | R2,R3 |
| 32 | R3 | Medium | domain | handle-set-acoustic-profile | `handle_set_acoustic_profile` needs explicit `Unknown -> Speakers` normalization on the teacher-override write path t... | OPEN |  | R3 |
| 33 | R3 | Medium | test_quality | vad | `vad.js` lacks planned coverage for the `create()` browser wrapper. The strategy covers `tickVad` state logic but not... | OPEN |  | R3 |
| 34 | R3 | Low | test_quality | teardown-behavior-while-vad | `teardown()` behavior while VAD is not `SILENT` is unspecified. The plan needs an explicit rule on whether cleanup em... | OPEN |  | R3 |
| 35 | R3 | Low | test_quality | teacher-override-path-from | The teacher override path from `IosForced` to `Headphones` is not directly tested, leaving the intended authority rul... | OPEN |  | R3 |
| 36 | R3 | Low | domain | adr-amendment-spec-omits | The ADR amendment spec omits the remaining iOS sample-rate resampling limitation, so the reclassification rationale i... | OPEN |  | R3 |
| 37 | R3 | Low | code_quality | file-map-entry-signalling | The file map entry for `signalling.js` does not call out the required `Exports` header update for the new teacher-han... | OPEN |  | R3 |
