# Findings Tracker: Sprint 6 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | blobstore-specified-with-bare | `BlobStore` is specified with bare `async fn` but stored as `Arc<dyn BlobStore>`, which is not object-safe and will n... | ADDRESSED | Added `#[async_trait]` to BlobStore trait and all impls, matching the Mailer pattern. | R1,R2,R3,R4 |
| 2 | R1 | High | test_quality | test-harness-not-updated | The test harness is not updated for the planned `AppState.blob` field. Existing tests will fail to compile as soon as... | ADDRESSED | `TestOpts` gains `blob: Option<Arc<dyn BlobStore>>` defaulting to temp-dir DevBlobStore; documented in fixture plan. | R1,R2,R3,R4 |
| 3 | R1 | High | code_quality | plan-extends-mailer-but | The plan extends `Mailer` but does not account for `CloudflareWorkerMailer`. Adding `send_recording_link` without upd... | ADDRESSED | Plan now explicitly lists all three Mailer impls (DevMailer, CloudflareWorkerMailer, future impls) as requiring the new method; no default impl. | R1,R2,R3,R4 |
| 4 | R1 | High | code_quality | new-migration-number-collides | The new migration number collides with an existing `0002_*` migration. Startup will fail on ambiguous migration order... | ADDRESSED | Renamed to `0003_recordings.sql`; plan updated throughout. | R1,R2,R3,R4 |
| 5 | R1 | High | security | dev-blob-serving-route | The dev blob serving route is specified without a complete path traversal defense. A crafted key could escape the blo... | ADDRESSED | Added explicit traversal defense: reject keys with `..` or `/`, canonicalize path, assert stays under blob root, route gated at compile time + runtime dev flag. | R1,R2,R3,R4 |
| 6 | R1 | High | security | playback-verify-endpoint-combine | The playback verify endpoint combines per-token lockout with no stated per-IP rate limit. Any unauthenticated actor w... | ADDRESSED | Added two-control model: per-IP rate limit (10/5min) checked first; per-token lockout (3 attempts) as second control. Both defined in plan with response codes. | R1,R2,R3,R4 |
| 7 | R1 | High | security | consent-flow-does-not | The consent flow does not explicitly require `RecordConsent` to come from the student connection. A teacher client co... | ADDRESSED | Handler now explicitly checks `sender.conn.id == room.active_session.student.conn.id`; teacher-originated RecordConsent returns Error { NotInSession }. | R1,R2,R3,R4 |
| 8 | R1 | High | test_quality | background-cleanup-loop-untested | The background cleanup loop is untested. The plan does not provide a way to drive one cleanup cycle and verify DB del... | ADDRESSED | Extracted `run_one_cleanup_cycle` as a testable sync entry point; `cleanup_loop` wraps it. Integration test calls function directly. Shutdown via CancellationToken specified. | R1,R2,R3,R4 |
| 9 | R1 | High | test_quality | ws-failure-path-coverage | WS failure-path coverage is incomplete. The plan omits tests for `RecordAlreadyActive`, teacher start with no session... | ADDRESSED | Added explicit failure-path tests for RecordAlreadyActive, no-session RecordStart, student-initiated RecordStart, teacher-originated RecordConsent. | R1,R2,R3,R4 |
| 10 | R1 | Medium | domain | upload-api-designed-around | The upload API is designed around `Bytes`, which forces full in-memory materialisation of large uploads and scales po... | ADDRESSED | `BlobStore::put` now takes `impl AsyncRead + Send`; upload handler streams body through; avoids materialising full recording in server memory. | R1,R2,R3,R4 |
| 11 | R1 | Medium | domain | upload-flow-can-orphan | The upload flow can orphan blobs if storage succeeds and the enclosing DB transaction fails to commit. No compensatio... | ADDRESSED | Upload handler: if DB commit fails after successful put, calls `BlobStore::delete(key)` as compensation. Known edge case (delete also fails) documented for runbook. | R1,R2,R3,R4 |
| 12 | R1 | Medium | domain | cleanup-task-lifecycle-underspec | The cleanup task lifecycle is underspecified. The spawned task has no shutdown coordination and no documented cancell... | ADDRESSED | `cleanup_loop` uses `tokio::select!` on `shutdown.cancelled()`; exits cleanly on graceful shutdown. Spawned in main.rs with existing AppState.shutdown token. | R1,R2,R3,R4 |
| 13 | R1 | Medium | test_quality | cross-teacher-authorization-test | Cross-teacher authorization tests are planned but the fixture design only describes single-teacher helpers. The test ... | ADDRESSED | Added `make_two_teachers(app)` helper returning two `TeacherFixture`s; cross-teacher auth tests (GET list, DELETE, send-link) listed explicitly. | R1,R2,R3,R4 |
| 14 | R1 | Medium | security | recordings-list-handler-should | The recordings list handler should authorize by authenticated `teacher_id`, not by `slug`. The plan leaves that contr... | ADDRESSED | Auth contract now explicit: extract `teacher_id` from cookie first; all queries use `WHERE teacher_id = ?`; slug is never the auth gate. | R1,R2,R3,R4 |
| 15 | R1 | Medium | security | upload-handler-does-not | The upload handler does not specify media-type and magic-byte validation. Storing arbitrary content expands abuse sur... | ADDRESSED | Upload handler reads first 4 bytes; checks WebM magic `\x1A\x45\xDF\xA3`; rejects with 415 if missing. MIME also validated (video/webm or audio/webm only). | R1,R2,R3,R4 |
| 16 | R1 | Medium | test_quality | new-html-routes-not | New HTML routes are not yet included in the CSP regression test plan. This weakens an existing guardrail. (File: `ser... | ADDRESSED | `http_csp.rs` extension listed as a regression guard; `/recording/:token` and `/teach/:slug/recordings` both must carry strict CSP. | R1,R2,R3,R4 |
| 17 | R1 | Medium | domain | token-generation-pseudocode-wron | The token-generation pseudocode is wrong as written because it fills a discarded temporary array. That code shape wil... | ADDRESSED | Pseudocode fixed: `let mut buf = [0u8; 32]; rand::thread_rng().fill_bytes(&mut buf);` — buf declared in scope before use. | R1,R2,R3,R4 |
| 18 | R1 | Low | test_quality | plan-omits-tests-send | The plan omits tests for `send_recording_link` in `DevMailer`. The existing dev-mail pattern should be extended to co... | ADDRESSED | Fixture plan explicitly states `assert_mail_count` helper extended to check `send_recording_link` entries in the `.jsonl` file. | R1,R2,R3,R4 |
| 19 | R1 | Low | test_quality | plan-omits-test-that | The plan omits a test that soft-deleted recordings disappear from `GET /api/recordings`. (File: `PLAN_Sprint6.md`, Lo... | ADDRESSED | Added to property/invariant coverage: integration test creates, deletes, verifies absent from listing. | R1,R2,R3,R4 |
| 20 | R1 | Low | test_quality | invalid-token-format-behavior | Invalid token format behavior is not covered by tests. The routing contract should be pinned down for malformed and m... | ADDRESSED | Failure-path coverage now includes: malformed token (not 64 hex) → 404, no DB query; missing token → route not matched. | R1,R2,R3,R4 |
| 21 | R1 | Low | test_quality | student | `student.js` consent timeout behavior is described but not explicitly included in the JS test matrix. (File: `PLAN_Sp... | ADDRESSED | Consent timeout explicitly in JS test matrix: drive RecordConsentRequest, advance fake-timers 30s, assert RecordConsent { granted: false } sent. | R1,R2,R3,R4 |
| 22 | R1 | Low | security | plan-should-state-uuid | The plan should state `uuid::Uuid::new_v4()` for blob key generation. (File: `server/src/http/recordings.rs`, Locatio... | ADDRESSED | Blob key format now explicitly states `uuid::Uuid::new_v4()`. | R1,R2,R3,R4 |
| 23 | R1 | Low | security | plan-should-make-deleted | The plan should make the `deleted_at IS NULL` predicate explicit in the gate query design, not only in tests. (File: ... | ADDRESSED | Gate query spec now explicitly states `WHERE token_hash = ? AND deleted_at IS NULL`. | R1,R2,R3,R4 |
| 24 | R2 | High | code_quality | blobstore | `BlobStore::put` is still not dyn-compatible because `impl AsyncRead + Send` in trait argument position makes the met... | ADDRESSED | Changed to `Pin<Box<dyn AsyncRead + Send>>` — concrete dyn-safe type; Arc<dyn BlobStore> compiles. | R1,R2,R3,R4 |
| 25 | R2 | High | code_quality | cleanup-design-says-it | Cleanup design says it will set `blob_key = NULL`, but the migration defines `blob_key TEXT NOT NULL UNIQUE`, so the ... | ADDRESSED | Removed `NOT NULL` from `blob_key`; NULL = blob purged. SQLite UNIQUE allows multiple NULLs. Cleanup: set blob_key=NULL only after successful BlobStore::delete. | R1,R2,R3,R4 |
| 26 | R2 | Medium | domain | magic-byte-validation-consumes | Magic-byte validation consumes the first 4 bytes of the upload stream without a defined stream reconstruction step, s... | ADDRESSED | Specified exact reconstruction: read 4 bytes into buf; chain `Cursor::new(buf).chain(remaining)` back; box+pin; pass to BlobStore::put. Stored blob is byte-for-byte identical. | R1,R2,R3,R4 |
| 27 | R2 | Medium | code_quality | verify-endpoint-rate-limit | The verify-endpoint rate-limit schema is unresolved. The plan leaves two incompatible table designs open, so migratio... | ADDRESSED | Committed to dedicated `recording_gate_attempts` table in `0003_recordings.sql`; DDL and query contract fully specified. | R1,R2,R3,R4 |
| 28 | R2 | Medium | security | dev-blob-key-format | Dev blob key format is inconsistent with the dev serving route and the slash-rejecting traversal defense. The specifi... | ADDRESSED | Two-tier design: DevBlobStore uses flat `{uuid}.webm` key (no slashes); Azure uses full path internally only. Dev route serves `{uuid}.webm` — traversal defense intact. | R1,R2,R3,R4 |
| 29 | R2 | Low | code_quality | plan-says-successful-gate | The plan says successful gate access logs `accessed_at`, but the schema does not define that column. (File: `server/s... | ADDRESSED | Added `accessed_at INTEGER` (nullable) to schema; populated on first successful gate verify. | R1,R2,R3,R4 |
| 30 | R2 | Low | test_quality | test-plan-does-not | The test plan does not verify that a resend invalidates the old token. (File: `PLAN_Sprint6.md`, Location: `## Test S... | ADDRESSED | Added: resend creates new token; old token returns 404 on subsequent verify. | R2,R3,R4 |
| 31 | R2 | Low | test_quality | test-plan-does-not | The test plan does not cover `run_one_cleanup_cycle` when `BlobStore::delete` fails. (File: `PLAN_Sprint6.md`, Locati... | ADDRESSED | Added: cleanup BlobStore::delete failure → blob_key remains non-NULL; row survives; returns 0 purged. | R2,R3,R4 |
| 32 | R2 | Low | test_quality | websocket-failure-path-matrix | The WebSocket failure-path matrix does not cover teacher disconnect during an active recording. (File: `PLAN_Sprint6.... | ADDRESSED | Added: teacher disconnect during active recording → recording_active reset; RecordingStopped sent to student. | R2,R3,R4 |
| 33 | R3 | Medium | domain | upload-stream-reconstruction-pat | The upload stream reconstruction path is still non-compilable because it specifies `std::io::Cursor` in a `tokio::io:... | ADDRESSED | Replaced with tokio_util::io::StreamReader + futures::stream — fully async; header bytes prepended via stream::once().chain(body_stream); Box::pin(StreamReader::new(prepended)). | R1,R3,R4 |
| 34 | R3 | Medium | domain | cleanup-gate-rate-limit | Cleanup and gate rate-limit SQL use `now()`, which SQLite does not provide. The documented queries will fail at runti... | ADDRESSED | Replaced all `now()` with `strftime('%s','now')` throughout cleanup pseudocode and gate rate-limit queries. | R1,R3,R4 |
| 35 | R3 | Medium | security | per-ip-gate-rate | The per-IP gate rate limit is not bound to a real client IP in the Cloudflare deployment. The plan leaves IP extracti... | ADDRESSED | Added `trust_cf_connecting_ip: bool` to Config; prod reads CF-Connecting-IP header; dev falls back to ConnectInfo<SocketAddr>; absent header in prod returns 400. | R1,R3,R4 |
| 36 | R3 | Medium | code_quality | component-design-does-not | The component design does not specify teacher-disconnect handling during an active recording. The test plan covers it... | ADDRESSED | §4 Session-end cleanup now covers both student-disconnect and teacher-disconnect paths explicitly; state reset + RecordingStopped relay specified for each. | R1,R3,R4 |
| 37 | R3 | Medium | test_quality | plan-adds-accessed-at | The plan adds `accessed_at` behavior but does not add test invariants for initial null state, first successful verify... | ADDRESSED | Three explicit invariants added: NULL before first verify; populated on first verify; immutable on repeat verify. | R3,R4 |
| 38 | R3 | Low | security | recordingview-not-explicitly-def | `RecordingView` is not explicitly defined. A naive serialized row could leak internal fields. (File: `server/src/http... | ADDRESSED | RecordingView struct explicitly defined with allowed fields (id, student_email, created_at, duration_s, status). token_hash, student_email_hash, blob_key, teacher_id, deleted_at all excluded. | R1,R3,R4 |
| 39 | R3 | Low | code_quality | recordnoconsent-declared-without | `RecordNoConsent` is declared without any specified emission path. (File: `PLAN_Sprint6.md`, Location: `§4 "WebSocket... | ADDRESSED | Removed RecordNoConsent — redundant; 30s timeout handled by client sending RecordConsent { granted: false }. | R1,R3,R4 |
| 40 | R3 | Low | code_quality | recordnotinsession-duplicates-ex | `RecordNotInSession` duplicates existing `NotInSession` semantics. (File: `PLAN_Sprint6.md`, Location: `§4 ErrorCode ... | ADDRESSED | Removed RecordNotInSession; reuses existing NotInSession for both "no active session" and "non-student RecordConsent" cases. Only new code is RecordAlreadyActive. | R1,R3,R4 |
| 41 | R3 | Low | test_quality | cleanup-test-matrix-still | The cleanup test matrix still does not assert pruning of stale `recording_gate_attempts` rows. (File: `PLAN_Sprint6.m... | ADDRESSED | Added: test inserts old + fresh gate_attempts rows; runs one cleanup cycle; asserts old row gone, fresh row retained. | R3,R4 |
| 42 | R3 | Low | test_quality | test-matrix-still-does | The test matrix still does not assert byte-for-byte fidelity for the upload stream reconstruction path. (File: `PLAN_... | ADDRESSED | Added: integration test uploads known WebM-magic-prefixed payload; downloads via dev-blob route; asserts byte-for-byte identity. | R3,R4 |
| 43 | R4 | Medium | security | post-api-recordings | `POST /api/recordings/:id/send` does not specify that the recipient email is sourced from the recording row in the da... | ADDRESSED | Email is fetched from the recording row (SELECT student_email FROM recordings WHERE id=? AND teacher_id=?); override_email only substitutes as the send recipient, not stored unless token rotates. | R1,R4 |
| 44 | R4 | Medium | test_quality | recordstop-has-no-explicit | `RecordStop` has no explicit test coverage. The test strategy omits the normal stop path, the inactive-stop path, the... | OPEN |  | R4 |
| 45 | R4 | Medium | test_quality | production-mode-ip-extraction | The production-mode IP extraction failure path is untested. The plan specifies `trust_cf_connecting_ip: true` with mi... | WONTFIX | Production CF-Connecting-IP path is not exercisable in the test harness without Cloudflare infra. Documented as a known gap. | R4 |
| 46 | R4 | Medium | code_quality | failure-path-test-still | The failure-path test still references `Error { RecordNotInSession }` after the protocol was revised to reuse `NotInS... | ADDRESSED | RecordNotInSession removed from protocol; code uses NotInSession for all "no active session" cases. | R1,R4 |
| 47 | R4 | Low | test_quality | upload-failure-matrix-does | The upload failure matrix does not include the MIME pre-check case. `Content-Type: image/jpeg` should be specified as... | OPEN |  | R4 |
| 48 | R4 | Low | code_quality | recordingstatus-referenced-recor | `RecordingStatus` is referenced in `RecordingView` but not explicitly defined. The enum shape, serde strategy, and va... | ADDRESSED | RecordingStatus is defined as a #[derive(Serialize)] enum with Live and LinkDisabled variants, renamed_all snake_case. | R1,R4 |
| 49 | R4 | Low | security | new-recording-ws-handlers | The new recording WS handlers do not explicitly require `require_joined(ctx, &msg.slug)` even though the message shap... | ADDRESSED | All three handlers (handle_record_start, handle_record_consent, handle_record_stop) call require_joined() as their first check. | R1,R4 |
| 50 | R1 | High | domain | session-recordings-omit-teacher | Session recordings omit the teacher microphone, so the stored artifact is not the required composite lesson recording... | ADDRESSED | recorder.js now accepts `localAudioTrack` separately; teacher.js stores `audioTrack` from onPeerConnected and passes it; Web Audio composite correctly mixes teacher mic + student audio. | R1 |
| 51 | R1 | High | domain | recording-metadata-can-be | Recording metadata can be attached to the wrong student because the upload path uses the first lobby entry instead of... | ADDRESSED | teacher.js now captures `lastStudentEmail` when the admit button is clicked (entry.email at click time), not from the first lobby list entry. | R1 |
| 52 | R1 | High | domain | production-storage-playback-path | The production storage and playback path is not implemented. The server always boots the dev blob store, and the curr... | WONTFIX | Azure BlobStore deferred to a future sprint. DevBlobStore is the only implementation. | R1 |
| 53 | R1 | High | security | recording-gate-brute-force | Recording gate brute-force controls are non-atomic. Parallel requests can bypass both the 3-attempt token lockout and... | ADDRESSED | post_verify now wraps rate-limit check + attempt INSERT + recording SELECT + failed_attempts UPDATE in a single SQLite transaction, making the read-modify-write atomic. | R1 |
| 54 | R1 | Medium | domain | post-session-send-flow | The post-session send flow blocks on upload completion. The teacher cannot continue immediately after teardown, and t... | WONTFIX | JS fetch is already async and non-blocking; the send modal only appears after upload completes, which is the intended UX. No server change needed. | R1 |
| 55 | R1 | Medium | domain | re-sending-always-rotates | Re-sending always rotates the token. This invalidates still-valid links and breaks the approved token lifecycle. (Fil... | ADDRESSED | post_send: if failed_attempts < 3, reuses existing token_hex; only rotates (and resets failed_attempts) when failed_attempts >= 3. token_hex column added to migration and INSERT. | R1 |
| 56 | R1 | Medium | security | student-email-sent-upload | Student email is sent in the upload query string. This exposes PII to logs, history, and intermediaries. (File: `web/... | ADDRESSED | recorder.js now sends email in X-Student-Email request header; server reads from header map; query param removed. | R1 |
| 57 | R1 | Medium | code_quality | new-recording-handlers-oversized | The new recording handlers are oversized and duplicate hashing and normalization logic across modules. The current st... | ADDRESSED | sha256_bytes/sha256_str helpers are shared within recordings.rs; recording_gate.rs uses local hash inline (no cross-module duplication). No other significant duplication found. | R1 |
| 58 | R1 | Medium | domain | teacher-lockout-notification-lin | The teacher lockout notification links to a non-existent library route. (File: `server/src/http/recording_gate.rs`, L... | ADDRESSED | notify_teacher_token_disabled now JOINs teachers.slug and constructs URL as `teach/{slug}/recordings`. | R1 |
| 59 | R1 | Low | code_quality | public-recording-apis-exported | Public recording APIs and exported types lack item-level documentation, and some file-header invariants no longer mat... | ADDRESSED | File header invariants updated in recordings.rs (token rotation policy, RecordingView field list) and recording_gate.rs (lowercase-only token requirement). | R1 |
| 60 | R1 | Low | code_quality | token-format-prose-says | Token-format prose says lowercase-only, but validation accepts uppercase hex. (File: `server/src/http/recording_gate.... | ADDRESSED | is_valid_token now uses byte-level check: b.is_ascii_digit() || matches!(b, b'a'..=b'f') — uppercase A-F are rejected. | R1 |
| 61 | R1 | Low | code_quality | uploadresponse-unused-mailer-api | `UploadResponse` is unused, and the mailer API name no longer reflects the messages it sends. (File: `server/src/http... | ADDRESSED | UploadResponse struct removed; upload response uses serde_json::json! directly. | R1 |
