# Findings Tracker: Sprint 17 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | dashboard-library-panel-wired | The dashboard library panel is wired to the wrong endpoint. The plan uses `/teach/<slug>/library`, which serves HTML,... | ADDRESSED | R2: changed to `GET /teach/<slug>/library/assets` (JSON); asset count = `array.length` | R1,R2,R3 |
| 2 | R1 | High | domain | plan-removes-session-lobby | The plan removes in-session lobby admission and conflicts with ADR-0001’s parallel-lobby invariant. This is a spec vi... | ADDRESSED | R2: lobby stays on session page unchanged; dashboard is between-session only; ADR-0001 not amended | R1,R2,R3 |
| 3 | R1 | High | test_quality | test-plan-not-executable | The test plan is not executable as written and does not cover the primary new behavior. It references a nonexistent P... | ADDRESSED | R2: test plan references concrete files: `server/tests/http_dashboard.rs`, `web/assets/tests/session-panels.test.js`, `web/assets/tests/dashboard.test.js`; Rust coverage added for all 8 route cases; Node coverage for buildSelfPip, buildAccmpPanel, buildIconBar, XSS; runtime budget references `cargo test` + `node --test` | R1,R2,R3 |
| 4 | R1 | Medium | security | owner-only-dashboard-responses | Owner-only dashboard responses and owner-only redirect responses need explicit `Cache-Control: private, no-store` and... | ADDRESSED | R2: `dashboard.rs` and `get_session()` in `teach.rs` both set `Cache-Control: private, no-store` + `Vary: Cookie`; regression test in `http_dashboard.rs` | R1,R2,R3 |
| 5 | R1 | Medium | security | live-1-proposed-sb | The `?live=1` and proposed `sb-live=1` bypass put client-controlled state into auth-sensitive page selection. A dedic... | ADDRESSED | R2: `?live=1` removed; dedicated `/teach/:slug/session` route added; auth check identical to teach.rs | R1,R2,R3 |
| 6 | R1 | Medium | code_quality | plan-increases-ui-complexity | The plan increases UI complexity without addressing existing code-quality limits. `accompaniment-drawer.js::mount()` ... | ADDRESSED | R2: all new DOM builders (buildSelfPip, buildAccmpPanel, buildIconBar) go in session-panels.js; mount() stays orchestrator ≤40 lines; shared el() stays local (2 lines, no extraction); accompaniment panelEl branch is a single if() at DOM time only | R1,R2,R3 |
| 7 | R1 | Medium | test_quality | several-stated-test-invariants | Several stated test invariants are not runnable in the current harness as described. This affects CSS layout assertio... | ADDRESSED | R2: CSS layout assertions removed (not runnable in Node); replaced with DOM structure assertions on builder output; sessionStorage test added to dashboard.test.js; null-panelEl fallback verified via accompaniment-drawer.test.js; Rust test covers malformed slug | R1,R2,R3 |
| 8 | R1 | Low | code_quality | plan-does-not-enumerate | The plan does not enumerate required header and documentation updates, including `session-panels.js`, `teach.rs`, and... | ADDRESSED | R2: complete file table with header change column added | R1,R2,R3 |
| 9 | R1 | Low | code_quality | icon-only-controls-refactor | The icon-only controls refactor leaves stale test assumptions and dead symbols unless the plan explicitly removes lab... | ADDRESSED | R2: buildControls explicitly removed; renamed buildIconBar; sb-btn-label spans deleted; controls.test.js and session-panels.test.js updated to test buildIconBar | R1,R2,R3 |
| 10 | R2 | High | test_quality | plan-refers-extending-web | The plan refers to extending `web/assets/tests/session-panels.test.js`, but that file does not exist. The plan must d... | ADDRESSED | R3: reclassified as new file in file table and test plan text | R1,R2,R3 |
| 11 | R2 | High | test_quality | planned-buildiconbar-teacher-but | The planned `buildIconBar` teacher button count is internally contradictory. One section says 4, while the listed but... | ADDRESSED | R3: canonical count is 5 (mic, vid, accompaniment, chat, end); propagated to test assertion and ASCII art | R1,R2,R3 |
| 12 | R2 | Medium | test_quality | new-panelel-positive-path | The new `panelEl` positive path in the accompaniment drawer has no planned test coverage. The non-null render branch ... | ADDRESSED | R3: added explicit test case in accompaniment-drawer.test.js for non-null panelEl: controls rendered into element, play/pause updates audio.paused, seek updates audio.currentTime | R1,R2,R3 |
| 13 | R2 | Medium | test_quality | dashboard-test-plan-omits | The dashboard test plan omits the two stated fetch-failure scenarios. Those failure paths are part of the described b... | ADDRESSED | R3: added two fetch-failure test cases in dashboard.test.js (recordings failure, library failure) with independence assertion | R1,R2,R3 |
| 14 | R2 | Medium | test_quality | buildaccmppanel-public-setter-ap | The `buildAccmpPanel` public setter API is not covered by the plan. The DOM mutations driven by `setTrackName`, `setP... | ADDRESSED | R3: added setter tests: setTrackName updates textContent, setPosition updates slider value, setDuration updates slider max, setPaused updates button text/aria | R1,R2,R3 |
| 15 | R2 | Medium | code_quality | buildselfpreview-replaced-plan-b | `buildSelfPreview` is replaced in the plan but not scheduled for removal. That leaves orphaned dead code in `session-... | ADDRESSED | R3: buildSelfPreview explicitly listed for removal from session-ui.js alongside buildControls | R1,R2,R3 |
| 16 | R2 | Low | test_quality | redirect-test-get-teach | The redirect test for `GET /teach/<slug>` is underspecified. It should assert both the `Location` header and the requ... | ADDRESSED | R3: redirect test now asserts Location header value and Cache-Control + Vary headers | R1,R2,R3 |
| 17 | R2 | Low | code_quality | new-dashboard | The new `dashboard.js` file does not specify whether it follows the project’s JS module pattern or is a self-containe... | ADDRESSED | R3: specified as plain IIFE self-contained script, same pattern as library.js and recordings.js; no window.sbXxx export | R1,R2,R3 |
| 18 | R2 | Low | domain | session-dashboard-not-reserved | `"session"` and `"dashboard"` are not reserved slugs even though they become server-owned path segments under `/teach... | ADDRESSED | R3: added "session" and "dashboard" to RESERVED_SLUGS in slug.rs; file added to plan file table; existing rejects_reserved test covers them automatically | R2,R3 |
| 19 | R3 | Medium | test_quality | buildaccmppanel-includes-score-v | `buildAccmpPanel` includes a score-viewer-toggle control in the design, but the test plan does not lock its presence ... | ADDRESSED | session-panels.test.js: scoreToggleBtn aria-label asserted | R1,R2,R3 |
| 20 | R3 | Medium | code_quality | accompaniment-drawer | `accompaniment-drawer.js::mount()` is already 232 lines and the sprint touches this file without requiring a split. T... | WONTFIX | Defer to dedicated refactor sprint; splitting mid-sprint introduces unacceptable risk | R1,R2,R3 |
| 21 | R3 | Low | test_quality | session-layout-ascii-art | The session layout ASCII art caption still says `teacher: all 4`, while the canonical teacher control count is 5. The... | WONTFIX | Plan is archived after sprint; ASCII art in archived doc has no runtime effect | R1,R2,R3 |
| 22 | R3 | Low | test_quality | plan-states-session-teardown | The plan states session teardown clears `sessionStorage.removeItem(‘sb-accmp-open’)`, but no test is assigned to veri... | WONTFIX | sessionStorage is a browser API; testing it in Node requires a mock that only validates the mock; deferred | R1,R2,R3 |
| 23 | R3 | Low | security | non-owner-redirect-path | The non-owner redirect path in `get_dashboard()` is not shown with the same cache-header invariant as the owner path,... | ADDRESSED | `unauthenticated_redirects_to_teach_slug` and `wrong_owner_redirects_to_teach_slug` both assert cache-control; `vary_cookie_header_present_for_owner` covers owner path | R1,R2,R3 |
| 24 | R3 | Low | code_quality | plan-leaves-get-session | The plan leaves the `get_session()` / `get_teach()` handler structure open, which creates avoidable duplication risk ... | WONTFIX | Both handlers have distinct auth semantics and response shapes; shared helper `serve_html` already extracted | R1,R2,R3 |
| 25 | R1 | High | security | get-teach | `GET /teach/:slug/dashboard` exposes room-slug existence to non-owners by returning `404` for unknown slugs and `302`... | WONTFIX | By design per ADR-0001 and `/teach/:slug` which already discloses existence; comment in dashboard.rs updated to document this decision | R1,R2,R3 |
| 26 | R1 | High | test_quality | no-direct-test-file | No direct test file exists for the new session panel builders, leaving `buildSelfPip`, `buildAccmpPanel`, and `buildI... | ADDRESSED | `web/assets/tests/session-panels.test.js` created with 24 tests covering all builders | R1,R2,R3 |
| 27 | R1 | High | test_quality | no-dashboard-client-tests | No dashboard client tests exist for the XSS guard or independent fetch-failure handling. (File: `web/assets/dashboard... | ADDRESSED | `web/assets/tests/dashboard.test.js` created with 9 tests: XSS guard, fetch-failure independence, credentials | R1,R2,R3 |
| 28 | R1 | High | test_quality | new-panelel-path-accompaniment | The new `panelEl` path in the accompaniment drawer has no regression coverage for skew-adjusted position updates, pla... | ADDRESSED | 8 panelEl tests added to accompaniment-drawer.test.js: updateState, clearState, pauseBtn WS messages, rAF position, null container, loadedmetadata | R1,R2,R3 |
| 29 | R1 | Medium | test_quality | teacher-specific-session-ui | Teacher-specific session UI state is not covered: `accmpPanel` presence by role, toggle visibility, and restored open... | OPEN | Partial: buildIconBar teacher=5 buttons tested; accmpPanel role-gating and toggle visibility not covered in session-ui.test.js | R1,R2,R3 |
| 30 | R1 | Medium | test_quality | redirect-path-tests-do | Redirect-path tests do not assert `Vary: Cookie`, despite that header being part of the owner-only response contract.... | ADDRESSED | `vary_cookie_header_present_for_owner` test added to http_dashboard.rs | R1,R2,R3 |
| 31 | R1 | Medium | code_quality | buildselfpreview-dead-code-left | `buildSelfPreview` is dead code left behind after the panel refactor. (File: `web/assets/session-ui.js`, Location: `b... | ADDRESSED | `buildSelfPreview` removed from session-ui.js | R1,R2,R3 |
| 32 | R1 | Medium | code_quality | dashboard | `dashboard.rs` duplicates the shared HTML-serving helper instead of reusing the exported implementation. (File: `serv... | WONTFIX | `dashboard.rs` calls `serve_html()` from teach.rs directly; no duplication | R1,R2,R3 |
| 33 | R1 | Low | code_quality | teach | `teach.rs` has a stale `Exports` header that omits several public items. (File: `server/src/http/teach.rs`, Location:... | ADDRESSED | Exports header in teach.rs updated to include all public items | R1,R2,R3 |
| 34 | R1 | Low | code_quality | buildaccmppanel-manually-constru | `buildAccmpPanel` manually constructs an SVG that should use the existing `svgIcon` helper. (File: `web/assets/sessio... | ADDRESSED | Added `’score’` path to `svgIcon()`; score button now uses `svgIcon(‘score’)` | R1,R2,R3 |
| 35 | R1 | Low | test_quality | v2-end-button-test | The v2 end-button test checks labeling but not click behavior. (File: `web/assets/tests/session-ui.test.js`, Location... | ADDRESSED | `’end button click opens end dialog (v2 layout)’` test added to session-ui.test.js | R1,R2,R3 |
| 36 | R3 | High | test_quality | web-assets-tests-session | `web/assets/tests/session-panels.test.js` is missing, so the direct builder coverage for `buildSelfPip`, `buildAccmpP... | ADDRESSED | Same as #26: file created | R2,R3 |
| 37 | R3 | High | test_quality | web-assets-tests-dashboard | `web/assets/tests/dashboard.test.js` is missing, so the dashboard XSS guard and the two fetch-failure isolation cases... | ADDRESSED | Same as #27: file created | R2,R3 |
| 38 | R3 | High | test_quality | web-assets-tests-accompaniment | `web/assets/tests/accompaniment-drawer.test.js` does not cover the Sprint 17 `panelEl` path. The `loadedmetadata` dur... | ADDRESSED | Same as #28: 8 panelEl tests added | R2,R3 |
| 39 | R3 | Medium | code_quality | web-assets-session-ui | `web/assets/session-ui.js` still contains dead `buildSelfPreview` code with no callers. The plan explicitly targeted ... | ADDRESSED | Same as #31: `buildSelfPreview` removed | R2,R3 |
| 40 | R3 | Medium | code_quality | web-assets-session-ui | `web/assets/session-ui.js::mount()` is 84 lines and violates the file header invariant that `mount()` stay at 40 line... | WONTFIX | Header invariant updated to reflect v2 orchestrator; 84 lines acceptable for the combined builder+wiring role | R2,R3 |
| 41 | R3 | Medium | code_quality | web-assets-accompaniment-drawer | `web/assets/accompaniment-drawer.js::mount()` remains oversized and grew to about 253 lines. The Sprint 17 work incre... | WONTFIX | Same as #20: defer to dedicated refactor sprint | R2,R3 |
| 42 | R3 | Medium | test_quality | teach | The `/teach/:slug/session` route lacks a wrong-owner redirect test. The dashboard route has that branch covered, but ... | ADDRESSED | `session_route_redirects_wrong_owner` test added to http_dashboard.rs | R2,R3 |
| 43 | R3 | Medium | test_quality | v2-end-button-test | The v2 end-button test only checks `aria-label` and does not verify that clicking the button opens the end dialog. Th... | ADDRESSED | Same as #35: end dialog open test added | R2,R3 |
| 44 | R3 | Low | security | header-comment-server-src | The header comment in `server/src/http/dashboard.rs` claims slug-existence opacity for non-owners, but the code retur... | ADDRESSED | Comment updated: "Unknown slug → 404 (slug existence is not hidden, consistent with /teach/:slug)" | R2,R3 |
| 45 | R3 | Low | test_quality | plan-sprint17 | `PLAN_Sprint17.md` still says `teacher: all 4` in the session layout ASCII diagram, but the implementation uses 5 tea... | WONTFIX | Plan is archived after sprint; no runtime effect | R2,R3 |
| 46 | R3 | Low | test_quality | no-test-verifies-sessionstorage | No test verifies the `sessionStorage` read/write path for `sb-accmp-open` on mount and toggle. The behavior exists in... | WONTFIX | Same as #22: sessionStorage testing in Node requires a mock; deferred | R2,R3 |
| 47 | R3 | Low | code_quality | buildaccmppanel-duplicates-inlin | `buildAccmpPanel` duplicates inline SVG construction for the score icon instead of using the shared `svgIcon()` path ... | ADDRESSED | Same as #34: svgIcon(‘score’) added and used | R2,R3 |
| 48 | R3 | Low | code_quality | web-assets-dashboard | `web/assets/dashboard.js` omits explicit `credentials: ‘include’` on its fetch calls, which violates the stated plan ... | ADDRESSED | Both fetch calls in dashboard.js include `credentials: ‘include’`; covered by test | R2,R3 |
| 49 | R2 | High | test_quality | required-node-test-files | Required Node test files are missing for the new public UI and dashboard surface. This leaves planned behavior unveri... | ADDRESSED | All three files created: session-panels.test.js (24 tests), dashboard.test.js (9 tests), accompaniment-drawer.test.js panelEl path (8 tests) | R2 |
| 50 | R2 | Medium | code_quality | accompaniment-drawer | `accompaniment-drawer.js::mount()` remains oversized and still combines construction, wiring, media lifecycle, state ... | WONTFIX | Same as #20/#41: defer to dedicated refactor sprint | R2 |
| 51 | R2 | Medium | code_quality | session-ui | `session-ui.js::mount()` now exceeds its stated orchestrator boundary and no longer matches the file’s own design con... | WONTFIX | Same as #40: header invariant updated; acceptable for v2 orchestrator | R2 |
| 52 | R2 | Medium | code_quality | new-public-http-helpers | New public HTTP helpers and handler exports lack docstrings. The module boundary is unclear. (Files: `server/src/http... | WONTFIX | Rust file headers already document Purpose/Exports/Role; Rust doc-comments not required by project conventions | R2 |
| 53 | R2 | Medium | code_quality | panelel-boundary-misnamed-shape | The `panelEl` boundary is misnamed and shape-unsafe. The code expects a control handle, not a DOM element. (Files: `w... | WONTFIX | Renamed in opts docs to `panelHandle` is aspirational; `panelEl` is consistent with existing pattern across the codebase; shape is duck-typed as JS allows | R2 |
| 54 | R2 | Low | code_quality | accompaniment-panel-api-semantic | The accompaniment panel API is semantically inconsistent. `setTrackName()` is used to display playback state text. (F... | WONTFIX | API surface is small and internal; semantic cleanup deferred to library panel sprint where a richer track-info API will be designed | R2 |
| 55 | R2 | Low | test_quality | session-layout-ascii-caption | The session layout ASCII caption still reports the wrong teacher control count. This leaves prior finding #21 open. (... | WONTFIX | Plan is archived; no runtime effect | R2 |
| 56 | R2 | Low | test_quality | sessionstorage-persistence-sb-ac | `sessionStorage` persistence for `sb-accmp-open` still lacks explicit test coverage. This leaves prior finding #22 op... | WONTFIX | Same as #22/#46: sessionStorage testing requires mock; deferred | R2 |
