# Findings Tracker: Sprint 14 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | websocket-contract-internally-in | The WebSocket contract is internally inconsistent and not implementable as written. It requires `ErrorCode::Forbidden... | ADDRESSED | Added `Forbidden` variant to `ErrorCode`. Made `AccompanimentState` a full snapshot on every broadcast — no partial updates, no client-side caching. | R1,R2,R3,R4,R5 |
| 2 | R1 | High | domain | score-coordinate-contract-diverg | The score-coordinate contract diverges from the authoritative accompaniment model. The plan switches `BarCoord` to ab... | ADDRESSED | Plan now uses canonical `{bar, page, x_frac, y_frac, w_frac, h_frac}` from `sidecar.rs` throughout; pixel conversion deferred to frontend render time. | R1,R2,R3,R4,R5 |
| 3 | R1 | High | domain | variant-playback-not-domain | Variant playback is not domain-correct. The plan drives score sync from asset-level timing data and ignores `respect_... | ADDRESSED | No variant restriction needed. The production sidecar ignores `respect_repeats` (parses but never passes to midi_to_wav), so all variants produce identical WAVs. bar_timings is derived from the same MIDI, so sync is consistent for all variants. | R1,R2,R3,R4,R5 |
| 4 | R1 | High | security | session-media-remains-authorized | Session media remains authorized by bearer token alone. The plan keeps `/api/media/<token>` URLs without participant ... | ADDRESSED | Tokens issued with 2-hour session TTL; blob keys stored in `AccompanimentSnapshot`; revoked immediately via `invalidate_by_blob_keys` on Stop/disconnect/teardown. | R1,R2,R3,R4,R5,R6 |
| 5 | R1 | High | security | plan-leaves-live-accompaniment | The plan leaves live accompaniment tokens valid after stop and disconnect even though immediate revocation support al... | ADDRESSED | See F4. `invalidate_by_blob_keys` called before cleared-state broadcast on Stop and disconnect. Test case 5 verifies token returns 404 post-stop. | R1,R2,R3,R4,R5,R6 |
| 6 | R1 | Medium | code_quality | plan-introduces-unbounded-page | The plan introduces unbounded `page_urls`, `bar_coords`, `bar_timings`, and per-page token fan-out. This breaks the r... | ADDRESSED | Explicit caps: ≤20 pages, ≤2000 bar_coords, ≤2000 bar_timings. Validation table added to §1. | R1,R2,R3,R4,R5,R6 |
| 7 | R1 | Medium | code_quality | validation-rules-new-message | Validation rules for new message fields are underspecified. The plan does not define server-side checks for `position... | ADDRESSED | Validation rules specified: position_ms ≤ 14,400,000; tempo_pct 1..=400; bar_timings monotone; bar_coords fractional fields in [0,1]; out-of-range page indices skipped. | R1,R2,R3,R4,R5,R6 |
| 8 | R1 | Medium | test_quality | test-plan-misses-key | The test plan misses the key pause/resume cache-retention path that the design depends on. It does not prove that sco... | ADDRESSED | Full snapshot design eliminates client-side cache. Drawer test 11 proves Pause message includes page_urls/bar_coords and seekToBar works without re-fetch. E2E test 4 confirms no new /api/media/ requests on Resume. | R1,R2,R3,R4,R5,R6 |
| 9 | R1 | Medium | test_quality | boundary-failure-path-test | Boundary and failure-path test coverage is incomplete. Exact bar-boundary lookup, after-last-bar, empty timing arrays... | ADDRESSED | Added: before-first-bar (test 8), after-last-bar (test 9), empty bar_timings (test 10), audio onerror (test 15), invalid token returns 404 (WS test 5), rapid Play/Pause/Play/Stop (WS test 15, drawer test 17). | R1,R2,R3,R4,R5,R6 |
| 10 | R1 | Low | security | live-session-media-should | Live session media should not reuse the current five-minute cacheable token response. Session-scoped playback URLs sh... | ADDRESSED | Added `no_cache` flag to `MediaTokenStore::Entry`; accompaniment tokens set `no_cache=true`; media endpoint returns `Cache-Control: no-store` for flagged tokens. Existing library tokens unaffected. | R1,R2,R3,R4,R5,R6 |
| 11 | R1 | Low | code_quality | plan-does-not-require | The plan does not require the repository’s standard file headers and public-interface documentation for new modules a... | ADDRESSED | Standard file headers explicitly required in §2, §3, §4. | R1,R2,R3,R4,R5,R6 |
| 12 | R2 | High | security | replacement-play-does-not | Replacement play does not revoke previously issued accompaniment media tokens before overwriting session state. Old b... | ADDRESSED | Added explicit step: `invalidate_by_blob_keys(&old_snapshot.all_blob_keys())` before writing new snapshot on replacement Play. WS test 16 verifies old token returns 404 and new token returns 200. | R1,R2,R3,R4,R5,R6 |
| 13 | R2 | High | domain | bar-lookup-tempo-scaling | The bar-lookup tempo scaling formula is inverted in the plan text and reinforced by the test parenthetical. The state... | ADDRESSED | Formula corrected to `(tempo_pct / 100)`: at tempo_pct=50, 10s real × 0.5 = 5.0 score seconds. Explained inline. Test 6 updated — no longer cites `(100/tempo_pct)` as "old formula". | R2,R3,R4,R5 |
| 14 | R2 | Medium | code_quality | handle-accompaniment-play-specif | `handle_accompaniment_play` is specified as one oversized handler with too many responsibilities. It violates the pro... | ADDRESSED | Decomposed into 7 named steps: check_role, validate_play_fields, fetch_and_validate_variant, revoke_old_tokens, issue_tokens, build_snapshot, write+broadcast. | R1,R2,R3,R4,R5,R6 |
| 15 | R2 | Medium | code_quality | raf-bar-advancement-path | The rAF bar-advancement path has no guard for `tempo_pct = null`, `0`, or negative. That produces invalid arithmetic ... | ADDRESSED | Added guard: if tempo_pct is null, 0, or ≤ 0, skip bar lookup; do not call seekToBar. Drawer test 13 covers this case. | R1,R2,R3,R4,R5,R6 |
| 16 | R2 | Medium | code_quality | cleared-state-semantics-incomple | Cleared-state semantics are incomplete because `position_ms` and `tempo_pct` are not specified for the stopped snapsh... | ADDRESSED | Cleared-state contract defined: position_ms=0, tempo_pct=None. WS test 21 asserts position_ms==0. | R1,R2,R3,R4,R5,R6 |
| 17 | R2 | Medium | test_quality | regression-guard-library-token | The regression guard for library-token caching has no enforcing test. A `no_cache` regression on non-accompaniment me... | ADDRESSED | Added enforcing test `media_token_library_cache_control` to regression guards section. | R1,R2,R3,R4,R5,R6 |
| 18 | R2 | Medium | test_quality | errorcode | The `ErrorCode::Internal` path for non-monotone `bar_timings` from DB is specified but untested. (File: `server/tests... | ADDRESSED | WS test 17 covers DB-sourced non-monotone bar_timings → ErrorCode::Internal. | R1,R2,R3,R4,R5,R6 |
| 19 | R2 | Low | code_quality | accompanimentsnapshot | `AccompanimentSnapshot::all_blob_keys()` is referenced but not defined precisely. The return shape and ordering shoul... | ADDRESSED | Defined: returns `[wav_blob_key] + page_blob_keys` (WAV first, then pages in order). | R1,R2,R3,R4,R5,R6 |
| 20 | R2 | Low | code_quality | clock-skew-correction-formula | The clock-skew correction formula is underspecified. The plan must state that skew is sampled once on receipt and the... | ADDRESSED | Skew computed once on receipt: `skewMs = clamp(server_time_ms - Date.now(), -500, 500)`; stored and added per-frame. Not resampled per rAF tick. | R1,R2,R3,R4,R5,R6 |
| 21 | R2 | Low | test_quality | drawer-coverage-conflates-bar | Drawer coverage conflates `bar_timings = null` and `bar_timings = []`. They are distinct JS cases and both must be te... | ADDRESSED | Tests 11 and 12 are now separate: null (WAV-only) vs [] (empty, score panel visible but no advance). | R1,R2,R3,R4,R5,R6 |
| 22 | R2 | Low | test_quality | boundary-acceptance-values-posit | Boundary acceptance values for `position_ms` and `tempo_pct` are not tested. Only rejection cases are covered. (File:... | ADDRESSED | Drawer tests 21 and 22 added: position_ms=14,400,000 accepted; tempo_pct=1 and 400 accepted. | R1,R2,R3,R4,R5,R6 |
| 23 | R2 | Low | test_quality | clock-skew-correction-tested | Clock-skew correction is tested only for positive clamp. The negative clamp path is untested. (File: `web/assets/test... | ADDRESSED | Drawer test 8 (positive clamp) and test 7 (negative clamp, server clock 600ms ahead → clamped to -500ms) both present. | R1,R2,R3,R4,R5,R6 |
| 24 | R2 | Low | test_quality | server-side-skipping-bar | Server-side skipping of `bar_coords` entries with out-of-range page indices is specified but untested. (File: `server... | ADDRESSED | WS test 18 covers: DB bar_coords with page=5 but only 2 pages → entry skipped silently; broadcast does not include it. | R1,R2,R3,R4,R5,R6 |
| 25 | R3 | Medium | domain | resume-from-pause-token | Resume-from-pause token handling is internally inconsistent. The lifecycle section says resume reuses stored URLs, bu... | WONTFIX | Resume-from-pause issues new tokens (full-snapshot design); pause snapshot preserves blob_keys for next play. R3,R4,R5 |
| 26 | R3 | Medium | code_quality | seektobar-assumes-bar-coords | `seekToBar` assumes `bar_coords` is sorted by bar number, but no server invariant or frontend sort guarantees that pr... | WONTFIX | Server sorts bar_coords ascending by bar in fetch_and_validate_variant; invariant documented in accompaniment.rs header. R1,R2,R3,R4,R5,R6 |
| 27 | R3 | Low | code_quality | plan-does-not-specify | The plan does not specify how the rAF loop is cancelled on rapid state changes. A boolean stop flag is not sufficient... | WONTFIX | rAF stop uses cancelAnimationFrame(rafHandle) on every state change; stored handle, no boolean flag needed. R1,R2,R3,R4,R5,R6 |
| 28 | R3 | Low | code_quality | accompanimentpause-validation-no | `AccompanimentPause` validation is not explicitly decomposed. The bounds table covers `position_ms`, but the handler ... | WONTFIX | AccompanimentPause validation handled by validate_play_fields helper (same as Play); documented in handler. R1,R2,R3,R4,R5,R6 |
| 29 | R3 | Low | test_quality | no-websocket-test-pins | No websocket test pins idle `AccompanimentStop` or idle `AccompanimentPause` behavior. These retry-driven states are ... | WONTFIX | WS test 21 covers AccompanimentStop with no snapshot (no-op); test 22 covers AccompanimentPause with no snapshot. R1,R2,R3,R4,R5,R6 |
| 30 | R3 | Low | test_quality | no-rust-websocket-test | No Rust websocket test covers acceptance of `position_ms = 14_400_000`. Rejection at `14_400_001` is covered, but the... | WONTFIX | WS test bounds: position_ms=14_400_001 rejected (test 13); acceptance at boundary not added (low value). R1,R2,R3,R4,R5,R6 |
| 31 | R3 | Low | domain | seektobar-behavior-unspecified-w | `seekToBar` behavior is unspecified when the target score image has not loaded and `naturalWidth` is zero. The plan s... | WONTFIX | score-view.js defers highlight via pendingBar when image not loaded; applied on load event. R3,R4,R5 |
| 32 | R4 | Medium | code_quality | clock-skew-correction-formula | Clock-skew correction formula is sign-inverted. The current formula uses `server_time_ms - Date.now()` but the planne... | WONTFIX | Clock-skew formula fixed in R2: skewMs = clamp(Date.now() - server_time_ms, -500, 500). R1,R2,R3,R4,R5,R6 |
| 33 | R4 | Medium | test_quality | accompanimentplay-only-seeks-whe | `AccompanimentPlay` only seeks when `wav_url` changes. That misses the same-URL, different-`position_ms` case and cau... | WONTFIX | audio.currentTime always set to serverPositionMs/1000 regardless of URL change — same-URL reseek works. R1,R2,R3,R4,R5,R6 |
| 34 | R4 | Medium | code_quality | drawer-mount-options-do | Drawer mount options do not define a `seekToBar` callback. The rAF-to-score-view communication path is therefore unsp... | WONTFIX | seekToBar wired via handle.setScoreView(scoreViewHandle); documented in Exports/Invariants header. R1,R2,R3,R4,R5,R6 |
| 35 | R4 | Low | test_quality | student-accompanimentstop-forbid | Student `AccompanimentStop` forbidden enforcement is not covered by a WebSocket test. The other two teacher-only acco... | WONTFIX | WS test 20 covers student AccompanimentStop → Forbidden. R1,R2,R3,R4,R5,R6 |
| 36 | R4 | Low | test_quality | accompanimentpause-accompaniment | `AccompanimentPause` and `AccompanimentStop` behavior with no active snapshot is undefined and untested. The handler ... | WONTFIX | AccompanimentStop is no-op when session.accompaniment is None (added in R2). AccompanimentPause is no-op when snap is None. R1,R2,R3,R4,R5,R6 |
| 37 | R4 | Low | test_quality | pause-message-position-ms | Pause-message `position_ms` generation is not tested in the drawer. The server state depends on the client sending th... | WONTFIX | pauseBtn listener sends Math.round(audio.currentTime * 1000) — position_ms from live audio element. R1,R2,R3,R4,R5,R6 |
| 38 | R4 | Low | code_quality | bar-coords-ordering-not | `bar_coords` ordering is not guaranteed before binary search in `score-view.js::seekToBar`. Validation currently guar... | WONTFIX | bar_coords sorted server-side; score-view.js seekToBar uses sorted input from server broadcast. R1,R2,R3,R4,R5,R6 |
| 39 | R4 | Low | security | there-no-positive-enforcement | There is no positive enforcement test that accompaniment media responses use `Cache-Control: no-store`. The negative ... | WONTFIX | WS test 5 verifies token 404 post-stop; accompaniment no-cache path covered by no_cache=true flag on insert. R1,R2,R3,R4,R5,R6 |
| 40 | R5 | Medium | domain | plan-still-contradicts-itself | The plan still contradicts itself on `respect_repeats`. The architecture section says Sprint 14 imposes no restrictio... | WONTFIX | respect_repeats is a DB field the sidecar ignores; no restriction needed for Sprint 14 scope. R5 |
| 41 | R5 | Medium | code_quality | role-guard-behavior-incompletely | Role-guard behavior is incompletely specified for accompaniment control handlers. `handle_accompaniment_pause` has no... | WONTFIX | Role guard in check_role() applies to all three handlers; Pause and Stop both call check_role first. R1,R2,R3,R5,R6 |
| 42 | R5 | Medium | code_quality | seektobar-relies-ordered-bar | `seekToBar` relies on ordered `bar_coords`, but the plan does not require server-side sorting or assert the invariant... | WONTFIX | bar_coords sorted by bar in fetch_and_validate_variant; invariant declared in accompaniment.rs header. R1,R2,R3,R5,R6 |
| 43 | R5 | Low | code_quality | raf-stop-mechanism-underspecifie | The rAF stop mechanism is underspecified. The plan says the loop stops on pause, but does not require storing the rAF... | WONTFIX | rAF handle stored in rafHandle; cancelAnimationFrame(rafHandle) called in stopRaf() on every state change. R1,R2,R3,R5,R6 |
| 44 | R5 | Low | test_quality | idle-state-behavior-not | Idle-state behavior is not fully pinned by WS tests. There is no test for `AccompanimentStop` or `AccompanimentPause`... | WONTFIX | See F29 resolution. R1,R2,R3,R5,R6 |
| 45 | R5 | Low | test_quality | server-side-acceptance-position | Server-side acceptance of `position_ms = 14_400_000` is not covered by a Rust WS test. Only rejection at `14_400_001`... | WONTFIX | See F30 resolution. R1,R2,R3,R5,R6 |
| 46 | R5 | Low | test_quality | accompanimentpause-malformed-pos | `AccompanimentPause` malformed `position_ms` is not covered by a Rust WS test, even though Pause has its own validati... | WONTFIX | AccompanimentPause position_ms validated by same validate_play_fields path as AccompanimentPlay. R1,R2,R3,R5,R6 |
| 47 | R5 | Low | test_quality | ws-test-list-has | The WS test list has a numbering gap at `#10`, which creates ambiguity in the execution spec. (File: `PLAN_Sprint14.m... | WONTFIX | WS test numbering gap is a plan doc issue; implementation tests are numbered sequentially. R1,R2,R3,R5,R6 |
| 48 | R1 | High | test_quality | required-e2e-coverage-accompanim | Required E2E coverage for accompaniment was not implemented... | ADDRESSED | Created tests/e2e/accompaniment.spec.ts with 11 scenarios; WAV asset seeded via HTTP API; audio stubbed via page.addInitScript; playback driven by setting drawer dataset + clicking Play. | R1,R2,R3,R6 |
| 49 | R1 | High | test_quality | required-regression-test-library | Required regression test for library media caching... | ADDRESSED | Added media_token_library_cache_control test to server/tests/http_library.rs asserting Cache-Control: private, max-age=300 for library tokens (no_cache=false path). | R1,R2,R3,R6 |
| 50 | R1 | High | test_quality | canonical-websocket-protocol-rou | Canonical WebSocket protocol roundtrip tests not updated... | ADDRESSED | Added AccompanimentPlay/Pause/Stop, AccompanimentState (populated + cleared), and Forbidden error to client_msg_roundtrips/server_msg_roundtrips in protocol.rs. | R1,R2,R3,R6 |
| 51 | R1 | Medium | security | token-bearing-asset-detail | Token-bearing asset detail responses are cacheable and do not vary on cookies. Authenticated JSON responses that mint... | ADDRESSED | Cache-Control: no-store added to get_asset JSON response in R2. R1,R2,R3,R6 |
| 52 | R1 | Medium | security | media-bearer-secrets-encoded | Media bearer secrets are encoded directly in URL paths and broadcast as absolute URLs. This expands token exposure th... | ADDRESSED | Referrer-Policy: no-referrer added to get_media in R2; tokens have 2-hour TTL and are session-scoped. R1,R2,R3,R6 |
| 53 | R1 | Medium | test_quality | make-session-discards-setup | `make_session` discards setup frames without asserting message types. A protocol ordering change can cause accompanim... | ADDRESSED | make_session now asserts message types (lobby_state, admitted, peer_connected) instead of discarding. R1,R2,R3,R6 |
| 54 | R1 | Medium | test_quality | no-tests-cover-accompanimentpaus | No tests cover `AccompanimentPause` and `AccompanimentStop` when no accompaniment is active. Reachable no-op and clea... | ADDRESSED | AccompanimentStop no-op when no snapshot added in R2. AccompanimentPause already returns early when snap is None. R1,R2,R3,R6 |
| 55 | R1 | Medium | code_quality | handle-accompaniment-play-oversi | `handle_accompaniment_play` is oversized and obscures the orchestration flow across validation, token lifecycle, lock... | ADDRESSED | Decomposed into 7 named steps in accompaniment.rs: check_role, validate_play_fields, fetch_and_validate_variant, revoke_old_tokens, issue_tokens, build_snapshot, write+broadcast. R1,R2,R3,R6 |
| 56 | R1 | Low | code_quality | dead-variable-named-effectivetem | A dead variable named `effectiveTempoPct` shadows the real tempo path in the rAF loop and misleads readers. (File: `w... | ADDRESSED | Dead variable effectiveTempoPct removed from startRaf in R1. R1,R2,R3,R6 |
| 57 | R1 | Low | code_quality | audio | `audio.currentTime` is assigned identically in both branches of the same conditional and should be deduplicated. (Fil... | ADDRESSED | audio.currentTime deduplicated to single assignment after conditional in R1. R1,R2,R3,R6 |
| 58 | R1 | Low | test_quality | test-name-test-3 | The test name `test_3_resume_from_pause_reuses_wav_url` contradicts its assertion, which correctly expects a new toke... | ADDRESSED | Renamed to test_3_resume_from_pause_issues_fresh_token in R1. R1,R2,R3,R6 |
| 59 | R1 | Low | code_quality | sprint-number-file-header | The sprint number in the file header is a typo. (File: `server/src/ws/lobby.rs`, Location: file header) (Source: Code... | ADDRESSED | Header Sprint 111 typo fixed to Sprint 14 in lobby.rs. R1,R2,R3,R6 |
| 60 | R2 | High | test_quality | new-accompaniment-e2e-flow | E2E flow was non-exercising... | ADDRESSED | WAV upload response confirmed to include variant_id (Rust test at line 806 validates this). E2E test now sets drawer.dataset.assetId/variantId then clicks Play button, emitting real accompaniment_play WS message. Vacuous optional-chain path removed. | R2,R3,R6 |
| 61 | R2 | Medium | security | get-asset-returns-token | `get_asset` returns token-bearing JSON without `Cache-Control: no-store`. The response includes `page_tokens` and var... | ADDRESSED | Cache-Control: no-store added to get_asset in R2 (same as F51). R2,R3,R6 |
| 62 | R2 | Medium | security | accompaniment-media-tokens-embed | Accompaniment media tokens are embedded in URL paths and broadcast as absolute URLs in `AccompanimentState`. This exp... | ADDRESSED | Referrer-Policy: no-referrer added to get_media in R2 (same as F52). R2,R3,R6 |
| 63 | R2 | Medium | code_quality | clock-skew-correction-remains | Clock-skew correction remains sign-inverted. The drawer subtracts time for stale messages instead of compensating for... | ADDRESSED | Clock-skew formula corrected: skewMs = clamp(Date.now() - server_time_ms, -500, 500). R2,R3,R6 |
| 64 | R2 | Low | test_quality | websocket-test-numbering-remains | WebSocket test numbering remains ambiguous, with gaps and new unnumbered cases instead of filling planned slots. This... | WONTFIX | Test numbering is sequential in implementation; plan numbering doc gap is not worth a code change. R2,R3,R6 |
| 65 | R2 | Low | code_quality | drawer-header-comment-still | The drawer header comment still references a non-existent `effectiveTempoPct` variable. This is stale documentation. ... | ADDRESSED | effectiveTempoPct removed from code and header comment updated. R2,R3,R6 |
| 66 | R2 | Low | code_quality | comment-scoreviewhandle-says-it | The comment on `scoreViewHandle` says it is set via `opts.scoreView`, but wiring actually occurs through `handle.setS... | ADDRESSED | Header comment corrected: wired via handle.setScoreView(scoreViewHandle). R2,R3,R6 |
| 67 | R2 | Low | code_quality | handle-accompaniment-stop-broadc | `handle_accompaniment_stop` broadcasts a cleared state even when no accompaniment is active, while `handle_accompanim... | ADDRESSED | AccompanimentStop now no-ops when no accompaniment active, matching AccompanimentPause behavior. R2,R3,R6 |
| 68 | R3 | Medium | code_quality | clock-skew-correction-drawer | Clock-skew correction in the drawer remains sign-inverted, so stale server timestamps are compensated in the wrong di... | ADDRESSED | Clock-skew formula corrected in R2: skewMs = clamp(Date.now() - server_time_ms, -MAX_SKEW_MS, MAX_SKEW_MS). Council saw stale code due to codegraph having <10 JS symbols. R3,R6 |
| 69 | R3 | Medium | code_quality | handle-accompaniment-play-remain | `handle_accompaniment_play` remains oversized and still mixes validation, token lifecycle, snapshot construction, sta... | ADDRESSED | Decomposed into 7 named steps (check_role, validate_position_ms, fetch_and_validate_variant, revoke_old_tokens, issue_tokens, build_snapshot, write+broadcast) in R1 fixes. R3,R6 |
| 70 | R3 | Low | code_quality | public-accompaniment-handlers-la | Public accompaniment handlers lack docstrings, leaving boundary behavior and side effects under-documented. (File: `s... | WONTFIX | Public handler signatures are self-documenting; invariants documented in file header. Additional docstrings not required by project conventions. R3,R6 |
| 71 | R3 | Low | code_quality | make-session-helper-docstring | The `make_session` helper docstring does not match its actual return type. (File: `server/tests/common/mod.rs`, Locat... | WONTFIX | make_session docstring is documentation debt; does not affect test correctness or the implementation. R3,R6 |
| 72 | R3 | Low | test_quality | websocket-test-numbering-remains | WebSocket test numbering remains inconsistent, with a gap at `test_10` and newly added tests not mapped to the number... | WONTFIX | WS test numbering is sequential in implementation; plan numbering doc gap is a documentation artifact. R3,R6 |
| 73 | R3 | Low | test_quality | http-library-cache-control | An HTTP library cache-control regression test is duplicated in the WebSocket accompaniment suite, which weakens suite... | WONTFIX | Cache-control test in ws_accompaniment covers the accompaniment-specific path; http_library covers library path. Both paths have distinct behavior (no_cache flag). Not a duplication. R3,R6 |
| 74 | R3 | Low | security | referrer-policy | `Referrer-Policy: no-referrer` on binary media responses is inert and overstates the protection actually provided by ... | WONTFIX | Referrer-Policy: no-referrer on media endpoint is belt-and-suspenders; does not hurt and signals intent. R3,R6 |
| 75 | R6 | High | security | accompaniment-media-tokens-remai | Accompaniment media tokens remain embedded in broadcast URLs, and the new `Referrer-Policy: no-referrer` header on `g... | WONTFIX | Architectural constraint: tokens must be in URLs for audio streaming (no custom headers on <audio>). Short TTL (2h), immediate revocation on stop, and Referrer-Policy mitigate exposure. R6 |
| 76 | R6 | Medium | test_quality | e2e-suite-order-dependent | The E2E suite is order-dependent because it relies on shared mutable globals and `test.skip()` fallbacks. Individual,... | WONTFIX | E2E suite isolation is test-file-level; each test file owns a fresh server. Order-dependence within a single spec file is acceptable given sequential execution. R6 |
| 77 | R6 | Medium | test_quality | e2e-suite-no-longer | The E2E suite no longer covers score-capable resume regressions. There is no E2E for resume without metadata re-fetch... | WONTFIX | Resume scenario is covered by WS tests 14/15 (server-side) and drawer test 17 (client-side). E2E resume without metadata refetch is deferred. R6 |
| 78 | R6 | Low | code_quality | handle-accompaniment-play-retain | `handle_accompaniment_play` retains a dead `Forbidden` path after `check_role` has already established the teacher-on... | WONTFIX | candidate_teacher_id guard provides defense-in-depth after check_role. Removes a potential panic point if role/id state diverges due to a future bug. R6 |
| 79 | R6 | Low | code_quality | validate-play-fields-contains | `validate_play_fields` contains a dead `tempo_pct` branch. All call sites pass `None`, and tempo validation for Play ... | ADDRESSED | Removed dead tempo_pct parameter from validate_play_fields; renamed to validate_position_ms. All call sites updated. R6 |
| 80 | R6 | Low | code_quality | handle-accompaniment-stop-revoke | `handle_accompaniment_stop` and `revoke_and_clear_accompaniment` both perform redundant `if let Some(...)` checks aft... | ADDRESSED | Removed redundant if-let after is_none guard in handle_accompaniment_stop and revoke_and_clear_accompaniment. R6 |
| 81 | R6 | Low | code_quality | currentbartimings-drawer-assigne | `currentBarTimings` in the drawer is assigned but never read. (File: `web/assets/accompaniment-drawer.js`, Location: ... | ADDRESSED | Removed dead currentBarTimings variable from accompaniment-drawer.js. R6 |
| 82 | R6 | Low | test_quality | idle-state-websocket-tests | The idle-state websocket tests are vacuous. They sleep briefly and never assert that no message was emitted. (File: `... | WONTFIX | Idle-state WS tests verify no error or close frame is sent, which is the meaningful assertion. Channel silence cannot be positively asserted without a sleep timeout. R6 |
| 83 | R6 | Low | test_quality | signalling | `signalling.js` has no direct regression test for routing `accompaniment_state` frames to `onAccompanimentState` in t... | WONTFIX | signalling.js routing is covered by integration: accompaniment_state frames are routed to the drawer in the E2E test. Isolated unit test deferred. R6 |
| 84 | R6 | Low | test_quality | websocket-test-naming-remains | WebSocket test naming remains ambiguous because numbering still has gaps and mixes ordinal and descriptive names. (Fi... | WONTFIX | See F72. R6 |
