# Findings Tracker: Sprint 5 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | test_quality | mailer-timeout-test-invalid | Mailer timeout test is invalid because a delayed HTTP response exercises total timeout, not connect timeout. The plan... | ADDRESSED | §5.1 #62–#63: split into two distinct tests — #62 stalls response body (total timeout), #63 accepts TCP but never sends HTTP response (connect timeout). Two separate wiremock behaviours, two distinct error assertions with tolerance bands. | R1,R2 |
| 2 | R1 | High | test_quality | healthz-introduced-but-not | `/healthz` is introduced but not tested at all, including readiness behavior during shutdown. | ADDRESSED | §5.1 #1–#2: added tests for 200 + minimal body in normal state and 503 after shutdown.cancel(). | R1,R2 |
| 3 | R1 | High | test_quality | cloudflare-worker-has-no | The Cloudflare Worker has no test coverage for auth handling, dispatch behavior, or its constant-time comparison path. | ADDRESSED | §5.1 #46–#51: six Worker tests covering valid auth, invalid auth, missing auth, wrong method, upstream 502, and `from`-from-config. | R1,R2 |
| 4 | R1 | High | domain | secret-comparison-planned-as | Secret comparison is planned as a hand-rolled constant-time implementation. | ADDRESSED | §4.1.1: Rust uses `subtle::ConstantTimeEq`; Worker uses `crypto.subtle` HMAC-based timing-safe compare (§4.10.3). Both named explicitly; test §5.1 #40 verifies `SecretString` eq cases. | R1,R2 |
| 5 | R1 | High | security | turn-deployment-vulnerable-ssrf | TURN deployment is vulnerable to SSRF — coturn does not deny RFC-1918, link-local, or IMDS ranges. | ADDRESSED | §4.10.2: added `denied-peer-ip` for RFC-1918 (10/8, 172.16/12, 192.168/16), link-local (169.254/16), loopback (127/8), CGNAT (100.64/10), and private IPv6 ranges (::1, fc00::/7, fe80::/10). | R1,R2 |
| 6 | R1 | High | security | turnserver | `turnserver.conf` is planned as world-readable while containing `static-auth-secret`. | ADDRESSED | §4.10.2: permissions changed to `0600`, owner set to `turnserver:turnserver`. | R1,R2 |
| 7 | R1 | High | domain | session-log-design-internally | Session-log design inconsistent — `log_id` non-optional but `open_row` runs after the session write. | ADDRESSED | §4.2.2: `log_id: Option<SessionLogId>` — transiently None. Sequencing explicit: admit writes session with None, then opens row, then sets Some. `record_peak`/`close_row` short-circuit on None. §5.1 #10 covers the open_row-failure path. | R1,R2 |
| 8 | R1 | High | test_quality | block-ttl-secs-clamping | `block_ttl_secs` clamping not tested. | ADDRESSED | §5.1 #31–#34: added tests for 0 = plain reject (close 1000), upper bound clamp at 86400, above-cap clamped, and absent field backward-compat. | R1,R2 |
| 9 | R1 | High | test_quality | regression-table-cross-reference | Regression table backward-compat reference points to wrong test. | ADDRESSED | §5.3 table: corrected reference to §5.1 #33; explained the correct semantics (serde default, plain reject, close 1000). | R1,R2 |
| 10 | R1 | Medium | domain | background-rate-limit-sweep | Background sweeper cancellation mechanism unspecified. | ADDRESSED | §4.5.1: sweeper `JoinHandle` stored on `AppState.ws_join_rate_sweeper`; `main.rs` calls `.abort()` on SIGINT; sweeper's own loop uses `state.shutdown.cancelled()`. | R1,R2 |
| 11 | R1 | Medium | domain | join-rate-limiter-plan | MutexGuard not explicitly dropped before `.await`. | ADDRESSED | §4.5.2: explicit code block shows guard as `DashMap RefMut` dropped at `}` before the send `.await` calls. Commentary in the snippet. | R1,R2 |
| 12 | R1 | Medium | security | turn-credentials-has-no | `/turn-credentials` has no application-layer rate limit. | ADDRESSED | §4.4.1: reuses the in-memory per-IP limiter (independent limit: 10 req / 60 s); returns 429 on excess. | R1,R2 |
| 13 | R1 | Medium | security | worker-accepts-from-from | Worker accepts `from` from request body — sender identity becomes attacker-controlled on secret leak. | ADDRESSED | §4.10.3 Worker code: `from` removed from request body; taken from `env.MAIL_FROM` only. §4.8.1: Rust mailer request body also omits `from`. §5.1 #51 pins this. | R1,R2 |
| 14 | R1 | Medium | security | cloudflare-ingress-restrictions | CF ingress restrictions treated as runbook step, not IaC. | ADDRESSED | §4.10.1: Container App row updated — CF IP allow-list codified in Bicep `ipSecurityRestrictions`, not runbook only. Bicep array parameterized; runbook documents refresh. | R1,R2 |
| 15 | R1 | Medium | security | sb-session-log-pepper | `SB_SESSION_LOG_PEPPER` not listed as prod-required. | ADDRESSED | §4.1.1 (new field `session_log_pepper`) + §4.1.2 env table: listed as **prod-required**, min 32 bytes, fail-fast in `validate_prod_config`. | R1,R2 |
| 16 | R1 | Medium | test_quality | plan-does-not-test | Short-secret for `SB_TURN_SHARED_SECRET` not tested. | ADDRESSED | §5.1 #36: `validate_prod_config` rejects < 32 bytes; 31-byte rejected, 32-byte accepted. | R1,R2 |
| 17 | R1 | Medium | test_quality | malformed-cf-connecting-ip | Malformed `CF-Connecting-IP` fallback not tested. | ADDRESSED | §5.1 #44–#45: malformed CF header falls through to X-Forwarded-For first token; both malformed falls through to socket addr. | R1,R2 |
| 18 | R1 | Medium | test_quality | concurrent-close-row-calls | Concurrent `close_row` calls not covered. | ADDRESSED | §4.2.2: first-writer-wins design (`WHERE ended_at IS NULL`). §5.1 #15 + §5.2: concurrent test + idempotency unit test. | R1,R2 |
| 19 | R1 | Medium | test_quality | duration-secs-behavior-underspec | `duration_secs` underspecified and untested. | ADDRESSED | §4.2.2: `duration_secs = MAX(0, ended_at - started_at)`. §5.1 #16: arithmetic test + negative-duration → 0 test. | R1,R2 |
| 20 | R1 | Medium | test_quality | replica-guard-min-max | Replica guard absent from test/regression strategy. | ADDRESSED | §5.3: added regression entry for `scripts/check-bicep.sh` asserting `min=max=1` in CI. | R1,R2 |
| 21 | R1 | Medium | domain | record-peak-atomics-not | `record_peak` atomics not anchored to concrete type. | ADDRESSED | §4.2.2: `ActiveSession` gains `peak_loss_bp: AtomicU16, peak_rtt_ms: AtomicU16` fields directly. DB update driven from these. | R1,R2 |
| 22 | R1 | Medium | code_quality | reject-path-does-not | Close codes not branched between plain reject (1000) and blocked (1008). | ADDRESSED | §4.6.2: explicit branch documented — plain reject → 1000, blocked → 1008. §5.3 regression guard + §5.1 #34 pins both codes. | R1,R2 |
| 23 | R1 | Medium | code_quality | config | `Config::from_env()` too many responsibilities. | ADDRESSED | §4.1.1: decomposed into `parse_env()` + `validate_prod_config()`. | R1,R2 |
| 24 | R1 | Low | code_quality | blocked-room-cardinality-cap | Cardinality cap is prose literal. | ADDRESSED | §4.6.2: `pub const BLOCK_LIST_CAP: usize = 256` named in `server/src/state.rs`. | R1,R2 |
| 25 | R1 | Low | domain | mailer-trait-object-bounds | `Mailer` trait bounds not confirmed as `Send + Sync`. | ADDRESSED | §4.8.1: explicit note that the existing `Mailer: Send + Sync + 'static` bounds (present since Sprint 1) are confirmed; `CloudflareWorkerMailer` upholds them. | R1,R2 |
| 26 | R1 | Low | code_quality | turn-ttl-secs | `turn_ttl_secs: u32` inconsistent with other duration fields. | ADDRESSED | §4.1.1: changed to `turn_ttl_secs: i64`. | R1,R2 |
| 27 | R1 | Low | code_quality | join-rate-config-naming | Join-rate fields drop `_rate_limit_` convention. | ADDRESSED | §4.1.1: renamed to `ws_join_rate_limit_per_ip` / `ws_join_rate_limit_window_secs`. | R1,R2 |
| 28 | R1 | Low | code_quality | dashmap-ipaddr-arc-mutex | `DashMap<IpAddr, Arc<Mutex<TokenBucket>>>` adds unjustified indirection. | ADDRESSED | §4.5.1: changed to `DashMap<IpAddr, WsJoinBucket>` (no Arc, no Mutex); DashMap shard lock is sufficient. | R1,R2 |
| 29 | R1 | Low | code_quality | sessionlogid-pub-uuid-weakens | `SessionLogId(pub Uuid)` exposes inner value. | ADDRESSED | §4.2.2: `SessionLogId(Uuid)` — private inner. `SessionLogId::new()` is the only constructor. | R1,R2 |
| 30 | R1 | Low | code_quality | session-log-test-prose | Test prose uses `ends_at` instead of `ended_at`. | ADDRESSED | §5.1 #9: corrected to `ended_at`. | R1,R2 |
| 31 | R1 | Low | code_quality | worker-mailer-struct-still | Mailer struct has redacted placeholder field name. | ADDRESSED | §4.8.1: struct fields renamed to `worker_url` and `bearer_secret`; `from`/`subject` moved to module-level constants. | R1,R2 |
| 32 | R1 | Low | code_quality | reduced-max-active-rooms | Reduced `max_active_rooms` default not justified. | ADDRESSED | §4.1.1: reverted — `max_active_rooms` stays at 1024 with justification (single replica cap is the constraint, not the in-memory limit). | R1,R2 |
| 33 | R1 | Low | test_quality | ice-client-tests-do | ICE client tests don't cover `ttl=0`. | ADDRESSED | §5.1 #56: `ttl=0` returns immediately stale; both calls issue a fetch; no division-by-zero. | R1,R2 |
| 34 | R1 | Low | test_quality | record-peak-missing-closed | `record_peak` on missing/closed row has no no-op contract. | ADDRESSED | §4.2.2: `WHERE id = ?` naturally finds nothing if row is absent/closed; §5.1 #14 pins this as an explicit test. | R1,R2 |
| 35 | R1 | Low | test_quality | sb-ws-join-rate | `SB_WS_JOIN_RATE_PER_IP=0` undefined and untested. | ADDRESSED | §4.5.1: 0 = disabled (all traffic passes); documented and named. §5.1 #24 tests this. | R1,R2 |
| 36 | R1 | Low | security | healthz-response-body-should | `/healthz` response body should be minimal. | ADDRESSED | §10 step 11: body is `{"status":"ok"}` — fixed string, no version or internal state. §5.1 #1 verifies the exact body. | R1,R2 |
| 37 | R2 | Medium | domain | turn-credential-rate-limiting | TURN credential rate limiting is internally inconsistent and under-specified. The plan says it has an independent lim... | ADDRESSED | After adding cookie auth (#51), the rate limiter only applies to authenticated teachers. 10 req/60s is appropriate for teacher use. Unauthenticated requests now receive 401 before reaching the rate limiter. | R1,R2 |
| 38 | R2 | Medium | domain | admit-open-row-sequencing | `admit()` to `open_row()` sequencing can orphan session-log rows. If disconnect occurs after `open_row()` succeeds bu... | ADDRESSED | lobby.rs: after open_row succeeds, check if active_session still exists when re-acquiring write lock; if absent (disconnect raced), close the orphaned row immediately with EndedReason::Disconnect. | R1,R2 |
| 39 | R2 | Medium | test_quality | session-metrics-test-plan | The `session_metrics` test plan does not cover the new per-connection 5-second rate limit, and one existing assertion... | OPEN |  | R2 |
| 40 | R2 | Medium | security | secret-parameters-planned-bicep | Secret parameters in the planned Bicep modules are not marked with `@secure()`. That exposes plaintext values in ARM ... | ADDRESSED | All secret params in container-app.bicep and coturn-vm.bicep are marked @secure(). Verified: sbTurnSharedSecret, sbCfWorkerUrl, sbCfWorkerSecret, sbSessionLogPepper, logWorkspaceKey, adminSshPublicKey (ssh pubkeys are not secret but @secure() adds no harm). | R1,R2 |
| 41 | R2 | Medium | code_quality | cloudflareworkermailer-plan-stil | The `CloudflareWorkerMailer` plan still has conflicting field names across the struct, impl, and resolution note. The... | ADDRESSED | Code is correct: struct fields are `worker_url` and `bearer_secret`, consistent with all usage. Finding was about plan doc inconsistency, not code. | R1,R2 |
| 42 | R2 | Low | test_quality | plan-does-not-specify | The plan does not specify behavior for `session_metrics` sent by a lobby student who has not yet been admitted. The t... | ADDRESSED | handle_session_metrics early-returns true (no-op, keep connection) if active_session is None — student in lobby just gets silently dropped frames. | R2 |
| 43 | R2 | Low | domain | peak-metric-accumulation-does | Peak metric accumulation does not name the required atomic operation. Without an explicit `fetch_max`, an implementer... | ADDRESSED | Code uses `AtomicU16::fetch_max` (compare-and-swap loop) in handle_session_metrics. Named explicitly. | R1,R2 |
| 44 | R2 | Low | domain | config | The `Config::from_env()` snippet does not show the production-only guard around `validate_prod_config()`. The comment... | ADDRESSED | config.rs: `from_env()` calls `parse_env()` then `validate_prod_config()` only when `!c.dev`. Guard is explicit in code. | R1,R2 |
| 45 | R2 | Low | security | worker-auth-path-needs | The Worker auth path needs an explicit empty-header check before `timingSafeEqual`. Without it, missing `Authorizatio... | ADDRESSED | magic-link-relay.js line 38: `if (!hdr) { return 401; }` before timingSafeEqual. Already implemented. | R1,R2 |
| 46 | R2 | Low | security | server-cargo | `server/Cargo.toml` is missing from the change list even though the plan introduces new dependency requirements. That... | ADDRESSED | server/Cargo.toml was modified in Sprint 5 implementation (reqwest, sha1, hmac, base64, subtle added). Commit d55b9f7 references prior implementation commit. | R1,R2 |
| 47 | R2 | Low | code_quality | wsjoinbucket-fields-exposed-too | `WsJoinBucket` fields are exposed too broadly as `pub`. This weakens the intended encapsulation around mutation under... | ADDRESSED | WsJoinBucket.count and .window_start_unix made private; only check_and_inc and sweep_stale (same module) mutate them. | R1,R2 |
| 48 | R2 | Low | code_quality | env-var-table-incomplete | The env-var table is incomplete for newly introduced config fields, including WS join limiter settings and default lo... | WONTFIX | The env-var table lives in the sprint plan which is being archived. The runbook (knowledge/runbook/deploy.md) does not maintain an env-var table — config.rs is the source of truth for env var names. | R1,R2 |
| 49 | R1 | High | domain | coturn-never-receives-deployed | coturn never receives the deployed TURN shared secret because the cloud-init payload is built from a raw triple-quote... | ADDRESSED | coturn-vm.bicep: split into cloudInitTemplate (raw string) + cloudInit = base64(replace(cloudInitTemplate, 'TURN_SECRET_PLACEHOLDER', turnSharedSecret)). | R1 |
| 50 | R1 | High | domain | azure-deployment-blocked-by | Azure deployment is blocked by a required `sbJwtSecret` parameter that is not passed in the documented deployment flo... | ADDRESSED | Removed `@secure() param sbJwtSecret string` from container-app.bicep entirely. It was unused. | R1 |
| 51 | R1 | High | security | turn-credentials-issues-valid | `/turn-credentials` issues valid TURN credentials without caller authentication, allowing unauthenticated credential ... | ADDRESSED | /turn-credentials now requires a valid teacher session cookie (401 otherwise). Students receive ice_servers in ServerMsg::Admitted via WS. signalling.js uses admitted ice_servers directly for student RTCPeerConnection; no unauthenticated HTTP fetch. | R1 |
| 52 | R1 | High | code_quality | session-log-cleanup-uses | session-log cleanup uses a tautological teacher-side condition and can close the active session log when the wrong co... | ADDRESSED | ws/mod.rs cleanup(): replaced `state.rooms.get(&slug).is_some()` with `rs.teacher_conn.as_ref().map(|c| c.id) == Some(ctx.id)`. Only the actual student or teacher conn triggers log closure. | R1 |
| 53 | R1 | Medium | domain | turns | `turns://` support is incomplete because coturn is configured for TLS on `5349` without certificate provisioning or r... | ADDRESSED | turns:// URL removed from build_ice_servers() and ServerMsg::Admitted. Only stun: and turn: URLs emitted until cert provisioned. | R1 |
| 54 | R1 | Medium | domain | record-peak-updates-already | `record_peak` updates already-closed session rows, contradicting the documented invariant for session-log writes (Fil... | ADDRESSED | session_log.rs: record_peak SQL WHERE clause now includes `AND ended_at IS NULL`. Closed rows are no-op. | R1 |
| 55 | R1 | Medium | domain | loss-bp-rtt-ms | `loss_bp` and `rtt_ms` are persisted without server-side bounds enforcement, so malformed client input can corrupt mo... | ADDRESSED | handle_session_metrics: `let loss_bp = loss_bp.min(10_000)` applied before atomic fetch_max and DB persist. rtt_ms is u16 (max 65535ms = ~65s, acceptable ceiling for RTT). | R1 |
| 56 | R1 | Medium | code_quality | six-production-files-use | six production files use file-level `#![allow(dead_code)]`, which suppresses dead-code detection broadly and obscures... | ADDRESSED | Removed `#![allow(dead_code)]` from all 6 files: mailer.rs, config.rs, turn.rs, session_log.rs, lobby.rs, rate_limit.rs. Build passes with zero dead_code warnings. | R1 |
| 57 | R1 | Medium | security | cf-worker-url-only | `cf_worker_url` is only checked for presence and is not validated as HTTPS before bearer-token use (File: `server/src... | ADDRESSED | validate_prod_config: parses cf_worker_url, returns ConfigError::Invalid if scheme != "https". Test added. | R1 |
| 58 | R1 | Low | code_quality | several-functions-materially-exc | several functions materially exceed the stated 60-line complexity ceiling, including `admit`, `cleanup`, `reject`, an... | WONTFIX | These functions encode the session/lobby state machine which is inherently multi-step. Splitting would increase indirection without improving readability. Accepted as a documentation guideline deviation. | R1 |
| 59 | R1 | Low | code_quality | resolve-peer-ip-contains | `resolve_peer_ip` contains dead `Forwarded`-header logic that implies RFC 7239 support but never executes it (File: `... | ADDRESSED | ws/mod.rs: removed the dead FORWARDED header chain; resolve_peer_ip now goes directly to X-Forwarded-For after CF-Connecting-IP. | R1 |
| 60 | R1 | Low | security | secretstring | `SecretString::PartialEq` does not uphold its documented constant-time claim on length mismatch (File: `server/src/au... | ADDRESSED | secret.rs: PartialEq now HMAC-SHA256 digests both operands with a fixed key, producing 32-byte outputs, then compares via subtle::ConstantTimeEq. Constant-time for any input length. | R1 |
| 61 | R1 | Low | code_quality | secretstring | `SecretString::is_empty()` has no callers (File: `server/src/auth/secret.rs`, Location: `is_empty`) (Source: Code Qua... | ADDRESSED | SecretString::is_empty() removed. | R1 |
| 62 | R1 | Low | security | deployment-workflow-still-relies | the deployment workflow still relies on a long-lived Azure service principal secret instead of OIDC federation (File:... | ADDRESSED | deploy.yml: azure/login@v2 now uses client-id/tenant-id/subscription-id with `permissions: id-token: write`. AZURE_CREDENTIALS secret replaced by three OIDC secrets. | R1 |
