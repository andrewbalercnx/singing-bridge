# File: .config/nextest.toml
# Purpose: cargo-nextest configuration — limits concurrency for connection-sensitive tests.
# Last updated: Sprint 27 (2026-05-08) -- add ci profile + serial group for db_pool test

[profile.default]

# db_pool_allows_concurrent_connections competes with parallel binaries for
# postgres connections; run it alone so the acquire timeout is meaningful.
[[profile.default.overrides]]
filter = 'binary(db_pool)'
test-group = 'serial'

[test-groups]
serial = { max-threads = 1 }

# CI profile: cap concurrency to avoid overwhelming the shared postgres instance.
# 30 test binaries × 5 connections each = 150 potential connections; Azure Basic
# tier caps at ~50. With test-threads=8 the peak is ~40 connections.
[profile.ci]
test-threads = 8

[[profile.ci.overrides]]
filter = 'binary(db_pool)'
test-group = 'serial'
