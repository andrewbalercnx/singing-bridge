# Findings Tracker: Sprint 7 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | plan-states-student-before | The plan states a student-before-admission signalling problem but only designs teacher-to-student lobby messaging. Th... | ADDRESSED | Plan R2 redesigned to one-way teacher→lobby pre-admission; bidirectional only after admit. | R1,R2 |
| 2 | R1 | High | domain | lobby-messaging-design-uses | The lobby messaging design uses an undefined `EntryId`, assumes `LobbyEntry.id`, and does not expose any entry identi... | ADDRESSED | `EntryId` defined in protocol.rs; included in every `LobbyState` message sent to teacher. | R1,R2 |
| 3 | R1 | High | security | handle-chat-must-authorize | `handle_chat` must authorize by current connection identity and active-session membership, not only by role or `activ... | VERIFIED | Security Expert confirmed: conn.id check against active_session.{teacher,student}_conn.id implemented. | R1,R2,R4 |
| 4 | R1 | High | security | handle-lobby-message-must | `handle_lobby_message` must verify that the sender is the current `teacher_conn`, not only a connection with `Role::T... | VERIFIED | Security Expert confirmed: conn.id == room.teacher_conn.id check in place. | R1,R2,R4 |
| 5 | R1 | High | test_quality | test-strategy-omits-required | The test strategy omits required regression coverage for the new DOM elements and does not verify `ServerMsg::Chat.fr... | ADDRESSED | ws_chat.rs integration tests verify Chat.from field; JS tests cover label rendering. | R1,R2,R4 |
| 6 | R1 | Medium | code_quality | plan-proposes-new-chatnotinsessi | The plan proposes a new `ChatNotInSession` error even though `ErrorCode::NotInSession` already exists for the same co... | VERIFIED | Code Quality Expert confirmed: implementation reuses `ErrorCode::NotInSession`. | R1,R2,R4 |
| 7 | R1 | Medium | code_quality | chat-length-validation-does | Chat length validation does not follow the project's established dual-limit pattern. The plan needs `MAX_CHAT_BYTES` ... | VERIFIED | Code Quality Expert confirmed: dual-limit (MAX_CHAT_BYTES + MAX_CHAT_CHARS) implemented. | R1,R2,R4 |
| 8 | R1 | Medium | domain | admission-race-lobby-messaging | The admission race for lobby messaging is not fully specified in the teacher UX. If a student is admitted before the... | ADDRESSED | LobbyMessage targets LobbyEntry.conn.tx; admitted students leave lobby so the tx is already gone — silent drop on race is acceptable. | R1,R2 |
| 9 | R1 | Medium | test_quality | plan-does-not-specify | The plan does not specify whether empty chat messages are valid. The handler design currently accepts them, and the t... | ADDRESSED | Empty text → PayloadTooLarge; chat_empty_text_rejected test added. | R1,R2,R4 |
| 10 | R1 | Medium | test_quality | test-plan-references-unverified | The test plan references unverified regression guards and a new helper despite existing uncertainty about actual test... | ADDRESSED | make_session helper implemented in ws_chat.rs; all referenced tests exist. | R1,R2,R4 |
| 11 | R1 | Medium | test_quality | new-client-api-functions | New client API functions `sendChat` and `sendLobbyMessage` are not covered in JS unit tests even though similar signa... | ADDRESSED | sendChat and sendLobbyMessage serialisation tests added in chat.test.js. | R1,R2,R4 |
| 12 | R1 | Low | domain | chat-from | The `Chat { from: Role }` field is workable for a two-party chat, but the UI mapping of role-to-self is not documente... | ADDRESSED | teacher.js maps from=teacher→"You"/from=student→"Student"; student.js mirrors. Verified by JS unit tests. | R1,R2 |
| 13 | R1 | Low | code_quality | handle-chat-pseudocode-says | The `handle_chat` pseudocode says it resolves the other party's tx and sends to both parties without explicitly stati... | VERIFIED | Code Quality Expert confirmed: guard dropped before send; both txs sent to. | R1,R2,R4 |
| 14 | R1 | Low | security | plan-should-explicitly-preserve | The plan should explicitly preserve text-only rendering for chat and lobby messages with `textContent` and fixed clas... | VERIFIED | Security Expert confirmed: all user strings via .textContent only in teacher.js, student.js, signalling.js. | R1,R2,R4 |
| 15 | R2 | Medium | test_quality | plan-adds-new-teacher | The plan adds new teacher and student chat-related DOM nodes but does not require extending the established HTML regr... | OPEN | Deferred to Sprint 8 — add chat/lobby DOM markers to HTTP HTML regression tests. | R2,R4 |
| 16 | R2 | Medium | code_quality | handle-lobby-message-relies | `handle_lobby_message` relies on `EntryNotFound` and `NotOwner` failure paths, but the plan does not state whether th... | VERIFIED | Code Quality Expert confirmed: both error codes correctly returned; integration tests cover both paths. | R2,R4 |
| 17 | R2 | Low | test_quality | test-strategy-does-not | The test strategy does not list an empty-string rejection case for `LobbyMessage`, despite the handler design requiri... | OPEN | Deferred to Sprint 8 — add lobby_message_empty_text_rejected test. | R2,R4 |
| 18 | R2 | Low | test_quality | js-test-plan-does | The JS test plan does not cover `#chat-panel` visibility changes on `PeerConnected` and `PeerDisconnected`. (File: `P... | ADDRESSED | chat panel visibility lifecycle test added in chat.test.js. | R2,R4 |
| 19 | R2 | Low | code_quality | validation-order-checks-upper | Validation order checks upper bounds before rejecting empty text in the handler pseudocode. (File: `server/src/ws/mod... | VERIFIED | Code Quality Expert confirmed: empty check is first in validate_chat_text. | R2,R4 |
| 20 | R2 | Low | code_quality | reusing-payloadtoolarge-empty-te | Reusing `PayloadTooLarge` for empty text overloads the meaning of the error code and weakens machine-readable error h... | OPEN | Deferred to Sprint 8 — consider adding `EmptyPayload` error code. | R2,R4 |
| 21 | R2 | Low | code_quality | plan-acknowledges-chat-log | The plan acknowledges chat log growth as a risk but does not make an explicit bounded-resource decision for the clien... | VERIFIED | Code Quality Expert confirmed: replaceChildren() clears log on disconnect; growth bounded per session. | R2,R4 |
| 22 | R4 | Medium | test_quality | new-chat-lobby-dom | New chat and lobby DOM markers lack regression assertions in the existing HTTP HTML test suite. (File: `server/tests/... | OPEN | Deferred to Sprint 8. | R4 |
| 23 | R4 | Medium | test_quality | websocket-tests-do-not | WebSocket tests do not cover connection-identity authorization failures for rogue connections sending `Chat` or `Lobb... | OPEN | Deferred to Sprint 8. | R4 |
| 24 | R4 | Low | code_quality | handle-chat-handle-lobby | `handle_chat` and `handle_lobby_message` silently drop messages from unjoined connections instead of returning an exp... | OPEN | Deferred to Sprint 8 — return explicit not_in_session/not_owner error. | R4 |
| 25 | R4 | Low | test_quality | byte-boundary-coverage-multibyte | Byte-boundary coverage for multibyte chat payloads is absent, so `MAX_CHAT_BYTES` is not directly pinned by tests. (F... | OPEN | Deferred to Sprint 8 — add 4-byte emoji boundary test. | R4 |
