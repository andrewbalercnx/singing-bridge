# Findings Tracker: Sprint 3 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | tier-reason-truncation-unsafe | `tier_reason` truncation is unsafe unless implemented on character boundaries. A byte-based `String::truncate(200)` w... | ADDRESSED | R2: §4.12 now shows `truncate_to_chars(s, max_chars)` using `chars().take(max_chars).collect()`; §5.2 includes a regression fixture with a 3-byte codepoint ('中') straddling the byte boundary that would fail byte-based truncation. `MAX_TIER_REASON_LEN` constant in `protocol.rs` (§4.11) documents the cap in chars, not bytes. | R1,R2 |
| 2 | R1 | High | code_quality | wirebidirectionalmedia-has-no-cl | `wireBidirectionalMedia` has no cleanup path for partial acquisition failure. If local audio succeeds and local video... | ADDRESSED | R2: Partial-failure cleanup extracted into pure `acquireMedia(audioImpl, videoImpl)` helper (§4.4). On video throw, audio stream tracks are stopped before the error propagates. Node-testable via injected impls. New test `signalling.test.js::partial failure stops audio stream when video acquisition throws` (§5.3) pins the behaviour. | R1,R2 |
| 3 | R1 | High | test_quality | controls | `controls.js` has no automated coverage for the mute/video toggle state machine. The current plan relies on manual ch... | ADDRESSED | R2: `deriveToggleView(enabled, onLabel, offLabel)` extracted as pure helper (§4.5) returning `{label, ariaPressed}`. UMD export for Node. 6 tests in `controls.test.js` covering true/false/repeated-toggle/null-enabled/absent-labels/return-shape (§5.1 item 10). | R1,R2 |
| 4 | R1 | High | test_quality | codec-preference-ordering-left | Codec-preference ordering is left untested even though the ordering logic is pure and extractable. This leaves the co... | ADDRESSED | R2: `orderCodecs(codecs, prefer)` extracted as pure helper (§4.3) with stable partition (NOT .sort) for explicit ordering guarantee. UMD export. 5 tests in `video.test.js`: H264-first, VP8-first, empty input, unknown prefer, stable ordering (§5.1 item 9). `applyCodecPreferences` is now a thin browser wrapper over the tested helper. | R1,R2 |
| 5 | R1 | High | test_quality | browser-tier-decision-boundaries | Browser-tier decision boundaries are not tested at version floors. The plan omits `floor - 1`, `floor`, and `floor + ... | ADDRESSED | R2: §5.1 item 7 adds 9 boundary tests driven by `BROWSER_FLOORS` constants — Chrome/Firefox/Safari-desktop × {floor-1→degraded, floor→supported, floor+1→supported}. Named constants keep the tests synchronised with any future floor bump. Checklist step 2 lists the count explicitly. | R1,R2 |
| 6 | R1 | High | security | browser-gating-design-trusts | The browser-gating design trusts client-reported compatibility state. The server still admits `lobby_join` without in... | ADDRESSED | R2: §4.11 (explicit trust-model note) + §9 decision #4 + #6 state that the gate is UX advisory, not a security boundary — no privileged surface behind the gate; tampering only harms the client's own session. Default tier flipped from `Supported` to `Degraded` (§4.11, §9 #6) so a missing/tampered `tier` field surfaces a warning to the teacher rather than silently passing as healthy. `test_lobby_join_without_tier_defaults_to_degraded` (§5.2) pins the new default. | R1,R2 |
| 7 | R1 | Medium | code_quality | refs | The `refs.audio` to `refs.media` rename is not made explicit where teardown actually reads the ref. This is an easy s... | ADDRESSED | R2: §4.4 closing paragraph explicitly names the rename sites (makeTeardown + both connectTeacher and connectStudent); checklist step 7 also calls this out as a silent-regression risk so the editor touches every call site. | R1,R2 |
| 8 | R1 | Medium | code_quality | video | `video.js` needs a Node/CommonJS export path for `hasVideoTrack` if the planned Node tests are to run. The current mo... | ADDRESSED | R2: §3 module surface and §4.3 now wrap `video.js` in a UMD factory matching `sdp.js`. Pure helpers (`hasVideoTrack`, `orderCodecs`) exported under both CommonJS and browser; DOM wrappers (`startLocalVideo`, `attachRemoteVideo`, `detachRemoteVideo`, `applyCodecPreferences`) attached to `window.sbVideo` only. | R1,R2 |
| 9 | R1 | Medium | code_quality | wirecontrols-parameter-contract | The `wireControls` parameter contract is inconsistent across plan sections. One section uses `{ audio, video, hangup,... | ADDRESSED | R2: Canonical signature pinned in §3 module surface and §4.5 implementation: `wireControls({ audioTrack, videoTrack, onHangup })`. All references updated. | R1,R2 |
| 10 | R1 | Medium | test_quality | ontrack-audio-video-dispatch | `ontrack` audio/video dispatch has no direct regression test. Existing HTML marker tests do not exercise the branch l... | ADDRESSED | R2: `dispatchRemoteTrack(ev, { onAudio, onVideo })` extracted as pure helper in §4.4 (signalling.js UMD). 5 Node tests in `signalling.test.js`: audio→onAudio only, video→onVideo only, unknown kind silent, null/undefined event no-throw, missing handlers no-throw (§5.1 item 11). | R1,R2 |
| 11 | R1 | Medium | test_quality | teardown-coverage-incomplete-aft | Teardown coverage is incomplete after the `refs.media.teardown()` change. The plan does not include a JS-level regres... | ADDRESSED | R2: `teardownMedia(media, audioImpl, videoImpl)` extracted as pure helper in §4.4 (signalling.js UMD). Node test `signalling.test.js::teardown invokes detach + stop for both audio and video` (§5.3) with spy tracks and spy detach fns asserts: `detachRemoteAudio` + `detachRemoteVideo` each called once, `.stop()` called on every track of both streams. | R1,R2 |
| 12 | R1 | Medium | code_quality | tier-reason-handling-should | `tier_reason` handling should use the existing protocol-limit pattern. The plan leaves the 200-character cap as an im... | ADDRESSED | R2: `pub const MAX_TIER_REASON_LEN: usize = 200;` added to §4.11 (`protocol.rs`); §4.12 `truncate_to_chars` uses the constant; §5.2 oversized-reason test asserts against the constant (not a magic 200). | R1,R2 |
| 13 | R1 | Medium | test_quality | expected-behavior-unknown-tier | The expected behavior for an unknown `tier` string is not pinned. The test says "protocol error" without specifying c... | ADDRESSED | R2: §4.11 explicitly specifies WS close code 1003 (unsupported-data) via the existing Sprint 1 protocol-error pipeline. Test renamed to `test_lobby_join_with_unknown_tier_closes_with_1003` (§5.2) and asserts both the close code and that `lobby_state` was not emitted. | R1,R2 |
| 14 | R1 | Low | domain | tier-reason-truncation-test | The `tier_reason` truncation test fixture should include multi-byte input so it fails if byte-based truncation is use... | ADDRESSED | R2: §5.2 `test_lobby_join_with_oversized_tier_reason_is_truncated` now uses a 201-char input with at least one 3-byte codepoint ('中') placed so byte-based truncation would split inside the codepoint; asserts stored length is exactly 200 chars and the codepoint is intact. | R1,R2 |
| 15 | R1 | Low | domain | default-tier | The default `Tier::Supported` behavior needs an explicit source comment because it is a silent compatibility contract... | ADDRESSED | R2: Default flipped to `Tier::Degraded` and documented in §4.11 with a multi-line comment on `impl Default` explaining the conservative-default rationale (missing tier is a stale build, hand-crafted client, or tampered payload — warn the teacher, don't assume healthy). §9 decision #6 carries the rationale. | R1,R2 |
| 16 | R1 | Low | code_quality | setmute-setvideooff-toggle-calls | The `setMute` and `setVideoOff` toggle calls use inverted semantics that are correct by accident and hard to read. (F... | ADDRESSED | R2: §4.5 rewritten — click handlers now do `track.enabled = !track.enabled` directly (explicit toggle), then `paint(btn, track, onLabel, offLabel)` drives the view via `deriveToggleView` from the updated `track.enabled`. No more "inverted `muted`-vs-`enabled`" dance. | R1,R2 |
| 17 | R1 | Low | code_quality | hasvideotrack-input-guard-should | The `hasVideoTrack` input guard should mirror `audio.js::hasTrack` exactly to keep parallel helpers aligned. (File: `... | ADDRESSED | R2: §4.3 `hasVideoTrack` body is byte-for-byte parallel with Sprint 2's `audio.js::hasTrack` — same three guards (stream validity, id validity, per-track null tolerance). Comment added. Checklist step 4 calls out the parity requirement. | R1,R2 |
| 18 | R1 | Low | test_quality | plan-should-explicitly-state | The plan should explicitly state where `playsinline` assertions will live and should add a manual test for the `stude... | ADDRESSED | R2: §5.3 regression-guard row for Sprint 2 R1 #2 names `http_teach_debug_marker.rs::test_dev_teach_html_carries_debug_marker_student_view` and `..._teacher_view` as the location of the `playsinline` assertions (plus `#remote-video`, `#local-video` structure). Checklist step 19 enumerates the assertions. Checklist step 24 lists the manual unworkable-gate check (WebView not browser-automatable this sprint — screenshot in PR). | R1,R2 |
| 19 | R2 | Medium | test_quality | acquiremedia-has-no-direct | `acquireMedia` has no direct Node test for the all-success path. The plan only pins the partial-failure cleanup path.... | ADDRESSED | Post-R2: §5.3 row now calls out two Node tests — `acquireMedia success path returns {audio, video}` (success-path shape + no tracks stopped) and `acquireMedia partial failure stops audio stream when video acquisition throws`. §5.5 total updated to 8 signalling tests. | R1,R2 |
| 20 | R2 | Low | code_quality | browser | The `browser.js` module surface is inconsistent with later sections. §3.1 lists only `detectBrowser`, while §4.2 and ... | ADDRESSED | Post-R2: §3.1 module surface now declares `BROWSER_FLOORS` and `BROWSER_UA_FIXTURES` as explicit named exports alongside `detectBrowser`. | R1,R2 |
| 21 | R2 | Low | code_quality | risk-table-row-r6 | Risk table row R6 still cites the pre-flip test name. The correct guard is `test_lobby_join_without_tier_defaults_to_... | ADDRESSED | Post-R2: §6 R6 row updated to `test_lobby_join_without_tier_defaults_to_degraded` and references §4.11 / §9 #6 for the conservative-default rationale. | R1,R2 |
| 22 | R2 | Low | test_quality | ordercodecs-test-coverage-omits | `orderCodecs` test coverage omits `null` entries even though the implementation explicitly guards for them. Add a tes... | ADDRESSED | Post-R2: §5.1 item 9 gains test (f) pinning the null-preservation contract — null/undefined entries route to the non-preferred partition. `orderCodecs` test count now 6 (was 5). | R1,R2 |
| 23 | R2 | Low | test_quality | browser-detection-fixtures-omit | Browser detection fixtures omit Chrome on iOS. Add a `CriOS` fixture and an assertion that it resolves to `degraded` ... | ADDRESSED | Post-R2: §5.4 fixture list grows to 13 UAs, adding `CriOS` (Chrome on iOS) with explicit note that it must resolve to degraded via the iOS branch rather than its Chrome label. | R1,R2 |
| 24 | R2 | Low | domain | 4 | §4.12 uses mismatched prose and code for `truncate_to_chars`. The prose says `char_indices`, but the snippet uses `s.... | ADDRESSED | Post-R2: §4.12 prose rewritten — now says "count characters with `.chars().count()` for the guard and rebuild the String with `.chars().take(max_chars).collect()`", aligning with the snippet. | R1,R2 |
| 25 | R1 | High | test_quality | unworkable-feature-gate-path | The unworkable feature-gate path for `hasGetUserMedia === false` is not independently tested. A regression in that br... | ADDRESSED | R2: `browser.test.js` — new test `feature-absent env is unworkable (getUserMedia missing)` asserts that `{hasRTCPeerConnection:true, hasGetUserMedia:false}` produces `unworkable`. | R1,R2 |
| 26 | R1 | High | test_quality | android-webview-detection-via | Android WebView detection via the `; wv)` marker is untested. A common in-app browser class can bypass the intended u... | ADDRESSED | R2: Added `android_webview` UA fixture (Android `; wv)` marker). New test `Android WebView (; wv) marker is unworkable + isInAppWebView` asserts both flags. | R1,R2 |
| 27 | R1 | Medium | test_quality | websocket-integration-tests-only | The WebSocket integration tests only exercise `"tier":"degraded"`. They do not verify end-to-end handling for `"suppo... | ADDRESSED | R2: `ws_lobby_tier.rs` — new `test_lobby_join_supported_tier_round_trips` and `test_lobby_join_unworkable_tier_round_trips` verify both values reach the teacher unchanged. | R1,R2 |
| 28 | R1 | Medium | test_quality | served-html-tests-do | The served HTML tests do not assert `muted` on `#local-video`. Removing that attribute would cause immediate self-aud... | ADDRESSED | R2: `http_teach_debug_marker.rs` — both student and teacher tests now assert `body[local_idx..local_tag_end].contains("muted")` with an explicit "self-audio feedback risk" message. | R1,R2 |
| 29 | R1 | Medium | test_quality | teardownmedia-only-tested-with | `teardownMedia` is only tested with fully populated media state. Null or partial-init inputs are unverified. (File: `... | ADDRESSED | R2: `signalling.test.js` — three new tests for teardownMedia with only audio populated, only video populated, and empty-object media; assert detach is still called on both paths and track.stop is only called for present tracks. | R1,R2 |
| 30 | R1 | Medium | test_quality | acquiremedia-only-tested-video | `acquireMedia` is only tested for video-phase failure. Audio-phase failure propagation is unverified. (File: `web/ass... | ADDRESSED | R2: `signalling.test.js` — new test `acquireMedia audio-phase failure propagates and never calls video` asserts the error propagates and videoImpl is never invoked. | R1,R2 |
| 31 | R1 | Medium | domain | truncate-chars-does-unnecessary | `truncate_to_chars` does unnecessary work on oversized inputs. It performs two character scans and allocates a new `S... | ADDRESSED | R2: Rewritten to use a single `char_indices().nth(max_chars)` pass — the returned byte index is guaranteed char-aligned, so in-place `String::truncate(byte_idx)` is safe. No new allocation on the truncation path. | R1,R2 |
| 32 | R1 | Low | domain | tier-reason-lacks-field | `tier_reason` lacks the field-level byte cap applied to the other lobby string fields. Oversized values are silently ... | ADDRESSED | R2: New `MAX_TIER_REASON_BYTES = 4 × MAX_TIER_REASON_CHARS` constant. `ws/mod.rs::handle_lobby_join` now rejects with `FieldTooLong` when `tier_reason.len() > MAX_TIER_REASON_BYTES`, paralleling email/browser/device_class. New `test_lobby_join_oversized_tier_reason_rejected_at_byte_cap` pins the error path. | R1,R2 |
| 33 | R1 | Low | test_quality | truncate-chars-has-no | `truncate_to_chars` has no direct unit test that pins exact boundary behavior independently of WebSocket integration ... | ADDRESSED | R2: `server/src/ws/lobby.rs` — new `truncate_tests` module with 6 unit tests (ASCII under/at/over cap, multibyte boundary preservation, zero cap, empty input). Runs on every `cargo test`. | R1,R2 |
| 34 | R1 | Low | test_quality | android-tablet-classification-no | Android tablet classification is not covered by browser fixtures. (File: `web/assets/tests/browser.test.js`, Location... | ADDRESSED | R2: New `android_tablet` UA fixture + test `Android tablet UA is classified as tablet` asserts `device === 'tablet'`. | R1,R2 |
| 35 | R1 | Low | test_quality | session-control-button-tile | The in-session control button and tile container DOM is not pinned by server HTML tests. (File: `server/tests/http_te... | ADDRESSED | R2: `http_teach_debug_marker.rs` — both tests now assert `#mute`, `#video-off`, `#hangup`, and `class="tiles"` are present. | R1,R2 |
| 36 | R1 | Low | test_quality | block-notice-degraded-notice | `block-notice` and `degraded-notice` are not asserted in served student HTML despite being required by the landing-pa... | ADDRESSED | R2: `http_teach_debug_marker.rs` — student-view test now asserts `#block-notice` and `#degraded-notice` (teacher view intentionally does not carry these). Also fixed a latent test-infra bug where `get_html` re-used the `signup_teacher` cookie via reqwest's cookie jar — `get_html` now uses a fresh client per call so `cookie: None` genuinely means unauthenticated. | R1,R2 |
| 37 | R1 | Low | code_quality | admitoutcome | `AdmitOutcome::NoRoom` is dead code. The enum variant is never constructed because the function returns before the ou... | ADDRESSED | R2: `AdmitOutcome::NoRoom` variant and its unreachable match arm removed from `server/src/ws/lobby.rs`. | R1,R2 |
| 38 | R1 | Low | code_quality | max-tier-reason-len | `MAX_TIER_REASON_LEN` is a misleading name for a character-count limit. (File: `server/src/ws/protocol.rs`, Location:... | ADDRESSED | R2: Renamed `MAX_TIER_REASON_LEN` → `MAX_TIER_REASON_CHARS`; added complementary `MAX_TIER_REASON_BYTES` for the byte-level protocol cap. All callers + tests updated. | R1,R2 |
| 39 | R2 | Medium | code_quality | wirebidirectionalmedia-names-its | `wireBidirectionalMedia` names its parameter `tier` even though callers pass the full browser-detection object. This ... | ADDRESSED | Post-R2: `wireBidirectionalMedia(pc, detect)` renamed parameter to `detect`; both call sites in `connectTeacher` and `connectStudent` also renamed their local `tier` variable to `detect` to match. `lobby_join` still sends `tier: detect.tier` so the wire format is unchanged. | R2 |
| 40 | R2 | Low | code_quality | student | `student.js` captures `handle` in `onHangup` before the `const handle` binding is initialised. The current call order... | ADDRESSED | Post-R2: `student.js` changed `const handle = await …` to `let handle = null; handle = await …`; every closure (onHangup, beforeunload) now guards with `if (handle) handle.hangup()` so the safety does not depend on unstated call ordering. | R2 |
