# Findings Tracker: Sprint 16 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | nfs-design-cannot-be | The NFS design cannot be deployed on the existing storage account. Azure Files NFS requires `supportsHttpsTrafficOnly... | ADDRESSED | Plan updated: new NFS storage account required (separate from old SMB account); old account deleted after verification. `supportsHttpsTrafficOnly: false` noted as NFS requirement. | R1,R2,R3,R4,R6 |
| 2 | R1 | High | domain | plan-treats-container-app | The plan treats Container App Environment VNet integration as an in-place modification. It is a destructive replaceme... | ADDRESSED | Plan updated: CAE is a destructive replacement; 7-step cutover sequence with downtime window documented in plan and runbook. | R1,R2,R3,R4,R6 |
| 3 | R1 | High | domain | planned-container-apps-storage | The planned Container Apps storage change is not implementable at the current API version. `Microsoft.App/managedEnvi... | ADDRESSED | Plan updated: all Container Apps resources upgraded to `2024-03-01` API; `nfsAzureFile` property shape specified; `accountKey` removed. | R1,R2,R3,R4,R6 |
| 4 | R1 | High | security | security-model-grants-excessive | The security model grants excessive privilege to the mounted database share. `NoRootSquash` combined with a root-runn... | ADDRESSED | Plan updated: Dockerfile adds non-root `appuser` UID 1000; init container `chown`s `/data`; `RootSquash` on NFS share; `securityContext.runAsNonRoot=true` on server container. | R1,R2,R3,R4,R6 |
| 5 | R1 | High | test_quality | test-strategy-claims-existing | The test strategy claims existing coverage for `PRAGMA foreign_keys=ON`, but no such test exists. The plan must add a... | ADDRESSED | Plan updated: `db_pragma_foreign_keys_on` added as an explicit new test in `server/tests/db_pragmas.rs`. | R1,R2,R3,R4,R6 |
| 6 | R1 | High | test_quality | plan-dismisses-sqlite-ioerr | The plan dismisses `SQLITE_IOERR` handling as already covered, but no test verifies DB-error propagation to HTTP 500.... | ADDRESSED | Plan updated: `db_error_returns_500` added as a new integration test in `server/tests/db_error_500.rs`. | R1,R2,R3,R4,R6 |
| 7 | R1 | Medium | domain | plan-omits-required-operational | The plan omits a required operational constraint: WAL-on-NFS is only safe because the app remains single replica. The... | ADDRESSED | Plan updated: risk table entry added; `minReplicas=maxReplicas=1` documented as mandatory invariant in `db.rs` header, Bicep comment, and runbook. | R1,R2,R3,R4,R6 |
| 8 | R1 | Medium | security | runbook-path-incomplete-under | The runbook path is incomplete under the proposed network lockdown. Storage ACLs limited to the subnet will block ope... | ADDRESSED | Plan updated: runbook section defines temporary-IP-allow procedure (`az storage account network-rule add/remove`) with time-limited access. | R1,R2,R3,R4,R6 |
| 9 | R1 | Medium | security | plan-acknowledges-that-nfs | The plan acknowledges that NFS transport is not TLS-protected but does not record this as an accepted risk for a data... | ADDRESSED | Plan updated: "Accepted risk: NFS transport encryption" section added; risk documented in risk table and plan directs update to ADR-0001. | R1,R2,R3,R4,R6 |
| 10 | R1 | Medium | code_quality | header-file-convention-coverage | Header and file-convention coverage is incomplete. The new `infra/bicep/vnet.bicep` needs a structured header. Existi... | ADDRESSED | Plan updated: "File Header Updates" section lists all three files with required changes. | R1,R2,R3,R4,R6 |
| 11 | R1 | Medium | test_quality | concurrency-test-design-underspe | The concurrency test design is underspecified and does not guard against regressions to `max_connections(1)`. The pla... | ADDRESSED | Plan updated: `db_concurrent_writers` includes pseudo-code with scratch-table lifecycle, 4 concurrent tasks, and a 2-second deadline that fails when pool is capped at 1. | R1,R2,R3,R4,R6 |
| 12 | R1 | Medium | test_quality | plan-has-no-regression | The plan has no regression guard for retained SQLite pragmas beyond WAL. `busy_timeout=30000` and `synchronous=NORMAL... | ADDRESSED | Plan updated: `db_pragma_busy_timeout` and `db_pragma_synchronous` added to `db_pragmas.rs` test list. | R1,R2,R3,R4,R6 |
| 13 | R1 | Low | code_quality | rationale-max-connections-8 | The rationale for `max_connections(8)` is inaccurate. WAL supports one serialized writer with concurrent readers. It ... | ADDRESSED | Plan updated: `max_connections(4)` with corrected rationale — WAL enables concurrent readers with one serialised writer; pool increase reduces read-path serialisation. | R1,R2,R3,R4,R6 |
| 14 | R1 | Low | domain | plan-should-note-that | The plan should note that moving `SB_DATA_DIR` to `/data` also makes the dev blob store persistent, not just the SQLi... | ADDRESSED | Plan updated: "Side effect" noted in options analysis section; change log note prescribed. | R1,R2,R3,R4,R6 |
| 15 | R1 | Low | security | runbook-should-prescribe-encrypt | The runbook should prescribe encryption for downloaded backup artifacts. Raw `.db` files contain credentials and PII.... | ADDRESSED | Plan updated: runbook backup section prescribes `gpg --symmetric --cipher-algo AES256` and plaintext deletion. | R1,R2,R3,R4,R6 |
| 16 | R2 | Medium | test_quality | db-concurrent-writers-regression | The `db_concurrent_writers` regression test does not validate the intended pool-sizing behavior and will not fail on ... | ADDRESSED | Replaced with `db_pool_allows_concurrent_connections`: acquires 2 connections simultaneously with a 100ms timeout; fails structurally with max_connections(1). No timing dependency. | R1,R2,R3,R4,R6 |
| 17 | R2 | Medium | security | container-runtime-user-guidance | The container runtime user guidance is incompatible with the existing distroless image. The plan must use numeric `US... | ADDRESSED | Plan corrected: Dockerfile already uses USER 65532:65532 (distroless); plan updated throughout to use 65532:65532; no named-user dependency; init container chown and securityContext updated to match. | R1,R2,R3,R4,R6 |
| 18 | R2 | Medium | security | documented-backup-procedure-non | The documented backup procedure is non-functional against the current distroless image because the container has no s... | ADDRESSED | Backup replaced with a Container App Job using Alpine+sqlite3 image; VACUUM INTO produces consistent copy; az containerapp exec dependency removed. backup-job.bicep added to files changed. | R1,R2,R3,R4,R6 |
| 19 | R2 | Low | test_quality | concurrency-test-pseudo-code | The concurrency test pseudo-code contradicts the fixture guidance by using a fixed `/tmp` database path instead of a ... | ADDRESSED | Pseudo-code updated to use tempfile::TempDir; auto-cleaned on drop; manual cleanup language removed. | R1,R2,R3,R4,R6 |
| 20 | R2 | Low | test_quality | failure-path-test-does | The failure-path test does not specify which database failure mode is used to force a 500 response, leaving the test ... | ADDRESSED | db_error_returns_500 now specifies pool.close().await before the request; PoolClosed propagates as AppError::Internal → 500; deterministic and reproducible. | R1,R2,R3,R4,R6 |
| 21 | R2 | Low | code_quality | files-changed-summary-names | The files-changed summary names `server/Dockerfile (or root Dockerfile)` even though the repository only has the root... | ADDRESSED | Files changed table updated: entry is now `Dockerfile` (root); noted as no-change-needed with header update only. | R1,R2,R3,R4,R6 |
| 22 | R2 | Low | security | init-container-image-not | The init container image is not pinned by digest even though it runs as root with write access to the database volume... | ADDRESSED | Plan updated: init container image is `alpine:3.19@sha256:<digest>` with note that digest must be resolved and committed at implementation time. | R1,R2,R3,R4,R6 |
| 23 | R3 | Medium | security | backup-container-app-job | Backup Container App Job image is not pinned by digest, despite running with sensitive database and backup access. Th... | ADDRESSED | Plan updated: backup image is dedicated `singing-bridge-backup:latest` in ACR; base images pinned by digest at implementation time; explicitly noted in plan. | R1,R2,R3,R4,R6 |
| 24 | R3 | Medium | security | backup-job-command-requires | Backup job command requires root for `apk add`, but the plan also requires `RootSquash` on the NFS share. That combin... | ADDRESSED | Plan updated: replaced runtime apk add with dedicated Python 3.12-slim image; sqlite3 built-in; USER 65532:65532; no runtime root; compatible with RootSquash. | R1,R2,R3,R4,R6 |
| 25 | R3 | Medium | security | backup-job-authentication-azure | Backup job authentication for Azure Blob upload is unspecified. The plan does not define managed identity or secret-b... | ADDRESSED | Plan updated: system-assigned managed identity; Storage Blob Data Contributor role assignment in backup-job.bicep; DefaultAzureCredential; no shared key or SAS. | R1,R2,R3,R4,R6 |
| 26 | R3 | Low | test_quality | test-strategy-still-references | Test Strategy still references the old test name `db_concurrent_writers` in two places after the R2 rename. (File: `P... | ADDRESSED | Both stale references updated to `db_pool_allows_concurrent_connections`. | R1,R2,R3,R4,R6 |
| 27 | R3 | Low | domain | max-connections-rationale-still | The `max_connections` rationale still contains a stale reference to "8 write-only connections" after the pool-size ra... | ADDRESSED | Stale phrase removed; rationale corrected: WAL allows one serialised writer with concurrent readers; pool increase reduces read-path serialisation. | R1,R2,R3,R4,R6 |
| 28 | R3 | Low | code_quality | plan-adds-backup-job | The plan adds `backup-job.bicep` but does not specify the required file header template for that new Bicep file. (Fil... | ADDRESSED | File Header Updates table extended with headers for backup-job.bicep, backup-job/Dockerfile, and backup-job/backup.py. | R1,R2,R3,R4,R6 |
| 29 | R3 | Low | code_quality | server-src-db | The `server/src/db.rs` header update spec omits `synchronous=NORMAL` from the invariants list even though the plan sa... | ADDRESSED | `synchronous=NORMAL` added to db.rs header update spec in File Header Updates table. | R1,R2,R3,R4,R6 |
| 30 | R4 | Medium | domain | backup-job-storage-access | Backup job storage access is underspecified. The plan does not state that `backup-job.bicep` mounts the NFS share at ... | ADDRESSED | backup-job.bicep mounts sb-data NFS volume at /data; DB_PATH env var set to /data/singing-bridge.db; securityContext runAsUser=65532. | R1,R2,R4,R6 |
| 31 | R4 | Medium | security | backup-blob-destination-undefine | Backup blob destination is undefined and its security posture is not specified. The plan names managed-identity RBAC ... | ADDRESSED | backup-job.bicep provisions dedicated backup storage account with deny-by-default networkAcls, VNet rule for ACA subnet, bypass=AzureServices; role scoped to backups container (Storage Blob Data Contributor). | R1,R2,R4,R6 |
| 32 | R4 | Low | test_quality | test-strategy-omits-coverage | The test strategy omits coverage for `infra/backup-job/backup.py`. The new backup logic has offline-testable behavior... | ADDRESSED | infra/backup-job/test_backup.py added: 5 tests covering VACUUM INTO consistency, destination-exists error, upload contract, cleanup on success, and cleanup on upload failure. | R1,R2,R4,R6 |
| 33 | R4 | Low | code_quality | file-header-specs-infra | File header specs for `infra/backup-job/Dockerfile` and `infra/backup-job/backup.py` are incomplete. Required `Role` ... | ADDRESSED | Role field added to Dockerfile, backup.py, and test_backup.py. | R1,R2,R4,R6 |
| 34 | R1 | High | domain | backup-restore-cover-only | Backup and restore cover only the SQLite file and omit the persistent accompaniment blob store, so the durable datase... | ADDRESSED | backup.py header corrected: blobs live at /data/dev-blobs/ on the same NFS share. Accepted risk documented: WAVs re-synthesizable, PDFs re-uploadable; DB backup is the critical item. | R1,R2,R4,R6 |
| 35 | R1 | High | domain | nfs-bootstrap-incompatible-with | The NFS bootstrap is incompatible with `RootSquash`: the init container relies on `chown` as root, which Azure Files ... | ADDRESSED | Init container removed entirely. rootSquash changed to NoRootSquash; container already runs as UID 65532 (enforced by USER directive + securityContext.runAsNonRoot=true). | R1,R2,R4,R6 |
| 36 | R1 | High | security | live-sqlite-database-now | The live SQLite database now contains authentication-bearing state on Azure Files NFS with `supportsHttpsTrafficOnly:... | WONTFIX | NFS v4.1 + Kerberos is not available on Azure Files Premium. Data-at-rest is encrypted by Azure. Risk accepted and documented in knowledge/decisions/0001-mvp-architecture.md. | R1,R2,R4,R6 |
| 37 | R1 | High | security | root-capable-init-container | The root-capable init container runs from a mutable tag with no digest pin, creating a pre-start supply-chain executi... | ADDRESSED | Init container removed entirely — no init container in the deployed configuration. | R1,R2,R4,R6 |
| 38 | R1 | High | security | backup-job-also-runs | The backup job also runs from mutable, unpinned images while holding database read access and Blob write permissions.... | ADDRESSED | python:3.12-slim pinned by sha256 digest in Dockerfile (2026-04-23 pull). backupImageName default removed from backup-job.bicep — now a required parameter with no mutable-tag fallback. | R1,R2,R4,R6 |
| 39 | R1 | High | test_quality | backup-script-has-no | The backup script has no tests, despite being the only production recovery mechanism. The snapshot and upload contrac... | ADDRESSED | infra/backup-job/test_backup.py added with 5 tests; all pass. | R1,R2,R4,R6 |
| 40 | R1 | High | test_quality | init-pool-discards-result | `init_pool` discards the result of `PRAGMA journal_mode=WAL`, so WAL fallback is silent at startup instead of failing... | ADDRESSED | db.rs uses fetch_one to read the journal_mode pragma result; startup returns an error if mode is not "wal" (or "memory" for in-memory test DBs). | R1,R2,R4,R6 |
| 41 | R1 | Medium | security | backup-job-identity-scoped | The backup job identity is scoped to the entire storage account instead of the backup container. (File: `infra/bicep/... | ADDRESSED | roleAssignment scope changed to backupContainer resource; role is Storage Blob Data Contributor on the container, not the storage account. | R1,R2,R4,R6 |
| 42 | R1 | Medium | security | backup-storage-account-has | The backup storage account has no network restriction such as disabled public network access or deny-by-default ACLs.... | ADDRESSED | networkAcls.defaultAction=Deny with VNet rule allowing ACA subnet; bypass=AzureServices for ARM/portal access. | R1,R2,R4,R6 |
| 43 | R1 | Medium | code_quality | backup | `backup.py` does not guard the SQLite connection with a context manager, so a failed `VACUUM INTO` leaks the connecti... | ADDRESSED | sqlite3.connect wrapped in `with` statement; connection closed on exception. | R1,R2,R4,R6 |
| 44 | R1 | Medium | code_quality | gallery | `gallery.html` reintroduces Google Fonts and breaks the project's CSP-compliant self-hosted font approach. (File: `we... | ADDRESSED | Removed 3 Google Fonts link tags (preconnect + stylesheet) from gallery.html. | R1,R2,R4,R6 |
| 45 | R1 | Medium | test_quality | database-tests-do-not | Database tests do not verify session-scoped pragmas across every pool connection, and they do not cover the read-path... | ADDRESSED | db_pragmas_apply_to_second_connection: acquires 2 simultaneous connections and reads foreign_keys/busy_timeout/synchronous on the second to prove after_connect runs for lazily-created connections. db_error_500.rs covers POST /auth/register with pool closed → 500. | R1,R2,R4,R6 |
| 46 | R2 | High | security | backup-image-references-still | Backup image references are still mutable and unpinned. This leaves the backup path exposed to supply-chain drift and... | ADDRESSED | python:3.12-slim pinned by sha256 digest in Dockerfile. backupImageName made a required parameter in backup-job.bicep (no default). | R2,R4,R6 |
| 47 | R2 | High | domain | backup-job-documentation-still | The backup job documentation still claims accompaniment blobs are outside the NFS failure domain, but production stor... | ADDRESSED | backup.py header corrected: blobs at /data/dev-blobs/ are on the same NFS share; shared-failure-domain risk accepted and documented (WAVs re-synthesizable, PDFs re-uploadable). | R2,R4,R6 |
| 48 | R2 | Medium | security | move-norootsquash-not-enforced | The move to `NoRootSquash` is not enforced as a security invariant. The implementation now depends on all containers ... | ADDRESSED | Invariant documented in container-app.bicep file header: every container mounting sb-data MUST run as UID 65532:65532 with runAsNonRoot=true. Dockerfile USER comment updated to reference NoRootSquash requirement. | R2,R4,R6 |
| 49 | R2 | Medium | test_quality | database-pragma-tests-still | Database pragma tests still validate only one pooled connection. They do not prove `after_connect` settings are appli... | ADDRESSED | db_pragmas_apply_to_second_connection test added: acquires 2 simultaneous connections with 100ms timeout; reads foreign_keys, busy_timeout, synchronous on the second connection. | R2,R4,R6 |
| 50 | R2 | Low | test_quality | backup | `backup.py` can mask the original `VACUUM INTO` failure by unconditionally unlinking a temp path that may not exist. ... | ADDRESSED | finally block guards os.unlink with os.path.exists; test_vacuum_into_fails_if_destination_exists added to verify error propagates. | R2,R4,R6 |
| 51 | R2 | Low | domain | backup-blob-names-use | Backup blob names use second-level timestamp precision. Two runs in the same UTC second can collide and fail the seco... | ADDRESSED | Timestamp format changed to %Y%m%d-%H%M%S-%f (includes microseconds). | R2,R4,R6 |
| 52 | R2 | Low | code_quality | backup-job-file-headers | Backup-job file headers remain incomplete, and `test_backup.py` also omits the required `Role` field. (File: `infra/b... | ADDRESSED | Role field added to Dockerfile, backup.py, and test_backup.py. | R2,R4,R6 |
| 53 | R2 | Low | code_quality | make-source-db-leaks | `make_source_db()` leaks temporary directories across test runs. (File: `infra/backup-job/test_backup.py`, Location: ... | ADDRESSED | make_source_db() replaced with a pytest fixture using tmp_path; pytest manages cleanup automatically. | R2,R4,R6 |
| 54 | R2 | Low | domain | tracker-plan-state-stale | Tracker and plan state are stale in several places. Implemented items remain marked open, and accepted-risk items are... | ADDRESSED | All findings updated with current status and resolution notes in this update. | R2,R4,R6 |
| 55 | R4 | Medium | security | backup-storage-account-still | The backup storage account still allows shared-key authentication, which undermines the managed-identity-only access ... | OPEN |  | R4,R6 |
| 56 | R4 | Medium | domain | plan-still-documents-rootsquash | The plan still documents `rootSquash: 'RootSquash'` while the implementation correctly uses `NoRootSquash`. This leav... | OPEN |  | R4,R6 |
| 57 | R4 | Low | security | backup-storage-account-network | The backup storage account network ACL uses `bypass: 'AzureServices'` with an incorrect rationale. Narrow this to `by... | OPEN |  | R4,R6 |
| 58 | R4 | Low | security | sidecar-container-has-no | The sidecar container has no declared `securityContext`. Declare non-root execution explicitly. (File: `infra/bicep/c... | OPEN |  | R4,R6 |
| 59 | R4 | Low | test_quality | second-connection-pragma-test | The second-connection pragma test does not assert `journal_mode`, so it does not explicitly document WAL as an invari... | OPEN |  | R4,R6 |
| 60 | R4 | Low | test_quality | backup-tests-do-not | The backup tests do not cover the `run_backup` path where `VACUUM INTO` fails, including exception propagation and te... | OPEN |  | R4,R6 |
| 61 | R4 | Low | domain | findings-tracker-stale | The findings tracker is stale. Several implemented fixes still remain marked `OPEN`, and accepted-risk items are not ... | OPEN |  | R4,R6 |
| 62 | R4 | Low | domain | plan-still-omits-implemented | The plan still omits the implemented backup job mount path and `DB_PATH` wiring. (File: `PLAN_Sprint16.md`, Location:... | OPEN |  | R4,R6 |
| 63 | R4 | Low | code_quality | test-comment-hardcodes-max | A test comment hardcodes `max_connections=4`, which will drift if the pool cap changes. (File: `server/tests/common/m... | OPEN |  | R4,R6 |
| 64 | R6 | Low | security | toctou-race-exists-between | TOCTOU race exists between `os.unlink` and `VACUUM INTO` when creating the temporary backup file. Use a private temp ... | OPEN |  | R6 |
| 65 | R6 | Low | code_quality | bare-open-used-without | Bare `open()` is used without a context manager in test setup. Use `Path.touch()` or a `with open(...)` block. (File:... | OPEN |  | R6 |
| 66 | R6 | Low | code_quality | multi-line-comment-blocks | Multi-line comment blocks in new code violate the repository comment convention. Collapse them to single-line comment... | OPEN |  | R6 |
| 67 | R6 | Low | code_quality | capturing-mkstemp-helper-duplica | The `capturing_mkstemp` helper is duplicated across two backup tests. Extract a shared fixture or helper. (File: `inf... | OPEN |  | R6 |
