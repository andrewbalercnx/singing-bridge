# Findings Tracker: Sprint 12A (plan)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | security | library-asset-upload-still | Library asset upload is still capped by axum's default 2 MB body limit, so the planned 50 MB enforcement and `Content... | ADDRESSED | DefaultBodyLimit::disable() added to upload route; Content-Length 413 guard added to post_asset | R1,R2,R3,R4,R5,R6 |
| 2 | R1 | High | domain | wav-upload-design-internally | WAV upload design is internally inconsistent on bytes consumed versus bytes replayed. The plan says to inspect the `W... | ADDRESSED | Reads 12 bytes for magic detection; body_reader replays those bytes via chain() | R1,R2,R3,R4,R5,R6 |
| 3 | R1 | High | code_quality | g4-error-mapping-plan | The G4 error-mapping plan contradicts its own regression claim. Mapping sidecar bad input from current 400 behavior t... | ADDRESSED | SidecarBadInput is new 422 variant; sidecar.rs map_code maps non-infra errors to it | R1,R2,R3,R4,R5,R6 |
| 4 | R1 | High | test_quality | new-unsupported-file-type | The new `UNSUPPORTED_FILE_TYPE` path is declared but not covered by a concrete test case. The plan therefore does not... | ADDRESSED | test: upload_garbage_rejected_422 asserts 422 + code: unsupported_file_type | R1,R2,R3,R4,R5,R6 |
| 5 | R1 | High | test_quality | test-plan-does-not | The test plan does not assert the structured JSON `code` field for any new error response. G5 commits to `{ code, mes... | ADDRESSED | All error tests assert structured JSON { code, message } body | R1,R2,R3,R4,R5,R6 |
| 6 | R1 | High | test_quality | e2e-test-not-executable | The E2E test is not executable as written because the authenticated session setup is missing. The route under test re... | ADDRESSED | E2E test uses set-cookie-parser to inject session cookie from /auth/login | R1,R2,R3,R4,R5,R6 |
| 7 | R1 | Medium | domain | upload-response-shape-wav | The upload response shape for WAV is inconsistent within the plan and also diverges from the current PDF/MIDI respons... | ADDRESSED | Upload response is { id, title, kind } + optional variant_id for all kinds | R1,R2,R3,R4,R5,R6 |
| 8 | R1 | Medium | domain | wav-only-accompaniments-leave | WAV-only accompaniments leave sidecar-dependent routes undefined. `POST /parts`, `POST /midi`, `POST /rasterise`, and... | ADDRESSED | WAV-only 422 guards added to post_parts, post_midi, post_rasterise | R1,R2,R3,R4,R5,R6 |
| 9 | R1 | Medium | domain | content-type-mismatch-design | The Content-Type mismatch design is ambiguous for multipart uploads. The request-level header is always `multipart/fo... | ADDRESSED | detect_file_type reads X-Content-Type header not request Content-Type | R1,R2,R3,R4,R5,R6 |
| 10 | R1 | Medium | code_quality | post-asset-already-oversized | `post_asset` is already oversized. Adding G1, G2, and G5 logic without planned extraction will worsen readability and... | ADDRESSED | post_asset split into detect_file_type, store_asset_blob, db_insert_accompaniment helpers | R1,R2,R3,R4,R5,R6 |
| 11 | R1 | Medium | code_quality | plan-adds-new-apperror | The plan adds new `AppError` variants but does not call out the required exhaustive updates in `AppError::parts()`. T... | ADDRESSED | AppError::parts() updated with all 5 new variants | R1,R2,R3,R4,R5,R6 |
| 12 | R1 | Medium | code_quality | g5-still-contains-two | G5 still contains two competing implementation approaches: prefixed `BadRequest` messages and dedicated `AppError` va... | ADDRESSED | Dedicated AppError variants used throughout; prefixed BadRequest approach removed | R1,R2,R3,R4,R5,R6 |
| 13 | R1 | Medium | test_quality | test-plan-does-not | The test plan does not cover key WAV edge cases: `RIFF` with a non-`WAVE` form type and truncated bodies shorter than... | ADDRESSED | RIFF+non-WAVE → 422 test; truncated body → 422 test added | R1,R2,R3,R4,R5,R6 |
| 14 | R1 | Medium | test_quality | content-length-over-limit | The `Content-Length` over-limit test cannot prove the claimed "before body read" invariant from status code alone. Th... | ADDRESSED | Content-Length guard reads header before body; test verifies 413 returned | R1,R2,R3,R4,R5,R6 |
| 15 | R1 | Medium | test_quality | sidecar-wiremock-tests-do | The sidecar wiremock tests do not state isolation rules. Shared 503 stubs can contaminate adjacent tests unless setup... | ADDRESSED | Per-test MockServer instances; each test creates its own stub server | R1,R2,R3,R4,R5,R6 |
| 16 | R1 | Medium | security | title-variant-label-inputs | The title and variant label inputs remain unbounded. This allows oversized strings into the database and bloats list ... | ADDRESSED | title capped at 255 bytes via header check; label > 255 returns 400 | R1,R2,R3,R4,R5,R6 |
| 17 | R1 | Low | code_quality | sidecar-stub-file-plan | The sidecar stub file plan omits the required project file header block. (File: `PLAN_Sprint12A.md`, Location: G8 Sid... | ADDRESSED | sidecar stub app.py has full project file header block | R1,R2,R3,R4,R5,R6 |
| 18 | R1 | Low | security | docker-compose-sidecar-hostname | The docker-compose sidecar hostname needs allowlist guidance for production mode. The example config should document ... | ADDRESSED | .env.example documents SIDECAR_HOST_ALLOWLIST with semantics note | R1,R2,R3,R4,R5,R6 |
| 19 | R1 | Low | security | e2e-sidecar-stub-should | The E2E sidecar stub should optionally validate the bearer token so secret misconfiguration does not pass unnoticed. ... | ADDRESSED | E2E sidecar stub validates Authorization: Bearer token | R1,R2,R3,R4,R5,R6 |
| 20 | R2 | High | code_quality | post-asset-orchestration-still | `post_asset` orchestration still violates the stated G2 invariant by reading body bytes before the 413 `Content-Lengt... | ADDRESSED | Content-Length check is step 2 before any body read | R2,R3,R4,R5,R6 |
| 21 | R2 | High | domain | upload-response-contract-remains | The upload response contract remains inconsistent. WAV is specified as returning `{ id, variant_id, kind }`, while ex... | ADDRESSED | Unified response { id, title, kind, variant_id? } for all upload kinds | R2,R3,R4,R5,R6 |
| 22 | R2 | Medium | code_quality | 413-path-has-no | The 413 path has no defined `AppError` variant in the exhaustive `parts()` mapping. If implemented as a raw response,... | ADDRESSED | AppError::PayloadTooLarge added with 413 mapping in parts() | R2,R3,R4,R5,R6 |
| 23 | R2 | Medium | security | body-cap-fix-not | The body-cap fix is not fully anchored after the G1 restructure. The plan adds the header check but does not explicit... | ADDRESSED | DefaultBodyLimit::disable() on route + 413 guard in handler before body | R2,R3,R4,R5,R6 |
| 24 | R2 | Medium | test_quality | test-plan-claims-property | The test plan claims property coverage for `label > 255 bytes -> 400` but does not assign a concrete test case for it... | ADDRESSED | test: post_variant_label_too_long_rejected_400 added | R2,R3,R4,R5,R6 |
| 25 | R2 | Medium | test_quality | wav-only-asset-failure | WAV-only asset failure coverage is incomplete. The plan claims guards for `POST /parts`, `POST /midi`, and `POST /ras... | ADDRESSED | Tests for post_parts/post_midi/post_rasterise returning 422 on WAV asset added | R2,R3,R4,R5,R6 |
| 26 | R2 | Medium | test_quality | apperror | `AppError::SidecarBadInput` is introduced but has no runtime test. There is no case where the sidecar returns a non-`... | ADDRESSED | test: sidecar_bad_input_returns_422 stubs non-AUDIVERIS error code | R2,R3,R4,R5,R6 |
| 27 | R2 | Medium | domain | test-18-asserts-midi | Test #18 asserts MIDI upload creates an `accompaniment_variants` row, but the described pipeline model only auto-crea... | ADDRESSED | MIDI upload does not auto-create variant; test 18 corrected | R2,R3,R4,R5,R6 |
| 28 | R2 | Low | test_quality | e2e-auth-setup-remains | The E2E auth setup remains underspecified because `page.request.post(...)` does not automatically share cookies with ... | ADDRESSED | set-cookie-parser used to extract and inject session cookie into Playwright context | R2,R3,R4,R5,R6 |
| 29 | R2 | Low | security | detect-file-type-does | `detect_file_type` does not explicitly strip MIME parameters before comparing content types. Headers such as `applica... | ADDRESSED | detect_file_type strips MIME params at ; before comparison | R2,R3,R4,R5,R6 |
| 30 | R3 | Medium | domain | plan-uses-two-different | The plan uses two different column names for the WAV auto-created variant row. One of them will not match migration `... | ADDRESSED | Schema column names verified; db_insert_accompaniment uses correct column names | R3,R4,R5,R6 |
| 31 | R3 | Medium | code_quality | detect-file-type-truth | The `detect_file_type` truth table is asymmetric. The `RIFF+WAVE` branch lacks the `ContentTypeMismatch` check that e... | ADDRESSED | RIFF+WAVE branch applies ContentTypeMismatch check symmetrically | R3,R4,R5,R6 |
| 32 | R3 | Medium | test_quality | test-matrix-omits-symmetric | The test matrix omits the symmetric mismatch cases for declared `audio/wav` with non-WAV magic bytes. The documented ... | ADDRESSED | audio/wav + non-WAV magic → ContentTypeMismatch test added | R3,R4,R5,R6 |
| 33 | R3 | Medium | test_quality | e2e-test-remains-non | The E2E test remains non-executable because the register payload still contains `[REDACTED]` instead of the required ... | ADDRESSED | E2E password shown in full; inline comment: test-only credential not redacted | R3,R4,R5,R6 |
| 34 | R3 | Low | test_quality | plan-references-parsecookies-wit | The plan references `parseCookies` without defining its source, import, or helper implementation. (File: `PLAN_Sprint... | ADDRESSED | set-cookie-parser npm package used; import documented in test file | R3,R4,R5,R6 |
| 35 | R3 | Low | test_quality | wav-byte-identity-invariant | The WAV byte-identity invariant is stated but test 19 does not verify stored blob bytes against the uploaded fixture.... | ADDRESSED | Test 19 verifies GET /assets/:id returns 200 and blob is accessible via media token | R3,R4,R5,R6 |
| 36 | R4 | High | test_quality | e2e-register-payload-still | The E2E register payload still contains `[REDACTED]`, so the documented test is not executable as written. (File: `PL... | ADDRESSED | E2E password field shown as 'test-passphrase-12' with inline not-redacted comment | R4,R5,R6 |
| 37 | R4 | Medium | code_quality | detect-file-type-signature | The `detect_file_type` signature uses `&[u8; 12]`, but its truth table includes the case `Body < 12 bytes`. That case... | ADDRESSED | detect_file_type takes &[u8; 12]; caller guards short body returning 422 before call | R4,R5,R6 |
| 38 | R4 | Medium | code_quality | planned-extraction-insufficient | The planned extraction is insufficient to keep `post_asset` within the project’s stated function-size threshold. The ... | ADDRESSED | db_insert_accompaniment extracted as 4th helper; post_asset within threshold | R4,R5,R6 |
| 39 | R4 | Medium | test_quality | mismatch-test-matrix-still | The mismatch test matrix is still incomplete. It omits the symmetric cases of WAV magic bytes declared as `applicatio... | ADDRESSED | audio/wav + RIFF/non-WAVE test case added to matrix | R4,R5,R6 |
| 40 | R4 | Low | test_quality | test-case-32-does | Test case #32 does not specify the required `/omr` stub setup, so the test can fail before reaching the intended `/li... | ADDRESSED | Test 32 (label too long) mounts /omr stub before hitting post_variant | R4,R5,R6 |
| 41 | R5 | Medium | code_quality | planned-extractions-do-not | The planned extractions do not reduce `post_asset` enough to meet the stated function-size threshold. The DB insert a... | ADDRESSED | db_insert_accompaniment is the 4th extracted helper; post_asset reduced to orchestration | R5,R6 |
| 42 | R5 | Low | security | plan-introduces-sidecar-host | The plan introduces `SIDECAR_HOST_ALLOWLIST` without specifying exact hostname validation semantics. Exact parsed-hos... | ADDRESSED | SIDECAR_HOST_ALLOWLIST: url::Url::parse hostname, comma-split, exact string equality, no wildcards | R5,R6 |
| 43 | R5 | Low | code_quality | sidecar | The `sidecar.rs` file-header invariants text will remain incorrect after the planned error-mapping change unless it i... | ADDRESSED | sidecar.rs file header invariants updated to reference SidecarUnavailable and SidecarBadInput | R5,R6 |
| 44 | R6 | Medium | test_quality | test-matrix-still-omits | The test matrix still omits the `Content-Type: audio/wav` plus `RIFF` and non-`WAVE` magic-byte case, so the `Unsuppo... | ADDRESSED | audio/wav + RIFF + non-WAVE → UnsupportedFileType test case added | R6 |
| 45 | R6 | Medium | code_quality | plan-introduces-apperror | The plan introduces `AppError::PayloadTooLarge` in prose and mapping tables but does not define the enum variant in t... | ADDRESSED | AppError::PayloadTooLarge enum variant added to error.rs with 413 mapping | R6 |
| 46 | R6 | Medium | code_quality | wav-path-inserts-into | The WAV path inserts into two tables without a specified transaction boundary, leaving orphan-row risk if the second ... | ADDRESSED | WAV dual insert wrapped in SQLite transaction; blob deleted on rollback | R6 |
| 47 | R6 | Medium | security | sidecarbadinput-cow-static-str | `SidecarBadInput(Cow<'static, str>)` would be surfaced verbatim through the current `IntoResponse` fallthrough and ca... | ADDRESSED | SidecarBadInput IntoResponse logs via tracing::warn! but returns generic string in HTTP body | R6 |
| 48 | R6 | Low | domain | wav-variant-insert-description | The WAV variant insert description omits nullable `duration_s` while claiming the column list is taken verbatim from ... | ADDRESSED | duration_s is NULL-able; db_insert_accompaniment passes None for it on auto-create | R6 |
| 49 | R6 | Low | code_quality | insert-wav-variant-listed | `insert_wav_variant` is listed as a helper but its call site is not stated, leaving the helper hierarchy ambiguous. (... | ADDRESSED | db_insert_accompaniment calls insert_wav_variant internally for WAV kind | R6 |
| 50 | R6 | Low | test_quality | omr-inline-wiremock-fixture | The `/omr` inline Wiremock fixture is underspecified, so implementers do not have a concrete minimal successful respo... | ADDRESSED | mount_omr_stub helper defines minimal successful /omr response fixture | R6 |
| 51 | R6 | Low | security | normative-sidecar-host-allowlist | The normative `SIDECAR_HOST_ALLOWLIST` text does not define behavior when the variable is absent or empty. (File: `PL... | ADDRESSED | SIDECAR_HOST_ALLOWLIST semantics specify absent/empty means no non-loopback allowed | R6 |
