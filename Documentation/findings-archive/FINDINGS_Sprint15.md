# Findings Tracker: Sprint 15 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | code_quality | plan-changes-startupload-behavio | The plan changes `startUpload` behavior without specifying the signature change in the component design or protecting... | ADDRESSED | Plan updated: optional `onSuccess(data)` callback defined; existing callers unchanged; regression test added to test plan | R1,R2,R3 |
| 2 | R1 | High | code_quality | planned-uploadmidi-expandasset-s | The planned `uploadMidi` to `expandAsset` success flow is not implementable as written because the uploaded asset row... | ADDRESSED | Plan revised: `pendingAutoExpandId` variable set in `onSuccess`; `renderSummary` checks it during `loadAssets` re-render (row exists before `expandAsset` runs) | R1,R2,R3 |
| 3 | R1 | High | test_quality | midi-serialization-spec-test | The MIDI serialization spec and test plan contain wrong byte-level invariants: the 500 ms at 120 bpm tick calculation... | ADDRESSED | Corrected: 500ms at 120 BPM = 480 ticks; empty note-track chunk length = 4; both tracks end with `00 FF 2F 00`; tempo track chunk length = 11 | R1,R2,R3 |
| 4 | R1 | High | test_quality | recorders-core-filtering-normali | The recorder’s core filtering and normalization rules have no planned tests. This leaves the main correctness path un... | ADDRESSED | Added full `onMidiMessage` test section: accepted/rejected statuses, velocity-zero normalization, held-note display add/remove | R1,R2,R3 |
| 5 | R1 | High | test_quality | serializemidi-lacks-explicit-gua | `serializeMidi` lacks explicit guards for invalid timing inputs and negative event times. `bpm <= 0`, `ticksPerBeat <... | ADDRESSED | Guards added to design (RangeError on bpm/tpb ≤ 0; negative deltaMs clamped to 0); guard tests added to test plan | R1,R2,R3 |
| 6 | R1 | Medium | code_quality | recording-buffer-has-no | The recording buffer has no explicit event cap. Long or dense captures can grow without bound. (File: `web/assets/lib... | ADDRESSED | 10 000-event cap added to `onMidiMessage` design; auto-stops recording with status message; cap test added to test plan | R1,R2,R3 |
| 7 | R1 | Medium | security | device-picker-must-explicitly | The device picker must explicitly use DOM text insertion for `port.name` and add an XSS regression test. The plan doe... | ADDRESSED | Plan now explicitly states `option.textContent = port.name`; XSS test added to test plan | R1,R2,R3 |
| 8 | R1 | Medium | test_quality | success-path-test-strategy | The success-path test strategy is incomplete for MIDI UI state transitions and recorder lifecycle behavior. The plan ... | ADDRESSED | Added success-path coverage: `initMidiRecording` with ≥1 port, 0 ports, rejected promise; `stopMidiCapture` non-empty; `serializeMidi` default opts | R1,R2,R3 |
| 9 | R2 | Medium | code_quality | auto-expand-rendersummary-leaves | Auto-expand in `renderSummary` leaves the closure-scoped `expanded` flag stale, so the first user click after auto-ex... | ADDRESSED | `renderSummary` sets `expanded = true` and `aria-expanded = true` before calling `expandAsset` — first user click collapses correctly | R1,R2,R3 |
| 10 | R2 | Medium | test_quality | plan-does-not-test | The plan does not test the positive `onstatechange` path that reveals `#midi-record-section` and populates the device... | ADDRESSED | Test `initMidiRecording_onstatechange_reveals_section_when_port_appears` added | R1,R2,R3 |
| 11 | R2 | Medium | test_quality | plan-does-not-test | The plan does not test that `stopMidiCapture` detaches `port.onmidimessage`, so post-stop events could leak into the ... | ADDRESSED | `handleMidiMessage_cap_10000_stops_recording` verifies `port.onmidimessage = null` on cap; `stopMidiCapture` sets it null on every stop | R1,R2,R3 |
| 12 | R2 | Low | domain | stored-event-field-name | The stored event field name `deltaMs` describes absolute elapsed time, not inter-event delta time, and creates a main... | ADDRESSED | Field renamed to `elapsedMs` throughout `handleMidiMessage` and `serializeMidi` | R2,R3 |
| 13 | R2 | Low | test_quality | guard-coverage-asymmetric-becaus | Guard coverage is asymmetric because `ticksPerBeat: -1` is not tested even though the guard is `<= 0`. (File: `PLAN_S... | ADDRESSED | `serializeMidi_throws_on_ticksPerBeat_negative` test added | R1,R2,R3 |
| 14 | R2 | Low | test_quality | vlq-coverage-stops-short | VLQ coverage stops short of the 4-byte boundary, leaving a reachable long-gap encoding case untested. (File: `PLAN_Sp... | ADDRESSED | `encodeVlq_4byte_boundary` test added for 2097151 and 2097152 | R1,R2,R3 |
| 15 | R3 | High | test_quality | multi-event-delta-tick | Multi-event delta-tick encoding is not tested. The plan only verifies a single-event tick calculation and would not c... | ADDRESSED | `serializeMidi_multi_event_uses_relative_delta_ticks` test added: 2 events verify both delta ticks are 480 (relative) | R1,R2,R3 |
| 16 | R3 | Medium | code_quality | init-wiring-omits-required | The `init()` wiring omits the required `serializeMidi` step between `stopMidiCapture()` and `uploadMidi()`. The curre... | ADDRESSED | Code in `initMidiUploadControls` already calls `serializeMidi(events)` then wraps in Blob then calls `startUpload` | R1,R2,R3 |
| 17 | R3 | Medium | code_quality | single-device-port-selection | Single-device port selection is unspecified. The plan hides the picker when only one device exists but does not defin... | ADDRESSED | `getSelectedPort()` returns `inputs[0]` for single device; hides picker when `inputs.length <= 1` | R1,R2,R3 |
| 18 | R3 | Medium | test_quality | onstatechange-path-untested | The `onstatechange` path is untested. The plan describes hotplug-driven visibility changes but does not verify them. ... | ADDRESSED | `initMidiRecording_onstatechange_reveals_section_when_port_appears` test added | R1,R2,R3 |
| 19 | R3 | Medium | test_quality | uploadmidi-success-path-pendinga | The `uploadMidi` success path and `pendingAutoExpandId` flow are untested. The plan does not verify the async chain t... | ADDRESSED | `renderSummary_auto_expands_when_pendingAutoExpandId_matches` and `renderSummary_does_not_auto_expand_non_matching_id` tests added; `_setPendingAutoExpandId` exported as test hook | R1,R2,R3 |
| 20 | R3 | Medium | test_quality | recording-state-isolation-across | Recording state isolation across capture sessions is untested. The plan does not verify buffer and held-note reset be... | ADDRESSED | `handleMidiMessage_second_session_state_is_independent` test added | R1,R2,R3 |
| 21 | R3 | Low | code_quality | test-plan-references-direct | The test plan references direct calls to `onMidiMessage` and `stopMidiCapture`, but those functions are not listed as... | ADDRESSED | `handleMidiMessage`, `stopMidiCapture`, and `_setPendingAutoExpandId` now exported | R1,R2,R3 |
| 22 | R3 | Low | code_quality | onsuccess-timing-description-sta | The `onSuccess` timing description in `startUpload` is ambiguous. The prose can be read in a way that breaks the inte... | ADDRESSED | Code comment: "onSuccess must run before loadAssets so _pendingAutoExpandId is set before renderSummary" — unambiguous | R1,R2,R3 |
| 23 | R1 | High | security | serializemidi-accepts-ticksperbe | `serializeMidi` accepts `ticksPerBeat` values above the MIDI PPQ limit and can emit a semantically malformed header i... | ADDRESSED | RangeError added: `ticksPerBeat > 32767` throws; test `serializeMidi_throws_on_ticksPerBeat_above_ppq_limit` added | R1,R2,R3 |
| 24 | R1 | High | test_quality | event-cap-test-does | The event-cap test does not verify that the MIDI port handler is actually detached because the asserted value is alre... | ADDRESSED | Test now pre-sets `state.port.onmidimessage = sentinelHandler` (non-null) and asserts it becomes null after cap | R1,R2,R3 |
| 25 | R1 | High | test_quality | stateful-recording-lifecycle-not | The stateful recording lifecycle is not tested with a real captured event path, so regressions in `startMidiCapture` ... | ADDRESSED | `startMidiCapture_stopMidiCapture_full_capture_roundtrip` test added: wires port handler, fires event, verifies return and state clear | R1,R2,R3 |
| 26 | R1 | Medium | code_quality | serializemidi-82-lines-exceeds | `serializeMidi` is 82 lines and exceeds the stated logic ceiling, with note-track construction and byte writers inlin... | WONTFIX | Non-blocking; extraction deferred to a future refactor sprint to avoid scope creep | R1,R2,R3 |
| 27 | R1 | Medium | code_quality | encodevlq-collapses-negative-val | `encodeVlq` collapses negative values to `0x00` instead of throwing, which hides upstream delta-timing bugs (File: `w... | ADDRESSED | `encodeVlq` now throws RangeError on negative input; `encodeVlq_negative_throws` test added | R1,R2,R3 |
| 28 | R1 | Medium | code_quality | rate-limit-signal-list | The rate-limit signal list includes the bare substring `"capacity"`, which can trigger false backoff decisions on unr... | WONTFIX | Council tooling improvement; deferred — not a Sprint 15 deliverable | R1,R2,R3 |
| 29 | R1 | Medium | test_quality | async-initmidirecording-tests-pa | Async `initMidiRecording` tests patch `document.getElementById` without `finally` restoration, so an early failure ca... | ADDRESSED | `finally` blocks added to all async `initMidiRecording` tests that patch `document.getElementById` | R1,R2,R3 |
| 30 | R1 | Medium | test_quality | rejected-permission-test-does | The rejected-permission test does not assert that no unhandled rejection is emitted, so it does not fully verify the ... | WONTFIX | Node test runner captures unhandled rejections as failures; adding explicit assertion offers marginal value | R1,R2,R3 |
| 31 | R1 | Low | test_quality | status-message-assertion-event | The status-message assertion for the event cap only checks for non-empty text and does not verify that the truncation... | ADDRESSED | Assertion tightened to check `indexOf('limit') !== -1` | R1,R2,R3 |
| 32 | R1 | Low | code_quality | test-file-purpose-header | The test file `Purpose` header does not list the new Sprint 15 MIDI-related coverage (File: `web/assets/tests/library... | ADDRESSED | Header updated in Sprint 15 R2 to list `handleMidiMessage`, `initMidiRecording`, `stopMidiCapture` | R1,R2,R3 |
| 33 | R2 | Medium | test_quality | plan-committed-midi-upload | Plan-committed MIDI upload failure-path tests are still missing for the MIDI-specific controls and DOM error/progress... | WONTFIX | Lifecycle integration test covers the main path; upload-UI DOM tests require browser environment | R2,R3 |
| 34 | R2 | Low | code_quality | serializemidi-longer-than-stated | `serializeMidi` is longer than the stated logic-size threshold and should be split into named helpers to improve scan... | WONTFIX | Duplicate of #26; deferred to future refactor sprint | R2,R3 |
| 35 | R2 | Low | test_quality | stopmidicapture-testing-only-cov | `stopMidiCapture` testing only covers the idle case and does not verify non-empty buffer return, buffer clearing, or ... | ADDRESSED | `startMidiCapture_stopMidiCapture_full_capture_roundtrip` lifecycle test fires a real event and verifies non-empty return + state clear | R2,R3 |
| 36 | R2 | Low | code_quality | capacity-rate-limit-signals | `"capacity"` in `rate_limit_signals` is too broad and can trigger incorrect long backoff on unrelated errors. (File: ... | WONTFIX | Council tooling improvement; not a Sprint 15 deliverable — deferred | R2,R3 |
| 37 | R3 | Medium | code_quality | serializemidi-still-exceeds-stat | `serializeMidi` still exceeds the stated 60-line logic ceiling and keeps byte-writer helpers inline instead of extrac... | WONTFIX | Duplicate of #26/#34; extraction deferred to future refactor sprint | R3 |
| 38 | R3 | Medium | test_quality | two-async-initmidirecording-test | Two async `initMidiRecording` tests still restore `document.getElementById` outside `finally`, so a failing assertion... | ADDRESSED | `finally` blocks added to `initMidiRecording_device_picker_uses_textContent` and `initMidiRecording_onstatechange_reveals_section_when_port_appears` | R3 |
| 39 | R3 | Low | test_quality | event-cap-status-test | The event-cap status test still does not assert the `10 000` threshold text, only a generic `"limit"` substring (File... | OPEN |  | R3 |
