# Findings Tracker: Sprint 10 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | samesite-strict-violates-persist | `SameSite=Strict` violates the persistent room URL behavior because browsers will not send the teacher session cookie... | ADDRESSED | Plan specifies SameSite=Lax; explains Strict would break external-link navigation to /teach/<slug> | R1,R2,R3,R4 |
| 2 | R1 | High | security | login-plan-still-leaks | The login plan still leaks account existence through timing because the unknown-email path skips Argon2 verification ... | ADDRESSED | Plan now calls verify_password against DUMMY_PHC for all unknown-email paths; Argon2 cost equalized | R1,R2,R3,R4 |
| 3 | R1 | High | code_quality | lockout-flow-not-specified | The lockout flow is not specified as an atomic operation. A separate check-then-record design allows concurrent attem... | ADDRESSED | record_and_check_limits uses single BEGIN IMMEDIATE transaction covering INSERT + COUNT | R1,R2,R3,R4 |
| 4 | R1 | High | code_quality | password-hashing-api-shape | The password hashing API shape is unsafe for callers because a synchronous `hash_password` contract pushes `spawn_blo... | ADDRESSED | hash_password() is async and wraps spawn_blocking internally; callers cannot hash on async runtime | R1,R2,R3,R4 |
| 5 | R1 | High | security | registration-flow-has-no | The registration flow has no server-side password minimum. Client-side confirmation is not a substitute. Weak or empt... | ADDRESSED | Server-side validation: min 12, max 128 chars; 400 with code password_too_short/password_too_long | R1,R2,R3,R4 |
| 6 | R1 | High | domain | migration-login-plan-do | The migration and login plan do not define a safe path for existing teachers with `password_hash IS NULL`. The plan e... | ADDRESSED | NULL-hash login behavior fully specified: 401 "invalid credentials" (reset_enabled=false) or "no password set" (true); Argon2 cost equalized via DUMMY_PHC | R1,R2,R3,R4 |
| 7 | R1 | Medium | test_quality | plan-mentions-cheap-argon2 | The plan mentions cheap Argon2 settings for tests but does not define a safe injection mechanism. A runtime `Config` ... | ADDRESSED | register_teacher fixture calls hash_password_with_params directly with CHEAP_PARAMS constant; no runtime config flag | R1,R2,R3,R4 |
| 8 | R1 | Medium | domain | registration-failure-coverage-in | Registration failure coverage is incomplete for duplicate slug collisions. `teachers.slug` is unique, so the plan mus... | ADDRESSED | Test matrix includes duplicate slug → 409 with suggestions | R1,R2,R3,R4 |
| 9 | R1 | Medium | security | login-protection-model-incomplet | The login protection model is incomplete because only per-account lockout is described. The planned schema already in... | ADDRESSED | IP-based throttle fully specified: ip_window_secs=300, ip_max_attempts=20; fires on unknown-email paths too | R1,R2,R3,R4 |
| 10 | R1 | Medium | code_quality | login-attempts-retention-unbound | `login_attempts` retention is unbounded. The plan needs pruning or bounded retention to avoid indefinite table growth... | ADDRESSED | Retention: cleanup.rs prunes rows older than 24h (same pattern as recording_gate_attempts) | R1,R2,R3,R4 |
| 11 | R1 | Medium | code_quality | lockout-rule-count-failures | The lockout rule "count failures after the most recent success" exists only in prose. The plan should define the exac... | ADDRESSED | Exact SQL specified: SELECT MAX(attempted_at) WHERE succeeded=1; COUNT failures WHERE succeeded=0 AND attempted_at > max(last_success, now-window) | R1,R2,R3,R4 |
| 12 | R1 | Medium | domain | architecture-record-stale | The architecture record is stale. Sprint 10 changes the primary auth model, but ADR-0001 still declares magic-link au... | ADDRESSED | ADR update section added: knowledge/decisions/0001-mvp-architecture.md updated to note magic-link superseded | R1,R2,R3,R4 |
| 13 | R2 | High | security | per-ip-throttle-bypassed | The per-IP throttle is bypassed for unknown-email attempts because login-attempt recording requires a non-null `teach... | ADDRESSED | teacher_id made nullable in login_attempts; record_and_check_limits always INSERTs (teacher_id=NULL for unknown-email); IP count covers all attempts | R2,R3,R4 |
| 14 | R2 | High | security | revised-login-flow-still | The revised login flow still leaks account existence by timing because the known-email wrong-password path performs a... | ADDRESSED | record_and_check_limits always INSERTs before returning regardless of teacher existence; DB write cost symmetric across all paths | R2,R3,R4 |
| 15 | R2 | Medium | domain | dummy-phc-specification-internal | The `DUMMY_PHC` specification is internally contradictory. One section describes a compile-time `static &str`; anothe... | ADDRESSED | Single specification: once_cell::sync::Lazy<String> initialized at first use via hash_password_with_params("", Params::DEFAULT); no static str | R2,R3,R4 |
| 16 | R2 | Medium | code_quality | visibility-hash-password-with | The visibility of `hash_password_with_params` is unspecified even though the test strategy expects reuse from integra... | ADDRESSED | Declared pub; plan explicitly states this is required for server/tests/common/mod.rs | R2,R3,R4 |
| 17 | R2 | Medium | test_quality | test-plan-does-not | The test plan does not state that `register_teacher` uses cheap Argon2 parameters. Without that, the stated suite run... | ADDRESSED | Fixture reuse plan now explicitly states CHEAP_PARAMS constant (m=8,t=1,p=1) used by register_teacher | R2,R3,R4 |
| 18 | R2 | Medium | test_quality | password-hash-null-login | The `password_hash IS NULL` login branch is specified but not covered by tests. That leaves the migration fallback an... | ADDRESSED | Both NULL-hash branches (reset_enabled=false and true) added to failure-path coverage | R2,R3,R4 |
| 19 | R2 | Low | code_quality | migration-text-cites-nonexistent | The migration text cites a nonexistent magic-link cleanup pattern. The existing cleanup precedent is `recording_gate_... | ADDRESSED | Migration section now references recording_gate_attempts as the cleanup precedent | R2,R3,R4 |
| 20 | R2 | Low | code_quality | hash-password-blocking-reference | `hash_password_blocking` is referenced in the `DUMMY_PHC` design but omitted from the module API table, leaving its v... | ADDRESSED | Module API table updated: hash_password() is the async public API; hash_password_with_params() is the sync helper used directly by tests | R2,R3,R4 |
| 21 | R2 | Low | test_quality | success-boundary-tests-password | Success-boundary tests for password lengths exactly 12 and exactly 128 are missing. | ADDRESSED | Boundary success tests added: exactly 12 chars → 200, exactly 128 chars → 200 | R2,R3,R4 |
| 22 | R2 | Low | test_quality | lockout-window-lower-boundary | The lockout-window lower boundary at exactly `window_secs` is not tested, so the SQL `>` semantics are not pinned aga... | ADDRESSED | Boundary test added and SQL semantics clarified: strict > excludes exact boundary; attempt at now-window_secs does NOT count | R2,R3,R4 |
| 23 | R2 | Low | security | disabling-password-reset-enabled | Disabling `password_reset_enabled` blocks magic-link routes but does not address already-issued tokens, and the operator-visible behavior under flag toggling is undocumented. | ADDRESSED | Operator runbook text added: existing unconsumed tokens inert (consume returns 403); explicit SQL purge command documented | R2,R3,R4 |
| 24 | R3 | Medium | code_quality | fixture-reuse-plan-contains | Fixture reuse plan contains a contradictory abandoned design artifact. The section still mentions `AppState.config.argon2_cheap_for_tests: bool` and then retracts it inline. | ADDRESSED | Removed retraction and config-flag mention; fixture section now describes single mechanism only (CHEAP_PARAMS constant + direct hash insert) | R2,R3,R4 |
| 25 | R3 | Medium | test_quality | null-hash-login-tests | NULL-hash login tests have no defined fixture or setup path. | ADDRESSED | Added insert_teacher_no_password(email, slug) helper that does raw INSERT with no password_hash column | R2,R3,R4 |
| 26 | R3 | Medium | test_quality | stated-auth-test-runtime | The stated auth test runtime budget is inconsistent with the planned IP throttle test using production-cost DUMMY_PHC. | ADDRESSED | Budget updated to < 10 s with per-path cost breakdown; IP throttle tests use direct SQL INSERT for seeding (no repeated DUMMY_PHC calls) | R2,R3,R4 |
| 27 | R3 | Medium | domain | lockout-window-boundary-test | The lockout-window boundary test contradicts the specified SQL operator. | ADDRESSED | Fixed: boundary predicate is strict >; attempt at now-window_secs does NOT count; both SQL and test text now consistent | R3,R4 |
| 28 | R3 | Medium | security | post-login-does-not | `post_login` does not specify how `peer_ip` is derived. | ADDRESSED | Handler spec now mandates ConnectInfo<SocketAddr> extractor (same as signup.rs); addr.ip().to_string() is the extraction path | R2,R3,R4 |
| 29 | R4 | Medium | security | login-register-ip-throttling | Login and register IP throttling is specified against raw `ConnectInfo<SocketAddr>` instead of the existing Cloudflar... | OPEN |  | R2,R4 |
| 30 | R4 | Medium | code_quality | successful-login-specified-as | Successful login is specified as two writes to `login_attempts`: the initial `succeeded=0` insert from `record_and_ch... | OPEN |  | R2,R4 |
| 31 | R4 | Low | test_quality | test-plan-does-not | The test plan does not assert `Set-Cookie` attributes on login success, register success, and logout, so the specifie... | OPEN |  | R2,R4 |
| 32 | R4 | Low | code_quality | placing-login-rate-limiting | Placing login rate-limiting types and functions in `auth/password.rs` breaks the existing module-separation pattern u... | OPEN |  | R2,R4 |
| 33 | R2 | Medium | security | login-register-ip-throttling | Login and register IP throttling still use the raw socket peer IP, so rate limits are ineffective behind Cloudflare o... | OPEN |  | R2 |
| 34 | R2 | Medium | code_quality | login-rate-limit-types | Login rate-limit types and logic remain in `auth/password.rs` instead of a rate-limit module. This keeps hashing and ... | OPEN |  | R2 |
| 35 | R2 | Medium | code_quality | production-argon2-parameters-dup | Production Argon2 parameters are duplicated in two places. A future one-sided edit can desynchronize the dummy hash p... | OPEN |  | R2 |
| 36 | R2 | Low | test_quality | cookie-attribute-assertions-rema | Cookie attribute assertions remain incomplete in HTTP tests. Success and logout paths still do not fully assert `Http... | OPEN |  | R2 |
| 37 | R2 | Low | test_quality | new-cleanup-branch-login | The new cleanup branch for `login_attempts` has no regression test. Old and recent rows are not tested across the ret... | OPEN |  | R2 |
| 38 | R2 | Low | test_quality | there-no-test-proving | There is no test proving a successful login resets the lockout window. A regression in the success-marking path would... | OPEN |  | R2 |
| 39 | R2 | Low | security | password-length-validation-uses | Password length validation uses UTF-8 byte length instead of Unicode character count. This does not match the approve... | OPEN |  | R2 |
| 40 | R2 | Low | security | logout-cookie-clearance-header | The logout cookie-clearance header omits `Secure` when secure cookies are required. The issue is low risk but leaves ... | OPEN |  | R2 |
| 41 | R2 | Low | code_quality | then-t-some-t | `and_then(|(t,)| Some(t))` should be `map(|(t,)| t)`. This is a Clippy-level cleanup issue. (File: `server/src/auth/p... | OPEN |  | R2 |
| 42 | R2 | Low | code_quality | signup-test-helper-name | The `signup()` test helper name no longer matches the `/auth/register` route it posts to. The helper behavior is corr... | OPEN |  | R2 |
