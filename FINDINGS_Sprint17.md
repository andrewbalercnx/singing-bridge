# Findings Tracker: Sprint 17 (plan)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | dashboard-library-panel-wired | The dashboard library panel is wired to the wrong endpoint. The plan uses `/teach/<slug>/library`, which serves HTML,... | ADDRESSED | R2: changed to `GET /teach/<slug>/library/assets` (JSON); asset count = `array.length` | R1,R2,R3 |
| 2 | R1 | High | domain | plan-removes-session-lobby | The plan removes in-session lobby admission and conflicts with ADR-0001’s parallel-lobby invariant. This is a spec vi... | ADDRESSED | R2: lobby stays on session page unchanged; dashboard is between-session only; ADR-0001 not amended | R1,R2,R3 |
| 3 | R1 | High | test_quality | test-plan-not-executable | The test plan is not executable as written and does not cover the primary new behavior. It references a nonexistent P... | ADDRESSED | R2: test plan references concrete files: `server/tests/http_dashboard.rs`, `web/assets/tests/session-panels.test.js`, `web/assets/tests/dashboard.test.js`; Rust coverage added for all 8 route cases; Node coverage for buildSelfPip, buildAccmpPanel, buildIconBar, XSS; runtime budget references `cargo test` + `node --test` | R1,R2,R3 |
| 4 | R1 | Medium | security | owner-only-dashboard-responses | Owner-only dashboard responses and owner-only redirect responses need explicit `Cache-Control: private, no-store` and... | ADDRESSED | R2: `dashboard.rs` and `get_session()` in `teach.rs` both set `Cache-Control: private, no-store` + `Vary: Cookie`; regression test in `http_dashboard.rs` | R1,R2,R3 |
| 5 | R1 | Medium | security | live-1-proposed-sb | The `?live=1` and proposed `sb-live=1` bypass put client-controlled state into auth-sensitive page selection. A dedic... | ADDRESSED | R2: `?live=1` removed; dedicated `/teach/:slug/session` route added; auth check identical to teach.rs | R1,R2,R3 |
| 6 | R1 | Medium | code_quality | plan-increases-ui-complexity | The plan increases UI complexity without addressing existing code-quality limits. `accompaniment-drawer.js::mount()` ... | ADDRESSED | R2: all new DOM builders (buildSelfPip, buildAccmpPanel, buildIconBar) go in session-panels.js; mount() stays orchestrator ≤40 lines; shared el() stays local (2 lines, no extraction); accompaniment panelEl branch is a single if() at DOM time only | R1,R2,R3 |
| 7 | R1 | Medium | test_quality | several-stated-test-invariants | Several stated test invariants are not runnable in the current harness as described. This affects CSS layout assertio... | ADDRESSED | R2: CSS layout assertions removed (not runnable in Node); replaced with DOM structure assertions on builder output; sessionStorage test added to dashboard.test.js; null-panelEl fallback verified via accompaniment-drawer.test.js; Rust test covers malformed slug | R1,R2,R3 |
| 8 | R1 | Low | code_quality | plan-does-not-enumerate | The plan does not enumerate required header and documentation updates, including `session-panels.js`, `teach.rs`, and... | ADDRESSED | R2: complete file table with header change column added | R1,R2,R3 |
| 9 | R1 | Low | code_quality | icon-only-controls-refactor | The icon-only controls refactor leaves stale test assumptions and dead symbols unless the plan explicitly removes lab... | ADDRESSED | R2: buildControls explicitly removed; renamed buildIconBar; sb-btn-label spans deleted; controls.test.js and session-panels.test.js updated to test buildIconBar | R1,R2,R3 |
| 10 | R2 | High | test_quality | plan-refers-extending-web | The plan refers to extending `web/assets/tests/session-panels.test.js`, but that file does not exist. The plan must d... | ADDRESSED | R3: reclassified as new file in file table and test plan text | R2,R3 |
| 11 | R2 | High | test_quality | planned-buildiconbar-teacher-but | The planned `buildIconBar` teacher button count is internally contradictory. One section says 4, while the listed but... | ADDRESSED | R3: canonical count is 5 (mic, vid, accompaniment, chat, end); propagated to test assertion and ASCII art | R2,R3 |
| 12 | R2 | Medium | test_quality | new-panelel-positive-path | The new `panelEl` positive path in the accompaniment drawer has no planned test coverage. The non-null render branch ... | ADDRESSED | R3: added explicit test case in accompaniment-drawer.test.js for non-null panelEl: controls rendered into element, play/pause updates audio.paused, seek updates audio.currentTime | R2,R3 |
| 13 | R2 | Medium | test_quality | dashboard-test-plan-omits | The dashboard test plan omits the two stated fetch-failure scenarios. Those failure paths are part of the described b... | ADDRESSED | R3: added two fetch-failure test cases in dashboard.test.js (recordings failure, library failure) with independence assertion | R2,R3 |
| 14 | R2 | Medium | test_quality | buildaccmppanel-public-setter-ap | The `buildAccmpPanel` public setter API is not covered by the plan. The DOM mutations driven by `setTrackName`, `setP... | ADDRESSED | R3: added setter tests: setTrackName updates textContent, setPosition updates slider value, setDuration updates slider max, setPaused updates button text/aria | R2,R3 |
| 15 | R2 | Medium | code_quality | buildselfpreview-replaced-plan-b | `buildSelfPreview` is replaced in the plan but not scheduled for removal. That leaves orphaned dead code in `session-... | ADDRESSED | R3: buildSelfPreview explicitly listed for removal from session-ui.js alongside buildControls | R2,R3 |
| 16 | R2 | Low | test_quality | redirect-test-get-teach | The redirect test for `GET /teach/<slug>` is underspecified. It should assert both the `Location` header and the requ... | ADDRESSED | R3: redirect test now asserts Location header value and Cache-Control + Vary headers | R2,R3 |
| 17 | R2 | Low | code_quality | new-dashboard | The new `dashboard.js` file does not specify whether it follows the project’s JS module pattern or is a self-containe... | ADDRESSED | R3: specified as plain IIFE self-contained script, same pattern as library.js and recordings.js; no window.sbXxx export | R2,R3 |
| 18 | R2 | Low | domain | session-dashboard-not-reserved | `"session"` and `"dashboard"` are not reserved slugs even though they become server-owned path segments under `/teach... | ADDRESSED | R3: added "session" and "dashboard" to RESERVED_SLUGS in slug.rs; file added to plan file table; existing rejects_reserved test covers them automatically | R2,R3 |
| 19 | R3 | Medium | test_quality | buildaccmppanel-includes-score-v | `buildAccmpPanel` includes a score-viewer-toggle control in the design, but the test plan does not lock its presence ... | OPEN |  | R3 |
| 20 | R3 | Medium | code_quality | accompaniment-drawer | `accompaniment-drawer.js::mount()` is already 232 lines and the sprint touches this file without requiring a split. T... | OPEN |  | R3 |
| 21 | R3 | Low | test_quality | session-layout-ascii-art | The session layout ASCII art caption still says `teacher: all 4`, while the canonical teacher control count is 5. The... | OPEN |  | R3 |
| 22 | R3 | Low | test_quality | plan-states-session-teardown | The plan states session teardown clears `sessionStorage.removeItem('sb-accmp-open')`, but no test is assigned to veri... | OPEN |  | R3 |
| 23 | R3 | Low | security | non-owner-redirect-path | The non-owner redirect path in `get_dashboard()` is not shown with the same cache-header invariant as the owner path,... | OPEN |  | R3 |
| 24 | R3 | Low | code_quality | plan-leaves-get-session | The plan leaves the `get_session()` / `get_teach()` handler structure open, which creates avoidable duplication risk ... | OPEN |  | R3 |
