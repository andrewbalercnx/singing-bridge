# Findings Tracker: Sprint 11 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | domain | upsert-student-cannot-use | `upsert_student` cannot use `INSERT OR IGNORE` plus `last_insert_rowid()` to return `student_id`; repeat joins for th... | ADDRESSED | Implementation uses INSERT OR IGNORE + SELECT (never `last_insert_rowid()`): `server/src/ws/session_history.rs:39-54`. | R1,R2 |
| 2 | R1 | High | domain | admission-flow-introduces-orphan | The admission flow introduces an orphan-row race for `session_event_id`. If the session ends after the DB write but b... | ADDRESSED | Re-lock after `open_event`; if `active_session` is gone, calls `close_event(…, EndedReason::Disconnect)` immediately: `server/src/ws/lobby.rs:428-444`. Mirrors the existing `session_log` orphan pattern. | R1,R2 |
| 3 | R1 | High | security | history-page-stores-renders | The history page stores and renders student email without sufficient protection. Websocket admission validates only l... | OPEN |  | R1,R2,R3,R4 |
| 4 | R1 | High | security | close-event-link-recording | `close_event` and `link_recording` are keyed by sequential integer `event_id` with no `teacher_id` ownership guard. T... | ADDRESSED | Both functions enforce `AND teacher_id = ?` in their WHERE clauses: `close_event` at `session_history.rs:86-99`; `link_recording` at `session_history.rs:155-165`. Tests `close_event_wrong_teacher_is_noop` and `link_recording_wrong_teacher_is_noop` verify neither mutates another teacher's rows. | R1,R2,R3,R4 |
| 5 | R1 | High | test_quality | test-strategy-omits-critical | The test strategy omits critical invariants and edge cases for the new persistence path: case-insensitive `upsert_stu... | ADDRESSED | All unit invariants covered: `upsert_student_case_insensitive`, `close_event_first_writer_wins`, `close_event_wrong_teacher_is_noop`, `recording_slot_roundtrip` in `session_history.rs`. WS integration tests added in `ws_session_handshake.rs`. Remaining test gaps (sleep, missing WS assertions) tracked separately as #16, #20, #21, #24. | R1,R2,R3,R4 |
| 6 | R1 | Medium | domain | recording-linkage-by-querying | Recording linkage by querying the teacher’s “most recent open session_event” is race-prone. Uploads that finish after... | ADDRESSED | Durable recording-slot pattern fully replaces any “most recent open event” query: slot written at consent time (`session_history.rs:103-121`), consumed atomically at upload (`session_history.rs:124-146`). No query against live session state at upload time. | R1,R2 |
| 7 | R1 | Medium | domain | archive-semantics-underspecified | Archive semantics are underspecified and currently wrong. The proposed cleanup uses `started_at` rather than session ... | ADDRESSED | Archive SQL uses `COALESCE(ended_at, started_at) < cutoff`: `session_history.rs:171-179`. For completed sessions the cutoff is measured from `ended_at`; live sessions fall back to `started_at`. Test `archive_old_events_by_completion_time` covers both branches. | R1,R2 |
| 8 | R1 | Medium | code_quality | close-event-should-accept | `close_event` should accept `EndedReason` directly and implement first-writer-wins semantics with `WHERE ... AND ende... | ADDRESSED | `close_event` signature takes `EndedReason` (not `&str`): `session_history.rs:78-84`. WHERE clause includes `AND ended_at IS NULL` for first-writer-wins: `session_history.rs:86-99`. Test `close_event_first_writer_wins` verifies second call is a no-op. | R1,R2,R3,R4 |
| 9 | R1 | Medium | code_quality | new-ids-specified-as | New ids are specified as bare `Option<i64>` on `ActiveSession`, which breaks the existing opaque-id pattern used by `... | OPEN |  | R1,R2,R3,R4 |
| 10 | R1 | Medium | domain | email-normalization-not-specifie | Email normalization is not specified for `students.email`. Deduplication uses `COLLATE NOCASE`, but storage should st... | ADDRESSED | `upsert_student` passes `lower(?)` in both the INSERT and the SELECT: `session_history.rs:41` and `:49`. Every row in `students.email` is therefore stored lowercase regardless of input casing. Tests `upsert_student_case_insensitive` confirms three mixed-case variants all resolve to the same id. | R1,R2 |
| 11 | R1 | Low | code_quality | naming-constant-hygiene-inconsis | Naming and constant hygiene are inconsistent. `duration_s` diverges from existing `duration_secs`, `LIMIT 100` and `9... | OPEN |  | R1,R2,R3,R4 |
| 12 | R1 | Low | security | students-table-has-no | The `students` table has no retention or erasure path, so raw student emails persist indefinitely. This needs explici... | OPEN |  | R1,R2,R3,R4 |
| 13 | R2 | Medium | security | consume-recording-slot-atomicity | `consume_recording_slot` atomicity is not specified at the SQL level, so the plan does not yet prove single-consumpti... | ADDRESSED | Implemented as `DELETE FROM recording_sessions WHERE teacher_id = ? RETURNING session_event_id, created_at`: `session_history.rs:131-133`. A single atomic DELETE+RETURNING statement; no TOCTOU window. | R2,R3,R4 |
| 14 | R2 | Medium | domain | durable-recording-slot-design | The durable recording-slot design has an undocumented overwrite limitation: one slot per teacher means the most recen... | OPEN |  | R2 |
| 15 | R2 | Medium | code_quality | plan-still-uses-transparent | The plan still uses transparent aliases for `StudentId` and `SessionEventId` instead of newtypes, so the compiler wil... | OPEN |  | R2,R3,R4 |
| 16 | R2 | Medium | test_quality | test-strategy-remains-incomplete | The test strategy remains incomplete for key persistence invariants: `close_event` ownership enforcement, recording-s... | ADDRESSED | Ownership: `close_event_wrong_teacher_is_noop` (`session_history.rs`). Single-consume: `recording_slot_roundtrip`. WS lifecycle DB assertion: `session_event_row_has_ended_at_after_disconnect` (`ws_session_handshake.rs:149`). | R2,R3,R4 |
| 17 | R2 | Low | domain | expired-recording-sessions-rows | Expired `recording_sessions` rows are left behind indefinitely unless a later consent overwrites them. The table is b... | ADDRESSED | `consume_recording_slot` uses `DELETE…RETURNING` (`session_history.rs:131-133`): the row is deleted atomically on any consume attempt, including expired ones. Test `recording_slot_expired_returns_none` asserts `COUNT(*) = 0` after a TTL-expired consume: `session_history.rs:418-426`. | R2 |
| 18 | R3 | High | code_quality | student-side-session-history | Student-side session history wiring is broken because `ctx.candidate_teacher_id` is `None` on student connections, ma... | ADDRESSED | `admit()` runs on the teacher's WS context, so `ctx.candidate_teacher_id` is the teacher's id. It is stored as `ActiveSession::session_teacher_id` at `lobby.rs:324`. The close path reads `session.session_event_id.zip(session.session_teacher_id)` at `ws/mod.rs:770`, so student-side disconnects correctly supply the teacher_id without touching `ctx`. | R3,R4 |
| 19 | R3 | Medium | code_quality | consume-recording-slot-not | `consume_recording_slot` is not atomic despite claiming atomic consume semantics. The current `SELECT` then `DELETE` ... | ADDRESSED | Rewritten as a single `DELETE…RETURNING` statement: `session_history.rs:131-133`. No SELECT precedes the DELETE; the TOCTOU window is closed. Same code evidence as #13. | R3,R4 |
| 20 | R3 | Medium | test_quality | no-websocket-integration-test | No websocket integration test covers the malformed `lobby_join` email path for emails missing `@` or failing the mini... | ADDRESSED | `lobby_join_invalid_email_closes_malformed` at `ws_session_handshake.rs:123` tests `["notanemail", "ab", "@"]` — covers missing-`@`, too-short, and bare-`@` cases; asserts connection receives a Close frame. | R3,R4 |
| 21 | R3 | Medium | test_quality | websocket-lifecycle-integration | The websocket lifecycle integration tests do not assert that a `session_event` row is persisted with non-`NULL` `ende... | ADDRESSED | `session_event_row_has_ended_at_after_disconnect` at `ws_session_handshake.rs:149` performs a full admit cycle, drops the student, then queries `session_events.ended_at` and asserts it is non-NULL. Note: uses a 100ms sleep as sync — tracked as #24. | R3,R4 |
| 22 | R3 | Low | code_quality | admit-has-accumulated-excessive | `admit()` has accumulated excessive nesting around session-history setup, reducing readability and increasing change ... | OPEN |  | R3,R4 |
| 23 | R3 | Low | test_quality | recording-slot-expired-returns | `recording_slot_expired_returns_none` does not verify that the expired `recording_sessions` row is deleted after cons... | ADDRESSED | Test `recording_slot_expired_returns_none` (`session_history.rs:403-426`) explicitly queries `COUNT(*) FROM recording_sessions WHERE teacher_id = 1` after consume and asserts `count == 0` with message "expired slot must be deleted on consume attempt". | R3,R4 |
| 24 | R4 | Medium | test_quality | websocket-lifecycle-test-uses | The websocket lifecycle test uses a real `100 ms` sleep as synchronization. This violates the stated test policy and ... | OPEN |  | R4 |
| 25 | R4 | Medium | code_quality | studentid-sessioneventid-remain | `StudentId` and `SessionEventId` remain transparent `i64` aliases. The compiler still permits cross-use of distinct I... | OPEN |  | R4 |
| 26 | R4 | Medium | security | history-page-returns-pii | The history page returns PII without a cache-control directive. Browser and intermediary caches can retain student em... | OPEN |  | R4 |
| 27 | R4 | Low | code_quality | admit-remains-oversized-deeply | `admit()` remains oversized and deeply nested. The session-history branch should be extracted to a helper. This leave... | OPEN |  | R4 |
| 28 | R4 | Low | code_quality | admit-re-checks-teacher | `admit()` re-checks `teacher_id` inside a branch that already proved it is `Some`. The inner bind is redundant and ad... | OPEN |  | R4 |
| 29 | R4 | Low | code_quality | activesession-uses-inconsistent | `ActiveSession` uses inconsistent field naming within the same session-history block. `student_id` breaks the `sessio... | OPEN |  | R4 |
| 30 | R4 | Low | code_quality | duration-s-unclear-name | `duration_s` is an unclear name for a formatted string value. The name diverges from the surrounding duration naming ... | OPEN |  | R4 |
