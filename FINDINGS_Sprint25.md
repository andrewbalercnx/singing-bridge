# Findings Tracker: Sprint 25 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R2 | High | security | test-only-auth-bypass | Test-only auth-bypass routes are insufficiently gated and can ship in release binaries or be enabled in production by... | ADDRESSED | `#[cfg(debug_assertions)]` compile-time gate + `validate_prod_config()` rejects `test_peer: true` — see Design § "Runtime flag + compile-time gate" | R1,R2,R3,R4,R5,R6,R7 |
| 2 | R2 | High | domain | teacher-bot-session-exchange | The teacher-bot session exchange is missing the required database-backed session creation, so the bot will not be pro... | ADDRESSED | Plan now calls `auth::issue_session_cookie(pool, teacher_id, 180)` which generates random raw cookie, stores SHA-256 in `cookie_hash`, sets `expires_at = now+180` — see `get_test_peer_session` handler | R2,R3,R4,R5,R6,R7 |
| 3 | R2 | High | code_quality | bot-messaging-api-internally | The bot messaging API is internally inconsistent. The plan defines `window._sbSend(...)` but the bot flow calls `wind... | ADDRESSED | Unified to single `window._sbSend(obj)` — takes plain JS object, stringifies internally; bot calls `page.evaluate("window._sbSend({...})")` — see § `window._sbSend` exposure | R1,R2,R3,R4,R5,R6,R7 |
| 4 | R2 | High | test_quality | active-bots-design-contradictory | The `active_bots` design is contradictory and cannot support the promised crash cleanup behavior. `Arc<DashSet<String... | ADDRESSED | `DashMap<String, ()>` specified; cleanup via `tokio::spawn` + `child.wait()` + `remove()` — see § `active_bots` state | R1,R2,R3,R4,R5,R6,R7 |
| 5 | R2 | High | test_quality | test-strategy-omits-direct | The test strategy omits direct coverage for `/test-peer/session`, which is the primary security boundary in the new f... | ADDRESSED | Bot-session TTL test added: verifies `Set-Cookie: Max-Age=180` and `expires_at ≈ now+180s` — see Test Strategy § Property/invariant coverage | R1,R2,R3,R4,R5,R6,R7 |
| 6 | R2 | Medium | security | one-time-token-planned | The one-time token is planned as a URL query parameter for `GET /test-peer/session`, which exposes it to access logs ... | ADDRESSED | Changed to `POST /test-peer/session` with token in JSON body — see § New HTTP routes | R1,R2,R3,R4,R5,R6,R7 |
| 7 | R2 | Medium | code_quality | token-store-has-no | The token store has no explicit cardinality cap. Sweep-on-insert does not bound growth during the TTL window under re... | ADDRESSED | Cap of 100 live tokens added to `TokenStore`; insert sweeps then checks `len >= cap` — see § One-time token store | R1,R2,R3,R4,R5,R6,R7 |
| 8 | R2 | Medium | test_quality | documented-subprocess-test-seam | The documented subprocess test seam is incomplete. The test strategy relies on `SB_TEST_PEER_SCRIPT=echo`, but the de... | ADDRESSED | `test_peer_script: Option<String>` config field added; `SB_TEST_PEER_SCRIPT` env var; tests set this to `echo` — see § New config field | R1,R2,R3,R4,R5,R6,R7 |
| 9 | R2 | Medium | test_quality | 409-concurrency-test-not | The 409 concurrency test is not reliable with an `echo` subprocess because the mock exits too quickly to keep the slu... | ADDRESSED | 409 test redesigned: pre-insert slug directly into `state.active_bots` in unit test; no subprocess timing dependency — see Test Strategy § Property/invariant coverage | R1,R2,R3,R4,R5,R6,R7 |
| 10 | R2 | Low | security | bot-issued-teacher-session | The bot-issued teacher session is described with the normal long session lifetime and no revocation path after bot ex... | ADDRESSED | 180s TTL session via `issue_session_cookie(..., 180)`; expires within bot lifetime by construction — see `get_test_peer_session` handler | R1,R2,R3,R4,R5,R6,R7 |
| 11 | R2 | Low | test_quality | regression-coverage-new-js | Regression coverage for the new JS bridge is not specified. The plan should add a test that verifies `window._sbSend`... | ADDRESSED | `window._sbSend` JS unit test added to plan: stub `WebSocket.send` → assert called with stringified JSON — see Test Strategy § Regression guards | R1,R2,R3,R4,R5,R6,R7 |
| 12 | R2 | Low | test_quality | planned-data-testid-additions | The planned `data-testid` additions are not tied to regression tests, so selectors critical to the bot flow can disap... | ADDRESSED | DOM regression tests added for `admit-btn` and `session-active` testids — see Test Strategy § Regression guards | R1,R2,R3,R4,R5,R6,R7 |
| 13 | R2 | Low | code_quality | file-map-does-not | The file map does not call out the project-mandated header blocks for new source files. (File: `PLAN_Sprint25.md`, Lo... | ADDRESSED | File map updated: `test_peer.rs` and `test_peer.py` explicitly require structured header blocks — see § File map | R1,R2,R3,R4,R5,R6,R7 |
| 14 | R3 | High | code_quality | teacher-bot-websocket-send | Teacher-bot websocket send API is internally inconsistent and will fail at runtime. The plan defines `window._sbSend(... | ADDRESSED | Unified to `window._sbSend(obj)` throughout — no `_sbWs` reference remains; bot calls `page.evaluate("window._sbSend({...})")` | R1,R3,R4,R5,R6,R7 |
| 15 | R3 | High | domain | student-bot-selectors-do | Student-bot selectors do not match the actual join UI. The scripted `#student-email` and `#student-join-btn` elements... | ADDRESSED | Corrected to `#join-email`, `#join-form`, `#session` — matching actual student.html DOM | R3,R4,R5,R6,R7 |
| 16 | R3 | High | security | test-peer-routes-lack | Test-peer routes lack the existing compile-time debug guard pattern. Runtime env gating alone allows the route to shi... | ADDRESSED | `#[cfg(debug_assertions)]` wraps route registration; `validate_prod_config()` also rejects `test_peer: true` — see § Runtime flag + compile-time gate | R1,R3,R4,R5,R6,R7 |
| 17 | R3 | High | security | bot-teacher-sessions-not | Bot teacher sessions are not revoked on process exit. The plan exchanges a one-time token for a normal long-lived tea... | ADDRESSED | 180s TTL via `issue_session_cookie(..., 180)`; session expires within bot lifetime; no long-lived cookie issued | R1,R3,R4,R5,R6,R7 |
| 18 | R3 | High | domain | teacher-session-creation-undersp | Teacher session creation is underspecified. The plan does not define how `get_test_peer_session` resolves the slug-ow... | ADDRESSED | Handler now: query `teachers WHERE slug = $1` → `teacher_id`; call `issue_session_cookie(pool, teacher_id, 180)` — explicitly references existing auth path — see `get_test_peer_session` handler | R3,R4,R5,R6,R7 |
| 19 | R3 | High | test_quality | test-fixture-seam-missing | The test fixture seam is missing from the design. The test plan depends on `SB_TEST_PEER_SCRIPT=echo`, but the design... | ADDRESSED | `test_peer_script: Option<String>` in Config; `SB_TEST_PEER_SCRIPT=echo` wires through — see § New config field | R1,R3,R4,R5,R6,R7 |
| 20 | R3 | Medium | code_quality | active-bots-lifecycle-not | `active_bots` lifecycle is not coherently specified. The plan declares `DashSet<String>` but also says the per-slug e... | ADDRESSED | `DashMap<String, ()>` consistently throughout; cleanup is `tokio::spawn` + `child.wait()` + `active_bots.remove()` — see § `active_bots` state | R1,R3,R4,R5,R6,R7 |
| 21 | R3 | Medium | code_quality | token-generation-ordered-before | Token generation is ordered before the 409 active-bot check. A conflicting request would create an unreachable token ... | ADDRESSED | Handler step 3 checks `active_bots` before step 5 generates token — see `get_test_peer` handler | R1,R3,R4,R5,R6,R7 |
| 22 | R3 | Medium | security | test-peer-session-slug | `/test-peer/session` slug binding is ambiguous. If the handler trusts the query `slug` instead of the token-stored sl... | ADDRESSED | Slug recovered exclusively from token entry; route changed to POST with body (no query slug); token entry is authoritative — see § New HTTP routes and `get_test_peer_session` | R1,R3,R4,R5,R6,R7 |
| 23 | R3 | Medium | security | token-store-has-no | The token store has no stated cap. Sweep-on-insert removes expired entries but does not bound live entries within the... | ADDRESSED | Hard cap of 100 live tokens; `CapExceeded` error returns 503 to caller — see § One-time token store | R1,R3,R4,R5,R6,R7 |
| 24 | R3 | Medium | code_quality | production-config-validation-doe | Production config validation does not reject `test_peer`. The route is powerful enough that release builds need both ... | ADDRESSED | `validate_prod_config()` returns error if `test_peer: true` — see § Runtime flag + compile-time gate | R1,R3,R4,R5,R6,R7 |
| 25 | R3 | Medium | test_quality | expired-token-sweeping-lacks | Expired-token sweeping lacks a positive invariant test. Current tests only validate consumption behavior, not bounded... | ADDRESSED | `sweep_expired` test: insert tokens, advance past TTL, assert all swept; insert fresh tokens, assert they remain — see Test Strategy § Property/invariant coverage | R1,R3,R4,R5,R6,R7 |
| 26 | R3 | Low | code_quality | file-map-uses-ambiguous | The file map uses ambiguous `or` targets, which leaves implementers without a concrete edit location. (File: `PLAN_Sp... | ADDRESSED | File map rewritten with one concrete target per row — see § File map | R1,R3,R4,R5,R6,R7 |
| 27 | R3 | Low | test_quality | mode-validation-coverage-too | Mode validation coverage is too narrow and should be table-driven. (File: `PLAN_Sprint25.md`, Location: Test Strategy... | ADDRESSED | Table-driven mode validation: invalid cases `""`, `"wizard"`, `"TEACHER"`, `"student "` → 400; valid `"teacher"`, `"student"` → 202 — see Test Strategy | R1,R3,R4,R5,R6,R7 |
| 28 | R3 | Low | test_quality | config | `Config::dev_default().test_peer` lacks a regression test. (File: `PLAN_Sprint25.md`, Location: Test Strategy regress... | ADDRESSED | `Config::dev_default().test_peer` regression test added — see Test Strategy § Property/invariant coverage | R1,R3,R4,R5,R6,R7 |
| 29 | R5 | High | domain | get-test-peer-session | `get_test_peer_session` is incompatible with the existing session protocol. The planned insert omits `cookie_hash` an... | ADDRESSED | Handler now calls `auth::issue_session_cookie(pool, teacher_id, 180)` which stores SHA-256(raw) as `cookie_hash` and issues raw token as cookie — fully compatible with `resolve_teacher_from_cookie` | R4,R5,R6,R7 |
| 30 | R5 | Medium | security | one-time-token-remains | The one-time token remains in a URL query parameter. That exposes it to access logs and proxy logs. (File: `PLAN_Spri... | ADDRESSED | Route changed to `POST /test-peer/session`; token passed in JSON body — see § New HTTP routes | R1,R4,R5,R6,R7 |
| 31 | R5 | Medium | code_quality | active-bots-lifecycle-ordering | The `active_bots` lifecycle ordering is wrong for fast-exit subprocesses. The cleanup task is planned before the slug... | ADDRESSED | Ordering corrected: slug inserted into `active_bots` before cleanup `tokio::spawn` — see § `active_bots` state and handler step 9 | R1,R4,R5,R6,R7 |
| 32 | R5 | Medium | test_quality | bot-session-ttl-invariant | The bot-session TTL invariant is not verified. The plan does not test that `/test-peer/session` issues `Max-Age=180` ... | ADDRESSED | Test added: `POST /test-peer/session` → assert `Set-Cookie: Max-Age=180` and `expires_at ≈ now_unix+180 ±5s` — see Test Strategy § Property/invariant coverage | R1,R4,R5,R6,R7 |
| 33 | R5 | Low | test_quality | new-window | The new `window._sbSend` bridge has no regression test. A refactor can silently break bot message injection with no a... | ADDRESSED | JS unit test added: stub `WebSocket.send`, call `window._sbSend({type:'Ping'})`, assert `send` called with `'{"type":"Ping"}'` — see Test Strategy § Regression guards and file map | R1,R4,R5,R6,R7 |
| 34 | R5 | Low | test_quality | planned-data-testid-admit | The planned `data-testid="admit-btn"` and `data-testid="session-active"` selectors have no regression tests. Bot-crit... | ADDRESSED | DOM regression tests added to file map: `teacher.test.js` tests `admit-btn`, `student.test.js` tests `session-active` — see § File map and Test Strategy | R1,R4,R5,R6,R7 |
| 35 | R5 | Low | code_quality | get-test-peer-handler | The `get_test_peer` handler design needs an explicit decomposition note. The current ten-step handler shape is too de... | ADDRESSED | Handler now specifies helper extraction: `validate_and_reserve(slug, mode, state)` and `spawn_bot(slug, mode, cmd, args, state)` — see `get_test_peer` handler | R1,R4,R5,R6,R7 |
| 36 | R4 | High | domain | bot-teacher-session-creation | Bot teacher session creation is specified against the wrong session model and will fail authentication or fail to com... | ADDRESSED | Uses `auth::issue_session_cookie(pool, teacher_id, 180)` — same path as normal login — see `get_test_peer_session` handler | R4,R6,R7 |
| 37 | R4 | High | code_quality | active-bots-lifecycle-ordering | `active_bots` lifecycle ordering contains a real race. Cleanup is registered before the slug is inserted, so a fast-e... | ADDRESSED | Ordering fixed: insert slug before spawning cleanup task — see § `active_bots` state code block | R1,R4,R6,R7 |
| 38 | R4 | Medium | test_quality | short-lived-bot-session | The short-lived bot session invariant is not covered by tests. The plan relies on a 180-second TTL as the revocation ... | ADDRESSED | TTL test added — see finding #32 resolution | R1,R4,R6,R7 |
| 39 | R4 | Medium | security | one-time-token-remains | The one-time token remains in a URL query parameter without a redaction layer. This exposes the token in request URIs... | ADDRESSED | POST body — see finding #30 resolution | R1,R4,R6,R7 |
| 40 | R4 | Low | test_quality | new-data-testid-selectors | The new `data-testid` selectors used by the bot still lack regression guards. (File: `PLAN_Sprint25.md`, Location: Te... | ADDRESSED | DOM regression tests added — see finding #34 resolution | R1,R4,R6,R7 |
| 41 | R4 | Low | code_quality | bot-startup-plan-allows | The bot startup plan allows a silence fallback when audio conversion dependencies are missing. That creates a connect... | WONTFIX | Dev-only subjective test tool; silence fallback still exercises the WebRTC and bot flow for non-audio features. `requirements-test-peer.txt` + install note is the correct mitigation. | R1,R4,R6,R7 |
| 42 | R6 | High | code_quality | route-registration-test-peer | Route registration for `/test-peer/session` still uses `get(...)` while the plan, bot flow, and tests require `POST`.... | ADDRESSED | Route changed to `post(test_peer::post_test_peer_session)` in route registration block and handler renamed — see § Runtime flag + compile-time gate | R1,R6,R7 |
| 43 | R6 | High | domain | bot-script-sends-type | The bot script sends `type: 'AccompanimentPlay'` even though `ClientMsg` uses `#[serde(rename_all = "snake_case")]` a... | ADDRESSED | Changed to `type: 'accompaniment_play'` throughout — see § `window._sbSend` exposure and teacher-bot flow step 6 | R6,R7 |
| 44 | R6 | Medium | test_quality | failure-path-coverage-omits | Failure-path coverage omits the distinct `no_teacher` branch in `get_test_peer` when `teachers WHERE slug = $1` retur... | ADDRESSED | `no_teacher` failure-path test added to Test Strategy: nonexistent slug → 404; active_bots unchanged; no token created — see § Failure-path coverage | R1,R6,R7 |
| 45 | R6 | Low | test_quality | failure-path-coverage-omits | Failure-path coverage omits the race where the one-time token is consumed and the teacher row is gone before session ... | ADDRESSED | Token-consumed-then-teacher-deleted test added: consume token → delete teacher → 404; replay → 401 — see § Failure-path coverage | R1,R6,R7 |
| 46 | R6 | Low | security | one-time-token-passed | The one-time token is passed to the bot subprocess as a CLI argument. This exposes the token to local process listing... | ADDRESSED | Token passed via stdin (not CLI arg): server writes `TOKEN\n` to child stdin, closes it; bot reads from `sys.stdin.readline()` — see handler step 7 | R1,R6,R7 |
| 47 | R7 | Medium | test_quality | cleanup-path-integration-tests | Cleanup-path integration tests depend on asynchronous bot cleanup completing before the next request, which creates a... | WONTFIX | `echo` exits in <10ms; 500ms sleep gives 50× headroom. Dev-only tooling; timing tolerance is acceptable. | R1,R7 |
| 48 | R7 | Medium | domain | teacher-wav-variant-selection | The teacher WAV variant selection query omits `a.deleted_at IS NULL`, so a soft-deleted accompaniment can be selected... | ADDRESSED | Added `AND a.deleted_at IS NULL` alongside the existing `v.deleted_at IS NULL` — `find_teacher_asset` in `server/src/http/test_peer.rs` | R7 |
| 49 | R7 | Medium | code_quality | plan-does-not-specify | The plan does not specify how `TokenStore` is stored in shared application state, even though both peer handlers need... | WONTFIX | Code is implemented: `token_store: Arc<TokenStore>` in AppState (debug-only). Plan is archived. | R1,R7 |
| 50 | R7 | Medium | code_quality | file-map-still-names | The file map still names `get_test_peer_session` after the handler was renamed to `post_test_peer_session` (File: `se... | WONTFIX | Plan is archived. Handler is correctly named `post_test_peer_session` in the code. | R1,R7 |
| 51 | R7 | Low | test_quality | wav-variant-404-failure | The WAV-variant 404 failure-path test omits the parallel assertion that no token-store entry is created for the slug ... | WONTFIX | Token is only inserted after the DB check succeeds. The `no_wav_variant` test exercises the failure path where `validate_and_reserve` returns before token insertion; no token entry is created by construction. | R1,R7 |
| 52 | R7 | Low | code_quality | helper-signatures-validate-reser | The helper signatures for `validate_and_reserve` and `spawn_bot` omit return types, leaving the error contract implic... | WONTFIX | Rust requires explicit return types; both functions have explicit return type signatures in code. Plan is archived. | R1,R7 |
| 53 | R7 | Low | test_quality | file-map-omits-new | The file map omits the new Rust integration test file `server/tests/test_peer.rs` (File: `PLAN_Sprint25.md`, Location... | WONTFIX | Plan is archived. Integration tests exist at `server/tests/http_test_peer.rs`. | R1,R7 |
| 54 | R1 | High | code_quality | teacher-bot-cookie-domain | Teacher bot cookie domain derivation crashes before navigation because `about:blank` is truthy and does not have a ho... | ADDRESSED | Domain now derived from `server` arg via `urlparse(server).hostname` — no dependency on `page.url` — `scripts/test_peer.py` | R1 |
| 55 | R1 | High | security | two-concurrent-test-peer | Two concurrent `/test-peer` requests for the same slug can both pass the duplicate check and spawn separate bots beca... | ADDRESSED | Replaced two-step `contains_key` + `insert` with atomic `DashMap::entry()` API — `validate_and_reserve` in `server/src/http/test_peer.rs` | R1 |
| 56 | R1 | Medium | code_quality | mode-validation-table-tautologic | `mode_validation_table` is tautological and never calls the validator. It provides no protection against regressions ... | ADDRESSED | Extracted `is_valid_mode(mode: &str) -> bool`; `mode_validation_table` test now calls it directly — `server/src/http/test_peer.rs` | R1 |
| 57 | R1 | Medium | code_quality | active-bots-guard-prevents | `active_bots_guard_prevents_duplicate` only exercises `DashMap` operations and does not test the handler guard. It ad... | ADDRESSED | Renamed to `active_bots_entry_api_is_atomic`; tests `Entry::Vacant` insert + `Entry::Occupied` detection + removal — `server/src/http/test_peer.rs` | R1 |
| 58 | R1 | Medium | test_quality | there-no-integration-test | There is no integration test for the teacher-mode 202 success path. The primary happy path for teacher bot launch is ... | ADDRESSED | Added `test_peer_teacher_mode_with_variant_returns_202`: registers teacher, seeds WAV variant, asserts 202 + body — `server/tests/http_test_peer.rs` | R1 |
| 59 | R1 | Medium | test_quality | window | `window._sbSend` is tested by source-string presence instead of behavior. The serialization contract for `sendRaw` is... | WONTFIX | Test 1 in `teacher.test.js` verifies `Signalling.send` JSON serialization behaviorally. Tests 2–3 are source-level regression guards ensuring the feature isn’t accidentally removed; this is the intent. | R1 |
| 60 | R1 | Medium | test_quality | test-peer-bot-exit | `test_peer_bot_exit_clears_active_bots` uses a fixed 500 ms sleep. The test is timing-sensitive and can flake under l... | WONTFIX | `echo` subprocess exits in <5ms; 500ms gives 100× headroom. Dev-only tooling on local machine. Acceptable trade-off. | R1 |
| 61 | R1 | Medium | test_quality | there-no-http-level | There is no HTTP-level test for token-store capacity exhaustion returning 503 `bot_capacity`. The handler mapping is ... | ADDRESSED | Added `test_peer_token_cap_returns_503`: fills store to cap=100 directly, then asserts HTTP 503 + `bot_capacity` + active_bots stays empty — `server/tests/http_test_peer.rs` | R1 |
| 62 | R1 | Medium | test_quality | test-peer-bad-script | `test_peer_bad_script_returns_503` does not assert that `active_bots` stays empty after spawn failure. A future inser... | ADDRESSED | Added `assert!(!app.state.active_bots.contains_key("myroom"))` after 503 assertion — `server/tests/http_test_peer.rs` | R1 |
| 63 | R1 | Medium | security | chromium-launched-with-no | Chromium is launched with `--no-sandbox` unconditionally. This removes browser-process isolation in the dev bot runti... | WONTFIX | Dev-only tooling for local manual testing. `--no-sandbox` is standard practice for Playwright in non-root dev environments. Not a production security boundary. | R1 |
| 64 | R1 | Medium | security | bot-session-cookie-issued | The bot session cookie is issued without conditionally adding `Secure`. HTTPS debug deployments would send a non-secu... | ADDRESSED | `Secure` attribute added when `state.config.base_url.scheme() == "https"` — `post_test_peer_session` in `server/src/http/test_peer.rs` | R1 |
| 65 | R1 | Low | test_quality | test-peer-session-ttl | `test_peer_session_ttl_expires_at_180s` selects rows with a TTL-based filter that depends on unrelated session lifeti... | ADDRESSED | Query now filters `WHERE expires_at < now + 300` to isolate bot session (180s TTL) from teacher session (30-day TTL) — `server/tests/http_test_peer.rs` | R1 |
| 66 | R1 | Low | code_quality | active-bots-compiled-into | `active_bots` is compiled into release builds while adjacent debug-only test-peer state is gated out. This is dead re... | ADDRESSED | `active_bots` gated behind `#[cfg(debug_assertions)]` in `AppState` (`state.rs`), construction in `main.rs`, and test harness — same gate as `token_store` | R1 |
| 67 | R1 | Low | code_quality | import-json-unused | `import json` is unused. (File: `scripts/test_peer.py`) (Source: code_quality) | ADDRESSED | Removed `import json` — `scripts/test_peer.py` | R1 |
| 68 | R1 | Low | code_quality | test-peer-script-names | `test_peer_script` names a command/interpreter override as if it were a script path. The naming is misleading. (Files... | WONTFIX | When `test_peer_script = Some("echo")`, `echo` acts as the script (receives py file path as arg). Config name is consistent with `SB_TEST_PEER_SCRIPT` env var. Rename would be churn. | R1 |
| 69 | R1 | Low | code_quality | teacher-bot-student-bot | `teacher_bot` and `student_bot` leave `page` untyped. This breaks the file’s otherwise consistent typing style. (File... | WONTFIX | Python type hints on Playwright `Page` objects require importing `playwright.async_api.Page` which adds a hard import-time dependency on playwright. Dev-only script; acceptable. | R1 |
