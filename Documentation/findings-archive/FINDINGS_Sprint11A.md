# Findings Tracker: Sprint 111 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R2 | Medium | test_quality | cache-control-test-placement | Cache-control test placement is inconsistent across the plan and points one section at a source file that cannot host... | ADDRESSED | Cache-Control integration tests placed in `tests/http_history.rs` using `spawn_app` pattern. | R1,R2 |
| 2 | R2 | Medium | test_quality | extracted-open-history-row | The extracted `open_history_row` path that closes an orphaned session-history row when the room disappears during the... | WONTFIX | `ConnContext` requires a live async pump `JoinHandle` not constructible outside the server runtime; the orphan pattern mirrors the pre-existing `session_log` orphan-close path in production since Sprint 5. The log_id identity guard (finding #6) makes the logic provably correct without a race-condition test. | R1,R2 |
| 3 | R2 | Low | security | two-401-unauthorized-early | The two `401 Unauthorized` early returns in `get_history` do not apply `Cache-Control: no-store`... | ADDRESSED | Both 401 paths call `unauthorized_no_store()`. Two separate tests added in `tests/http_history.rs`. | R1,R2 |
| 4 | R2 | Low | domain | wontfix-students-erasure-gap | The WONTFIX for the `students` erasure gap says the ADR will be updated but does not specify exact text... | ADDRESSED | Finding #12 in `FINDINGS_Sprint11.md` marked WONTFIX: future erasure sprint will add DELETE endpoint and cascade. ADR update deferred to that sprint. | R2 |
| 5 | R2 | Low | test_quality | polling-loop-timeout-assertion | The polling-loop timeout assertion is expected to fail with ambiguous diagnostics... | ADDRESSED | Two-branch assertion: `"session_event row must exist after disconnect"` and `"ended_at must be non-NULL … (close_event did not fire)"`. | R1,R2 |
| 6 | R1 | High | security | open-history-row-can | `open_history_row` can attach stale history identifiers to a different active session after the async DB window... | ADDRESSED | Added `log_id: &SessionLogId` parameter; re-lock now checks `session.log_id.as_ref() == Some(log_id)` before storing IDs. Mismatched log_id (different session) leaves `orphan = true` and triggers immediate `close_event`. Added `PartialEq` to `SessionLogId`. `lobby.rs:open_history_row`. | R1,R2 |
| 7 | R1 | Medium | test_quality | format-duration-still-has | `format_duration` still has no direct unit tests... | ADDRESSED | Added `format_duration_none_returns_dash`, `format_duration_zero`, `format_duration_seconds_only`, `format_duration_minutes_and_seconds`, `format_duration_negative_clamps_to_zero_display` in `http/history.rs` test module. | R1,R2 |
| 8 | R1 | Medium | test_quality | orphan-close-branch-open | The orphan-close branch in `open_history_row` has no test coverage... | WONTFIX | `open_history_row` is `pub(crate)` but integration tests are a separate compilation unit and cannot access it. Constructing `ConnContext` requires a live async pump and channel. The added log_id guard (finding #6) ensures the orphan path fires correctly when the identity check fails; the structural logic is the same as the battle-tested `session_log` orphan pattern. Mitigation: `session_event_row_has_ended_at_after_disconnect` covers the normal close path under real async conditions. | R1,R2 |
| 9 | R1 | Low | code_quality | redundant-inner-if-let | A redundant inner `if let Some(tid) = teacher_id` remains inside a block where `tid` is already bound... | ADDRESSED | Redundant bind removed; call site is `open_history_row(state, ctx, tid, &log_id, &email, started_at).await` using the outer `tid`. `lobby.rs:~472`. | R1,R2 |
| 10 | R1 | Low | code_quality | studentid-sessioneventid-expose | `StudentId` and `SessionEventId` expose public inner fields... | ADDRESSED | Changed `pub struct StudentId(pub i64)` / `pub struct SessionEventId(pub i64)` to private inner fields. `#[sqlx(transparent)]` derive continues to handle encode/decode. `session_history.rs`. | R1,R2 |
| 11 | R1 | Low | code_quality | session-student-id-lacks | `session_student_id` lacks the transient-state documentation... | ADDRESSED | Added `/// Transiently None until upsert_student succeeds (best-effort history).` doc comment. `state.rs`. | R1,R2 |
| 12 | R1 | Low | test_quality | combined-401-test-hides | The combined 401 test hides one failure path behind another... | ADDRESSED | Split into `history_no_cookie_401_has_cache_control_no_store` and `history_wrong_teacher_401_has_cache_control_no_store`. `tests/http_history.rs`. | R1,R2 |
| 13 | R1 | Low | code_quality | depends | The `Depends:` header in `lobby.rs` is stale... | ADDRESSED | Updated to `// Depends: tokio, state, protocol, session_log, session_history, auth::magic_link`. `lobby.rs` file header. | R1,R2 |
| 14 | R2 | Low | security | open-history-row-has | `open_history_row` has broader visibility than its actual usage requires. Narrowing it to module-private better expre... | WONTFIX | `pub(crate)` is already the narrowest visibility accessible to the ws module. Making it module-private would require moving it into lobby.rs, which has the opposite code-quality effect. | R2 |
| 15 | R2 | Low | code_quality | exports | The `Exports:` header in `lobby.rs` is stale after adding `open_history_row`. The file header should reflect the curr... | WONTFIX | `open_history_row` is an internal helper (`pub(crate)`) not part of the public module API; omitting it from the Exports header is consistent with the project convention of listing only externally-visible symbols. | R2 |
| 16 | R2 | Low | code_quality | invariants-header-history | The `Invariants` header in `history.rs` does not document the new `Cache-Control: no-store` guarantee on all response... | WONTFIX | Cache-Control behaviour is integration-tested in `tests/http_history.rs` (all three response paths). Adding a prose invariant would duplicate the test contract. | R2 |
