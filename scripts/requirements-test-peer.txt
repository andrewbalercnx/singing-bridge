# Requirements for scripts/test_peer.py (test-peer bot).
# Install with: pip install -r scripts/requirements-test-peer.txt
# Then run: playwright install chromium
playwright>=1.40
gtts>=2.5
pydub>=0.25
httpx>=0.27
