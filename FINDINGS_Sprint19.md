# Findings Tracker: Sprint 19 (code)

Editor: Update the **Status** and **Resolution** columns after addressing each finding.
Status values: `OPEN` | `ADDRESSED` | `VERIFIED` | `WONTFIX` | `REOPENED`

| # | Round | Severity | Lens | Tag | Finding | Status | Resolution | Routed |
|---|-------|----------|------|-----|---------|--------|------------|--------|
| 1 | R1 | High | test_quality | plan-omits-mandatory-test | The plan omits the mandatory `## Test Strategy` section required for plan approval. (File: `PLAN_Sprint19.md`, Locati... | ADDRESSED | ADDRESSED | R1,R3 |
| 2 | R1 | High | test_quality | plan-deletes-sprint-16 | The plan deletes Sprint 16 database-behavior regression coverage without defining equivalent PostgreSQL regression gu... | ADDRESSED | ADDRESSED | R1,R3 |
| 3 | R1 | High | code_quality | proposed-phase-7-harness | The proposed Phase 7 harness uses `PgConnectOptions::to_url_lossy()`, which does not exist in `sqlx` 0.8 and will not... | ADDRESSED | ADDRESSED | R1,R3 |
| 4 | R1 | High | security | plan-does-not-enforce | The plan does not enforce authenticated PostgreSQL TLS at runtime. It preserves `sslmode=require` semantics and adds ... | ADDRESSED | ADDRESSED | R1,R3 |
| 5 | R1 | High | domain | plan-keeps-startup-run | The plan keeps startup-run migrations in the application path even though the runtime role model does not permit DDL ... | ADDRESSED | ADDRESSED | R3 |
| 6 | R1 | High | domain | plan-removes-durable-blob | The plan removes durable blob-backed storage without providing a replacement, so the stated persistence outcome for t... | ADDRESSED | ADDRESSED | R3 |
| 7 | R1 | Medium | test_quality | testapp-cleanup-not-specified | `TestApp` cleanup is not specified as panic-safe, so failed tests can leak per-test PostgreSQL databases. (File: `PLA... | ADDRESSED | ADDRESSED | R1,R3 |
| 8 | R1 | Medium | test_quality | plan-does-not-define | The plan does not define coverage for PostgreSQL `CITEXT` behavior even though it relies on it for case-insensitive l... | ADDRESSED | ADDRESSED | R1,R3 |
| 9 | R1 | Medium | domain | plan-assumes-citext-available | The plan assumes `CITEXT` is available in every new database, but the extension is per-database and must be provision... | ADDRESSED | ADDRESSED | R3 |
| 10 | R1 | Medium | test_quality | exit-criteria-internally-inconsi | Exit criteria are internally inconsistent about the expected test surface, so approval cannot be checked unambiguousl... | ADDRESSED | ADDRESSED | R1,R3 |
| 11 | R1 | Low | test_quality | proposed-100-ms-connection | The proposed 100 ms connection-acquire timeout is CI-fragile and should be increased. (File: `PLAN_Sprint19.md`, Loca... | ADDRESSED | ADDRESSED | R1,R3 |
| 12 | R1 | Low | code_quality | plan-raises-max-connections | The plan raises `max_connections` to 10 without documenting the shared-server connection budget. (File: `PLAN_Sprint1... | ADDRESSED | ADDRESSED | R1,R3 |
| 13 | R1 | Low | code_quality | phase-3-documentation-names | Phase 3 documentation names `dev_mail_dir` but omits `dev_blob_dir`, leaving the config impact incomplete. (File: `PL... | ADDRESSED | ADDRESSED | R1,R3 |
| 14 | R1 | Low | test_quality | plan-leaves-execution-policy | The plan leaves the execution policy for Postgres-dependent inline tests in `cleanup.rs` undefined. (File: `PLAN_Spri... | ADDRESSED | ADDRESSED | R1,R3 |
| 15 | R1 | Low | security | proposed-dev-default-hardcodes | The proposed dev default hardcodes local database credentials in application code. (File: `PLAN_Sprint19.md`, Locatio... | ADDRESSED | ADDRESSED | R1,R3 |
| 16 | R2 | High | test_quality | mandatory-test-planning-missing | Mandatory test planning is missing, and critical regression/security-of-behavior checks are not codified. The plan la... | ADDRESSED | ADDRESSED | R1,R3 |
| 17 | R2 | High | test_quality | plan-permits-cleanup | The plan permits `cleanup.rs` database tests to be marked `#[ignore]`, which removes CI coverage from the only schedu... | ADDRESSED | ADDRESSED | R1,R3 |
| 18 | R2 | High | code_quality | sql-placeholder-rewrite-scope | The SQL placeholder rewrite scope is incomplete. Three production files using `?` placeholders are omitted, and the `... | ADDRESSED | ADDRESSED | R1,R3 |
| 19 | R2 | High | domain | migration-plan-omits-recording | The migration plan omits the `recording_sessions.session_event_id` type change required to keep the foreign key compa... | ADDRESSED | ADDRESSED | R3 |
| 20 | R2 | High | domain | plan-uses-one-sb | The plan uses one `SB_DATABASE_URL` for both migrations and runtime access, which conflicts with ADR 0002’s `sbmigrat... | ADDRESSED | ADDRESSED | R3 |
| 21 | R2 | High | security | production-database-tls-remains | Production database TLS remains at `sslmode=require`, so hostname and certificate verification stay disabled. With `t... | ADDRESSED | ADDRESSED | R1,R3 |
| 22 | R2 | Medium | security | production-configuration-path-st | The production configuration path still allows a localhost fallback DSN with embedded credentials when `SB_DATABASE_U... | ADDRESSED | ADDRESSED | R1,R3 |
| 23 | R2 | Medium | test_quality | test-database-cleanup-remains | Test database cleanup remains unresolved between explicit per-test cleanup and an automatic wrapper strategy. The pla... | ADDRESSED | ADDRESSED | R1,R3 |
| 24 | R2 | Medium | code_quality | plan-does-not-specify | The plan does not specify exact replacement defaults for `dev_mail_dir` and `dev_blob_dir` after decoupling from `dat... | ADDRESSED | ADDRESSED | R1,R3 |
| 25 | R3 | Medium | test_quality | plan-does-not-define | The plan does not define how `server/src/cleanup.rs` inline tests will obtain create/migrate/drop database access fro... | ADDRESSED | cleanup.rs inline tests now use `crate::db::test_helpers::make_test_db()` which creates, migrates, and auto-drops the per-test DB via RAII Drop. | R1,R3 |
| 26 | R3 | Medium | test_quality | exit-criterion-session-persisten | The exit criterion for session persistence across server restart has no corresponding test specification. The test st... | ADDRESSED | Added `session_persists_in_postgresql_across_restart` in `tests/regression.rs`: verifies session row is visible via a fresh independent pool (simulating a restarted server). | R1,R3 |
| 27 | R3 | Low | security | production-sslmode-verify-full | Production `sslmode=verify-full` validation uses whole-string substring matching. This can accept URLs where the lite... | ADDRESSED | Already fixed: `validate_prod_config` uses `split('&').any(|p| p == "sslmode=verify-full")` for exact parameter-level matching. Test `prod_verify_full_substring_not_enough` covers the embedded-in-db-name edge case. | R1,R3 |
| 28 | R3 | Low | security | production-localhost-rejection-i | Production localhost rejection is incomplete because it does not cover IPv6 loopback forms such as `[::1]`. Extend th... | ADDRESSED | Already fixed: `validate_prod_config` checks `contains("[::1]")`. Test `prod_ipv6_loopback_db_errors` covers this. | R1,R3 |
| 29 | R3 | Low | test_quality | per-test-database-naming | Per-test database naming is process-local. Parallel `cargo test` processes can generate identical database names and ... | ADDRESSED | DB names include process ID (`singing_bridge_test_{pid}_{n}`). Parallel test processes have distinct PIDs; the monotonic counter prevents same-PID intra-process collisions. | R1,R3 |
| 30 | R3 | Low | test_quality | fk-regression-guard-not | The FK regression guard is not verifiable as written because no planned test deletes a teacher record. Add a concrete... | ADDRESSED | Added `fk_prevents_teacher_deletion_with_active_session` in `tests/regression.rs`: attempts DELETE on a teacher with a live session and asserts FK rejection. | R1,R3 |
| 31 | R3 | Low | code_quality | test-db-url-drops | `test_db_url` drops query parameters from `DATABASE_TEST_URL` when constructing per-test URLs. This breaks TLS-requir... | ADDRESSED | All URL construction now uses `url::Url::parse` + `set_path` to replace only the database-name segment, preserving query strings (sslmode etc.). Common helper in `db::test_helpers::replace_db_name`. | R1,R3 |
| 32 | R3 | Low | domain | optional-migrate-subcommand-fall | The optional migrate subcommand falls back to `SB_DATABASE_URL`, which is the DML-only application credential. The fa... | WONTFIX | No migrate subcommand exists in `main.rs`; migrations are run as a separate CLI step per ADR 0002. Finding doesn't apply to the current implementation. | R3 |
| 33 | R3 | Low | domain | phase-5-description-0006 | The Phase 5 description of `0006_accompaniments.sql` incorrectly places `duration_s` on `accompaniments` instead of `... | WONTFIX | Error was in the now-archived PLAN_Sprint19.md, not in migration SQL. The actual migration file is correct. | R3 |
| 34 | R1 | High | code_quality | test-created-postgresql-database | Test-created PostgreSQL databases are not reliably dropped, so test runs leak databases across the suite and inline t... | ADDRESSED | `TestApp` now implements `Drop` (thread+runtime pattern for async cleanup from sync context); `TestDb` in `db::test_helpers` provides the same for inline tests. Both drop the database even on test panic. | R1 |
| 35 | R1 | High | security | signup-rate-limiting-still | Signup rate limiting still has a check-then-insert race under PostgreSQL `READ COMMITTED`, so concurrent requests can... | ADDRESSED | `rate_limit.rs`: INSERT now runs before SELECT COUNT so each transaction observes its own attempt; the race window is narrowed to concurrent transactions that INSERT before either commits. Reviewer-suggested fix (INSERT-before-COUNT). | R1 |
| 36 | R1 | Medium | test_quality | committed-citext-regression-guar | The committed CITEXT regression guard for `teachers.email` case-insensitivity is missing, so the PostgreSQL replaceme... | ADDRESSED | Added `citext_email_column_is_case_insensitive` and `citext_unique_constraint_is_case_insensitive` in `tests/regression.rs`. | R1 |
| 37 | R1 | Medium | code_quality | postgresql-test-database-url | PostgreSQL test database URL construction is duplicated in multiple places, which increases maintenance cost and make... | ADDRESSED | Shared `db::test_helpers` module (`make_test_db`, `replace_db_name`) used by all inline tests. Integration tests share `tests/common/mod.rs`. `db_pool.rs` now uses `spawn_app()`. | R1 |
| 38 | R1 | Medium | test_quality | foreign-key-enforcement-regressi | The foreign-key enforcement regression previously covered by `db_pragmas.rs` no longer has an explicit replacement te... | ADDRESSED | Added `fk_session_event_requires_valid_teacher` in `tests/regression.rs`: inserts a session_event with a non-existent teacher_id and asserts FK violation. | R1 |
| 39 | R1 | Medium | test_quality | cleanup-test-named-blob | The cleanup test named `blob_delete_failure_retains_key` asserts the opposite behavior and still does not cover the r... | ADDRESSED | Renamed test to `blob_delete_not_found_is_treated_as_purged`; updated assertions and comment to accurately describe DevBlobStore's NotFound-as-success semantics. | R1 |
| 40 | R1 | Medium | test_quality | parse-env-does-not | `parse_env()` does not have a direct unit test for missing `SB_DATABASE_URL`, so the new mandatory environment branch... | ADDRESSED | Added `parse_env_missing_database_url_errors` unit test in `config.rs` that removes the env var and asserts `ConfigError::Missing("SB_DATABASE_URL")`. | R1 |
| 41 | R1 | Low | code_quality | testapp | `TestApp::shutdown()` is an attractive misuse path because it omits database cleanup while coexisting with `cleanup()... | ADDRESSED | `cleanup()` removed; `shutdown()` is now the single teardown path: cancels server, awaits handle, closes pool, drops DB. Drop is the panic-safe fallback. | R1 |
| 42 | R1 | Low | code_quality | password | `password.rs` uses an identity `and_then` where `map` is the correct and clearer operation (File: `server/src/auth/pa... | ADDRESSED | Changed `last_success.and_then(|(t,)| Some(t))` to `last_success.map(|(t,)| t)`. | R1 |
| 43 | R1 | Low | security | production-db-url-validation | Production DB URL validation rejects DSN-style connection strings with a generic `sslmode=verify-full` error rather t... | ADDRESSED | Added early check in `validate_prod_config`: non-`postgres://` strings return `Invalid("SB_DATABASE_URL", "must be a URL starting with postgres://...")`. Test `prod_dsn_style_url_errors` added. | R1 |
| 44 | R1 | Low | security | infra-bicep-container-app | `infra/bicep/container-app.bicep` omits Cloudflare IPv6 ranges from `ipSecurityRestrictions`, which is an availabilit... | ADDRESSED | Added all 7 Cloudflare IPv6 ranges (`cf-v6-1` through `cf-v6-7`) to `ipSecurityRestrictions` with source comment. | R1 |
